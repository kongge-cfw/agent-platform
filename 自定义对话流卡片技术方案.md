# 自定义对话流卡片技术方案

| 项 | 内容 |
|---|---|
| 文档状态 | 方案（未开工） |
| 适用版本 | 当前主干已落地能力 + 本方案拟增能力 |
| 读者 | 平台研发、业务系统对接方 |
| 相关代码 | `ask_user_question` / `request_user_confirmation`、`EmbedChat` / `AgentDebug`、`UserQuestionStore` |
| 对照系统 | yunshu（AgentScope Java，闭集 Vue 卡 + formSchema） |

---

## 1. 背景与目标

### 1.1 问题

业务系统需要在 NanZi 对话里展示**复杂定版页面**（巡检确认、分配确认、明细核对等），并带确认 / 驳回 / 修改等交互。yunshu 能做到类似体验，但做法是把卡片编译进同一套前端。

NanZi 是**基础平台**：卡片版式属于业务系统，不能把巡检页、下发页写进平台仓库。

### 1.2 目标

1. 对话流具备与 yunshu 同级的「停下来 → 出可交互页 → 收回动作 → 继续跑」能力。
2. **平台只做协议、宿主、登记、安全**；**业务系统自己画卡**。
3. 复用现有 HITL（`awaiting_user` + 结构化用户回执），不另起步骤机、不另起 Channel/AG-UI。
4. 模型禁止输出任意 URL / HTML，只能引用已登记的 `card_key`。

### 1.3 非目标

- 不在平台内做可视化表单设计器。
- 不把整站业务工作台嵌进对话。
- 不改变现有 Embed 方向（业务站嵌 NanZi 仍然保留，与本方案正交）。
- 首版不做远程 Vue 组件热加载、不做微前端运行时。
- 不做 yunshu 那种会话步骤机（Plan → Confirm → Assign）。步骤留在业务提示词和工具顺序里。

---

## 2. 现状能力盘点（仅已落地）

### 2.1 HITL 主链路（可直接复用）

| 环节 | 已有实现 | 说明 |
|---|---|---|
| 中断工具 | `ask_user_question`、`request_user_confirmation` | 返回 `status=awaiting_user`，本轮停止 |
| SSE | `user_question`、`business_confirmation` | runner 在工具结果处组装并 yield |
| 终态 | `execution_status=awaiting_user` | `_turn_status_signal` 已认 `user_question`；`FinalizeStep` 将 `awaiting_user` 视为可恢复暂停 |
| 提问落库 | `UserQuestionStore`（Redis，TTL 900s） | 绑定 `user_id + conversation_id + question_id` |
| 回执续跑 | `【用户回答】` / `【业务确认】` | 作为下一条用户消息进入模型；路由对确认回执有 follow-up 识别 |
| 取消闸门 | `arm_cancel_confirmation_gate` | 用户刚取消时禁止再次弹确认卡 |

现有回执形态（须保持兼容，新卡另开前缀）：

```
【用户回答】
interaction_type: question
question_id: uq_xxx
selected_option_ids: ["a"]
custom_input: ...

【业务确认】用户已确定
confirmation_id: bc_xxx
请根据以下已确认字段继续执行：
- 字段名 (key): value
```

### 2.2 对话内已有卡片（均为平台写死 Vue）

| 卡片 | 组件 | 是否 HITL |
|---|---|---|
| 选项提问 | `UserQuestionCard.vue` | 是 |
| 扁平字段确认 | `BusinessConfirmationCard.vue` | 是 |
| 工具权限 | `ToolPermissionCard.vue` | 是（权限，不是业务卡） |
| 错误详情 | `ErrorDetailCard.vue` | 否 |
| 任务清单 | `ChatTodoCard.vue` | 否 |
| ChatBI 计划 / 数据集 / 报表 | `SqlPlanCard` 等 | 否 |
| 工作区 Canvas | iframe 预览 HTML/PDF | **不是**业务确认页 |

渲染入口：`EmbedChat.vue`、`AgentDebug.vue`，SSE 解析在 `agentscopeSseHandlers.ts`。

**没有**卡片注册表，**没有**按 `card_key` 挂第三方 UI。

### 2.3 业务系统现有接入面（方向与本方案不同）

| 能力 | 现状 | 与对话内卡片的关系 |
|---|---|---|
| Embed | `/embed/chat` + Ticket + `INIT_CONFIG` / `UPDATE_CONTEXT` | **业务嵌 NanZi**，解决助手进业务站，不解决卡进对话 |
| Embed 上行 | `NANZI_WIDGET_READY`、会话切换、点赞、打开数据门户 | **无**卡片提交事件 |
| HTTP 工具 `sys_api_tools` | `url_template` 仅后端发 HTTP | **不会**在对话框出 iframe |
| MCP | 出站工具调用 | 同样不出 UI |
| 场景模板 | 安装智能体套件 | 不是业务详情页 |

### 2.4 与 yunshu 的差距（要对齐的是体验，不是实现形态）

| yunshu | 本平台已有 | 缺口 |
|---|---|---|
| 闭集 Vue 卡 + formSchema | 2 张通用 HITL 卡 | 无业务自定义卡 |
| `@Tool` 写 `pendingInteraction` 后中断 | `awaiting_user` + 回执 | 缺通用 `interaction_type` |
| `RESUME_INTERACTION` | 下一条用户消息续跑 | 缺 `card_id + action + payload` |
| 历史 `card_sd_*` | 问卡 Redis；确认卡偏前端态 | 缺通用卡快照 |
| 模型只出 JSON | 工具入参白名单 | 缺 `card_key` 登记校验 |

yunshu 能快，是因为前端、编排、业务是一家。本平台不能把业务页搬进 `frontend/src/components`。

---

## 3. 总体方案

### 3.1 一句话

**登记卡种 + 业务 iframe 渲染 + 统一 HITL 回执。**  
平台提供宿主和协议；业务系统提供页面；智能体只传 `card_key` 和数据。

### 3.2 能力分层（必须分开，禁止混用）

| 层 | 谁画 UI | 何时用 | 现状 |
|---|---|---|---|
| **A. 平台通用卡** | 平台 Vue | 选项提问、扁平字段确认 | **已有，继续用** |
| **B. 业务自定义卡** | 业务系统页面（iframe） | 复杂定版 + 确认/驳回 | **本方案主路径** |
| **C. Embed 反嵌** | 业务站嵌 NanZi | 工作台里挂助手 | **已有，正交，不解决 B** |

简单「是/否」或少于约 10 个扁平字段，继续走 A，不要为每张确认登记业务页。

### 3.3 运行时总览

```
模型调用 show_ui_card(card_key, title, data, actions)
        │
        ▼
平台校验登记表（key / 智能体 / actions / origin）
        │
        ▼
签发短时 card_token，写入 pending（Redis，仿 UserQuestionStore）
        │
        ▼
SSE type=ui_card  →  execution_status=awaiting_user
        │
        ▼
对话气泡：平台外壳 + iframe(登记 URL?embed=1&card_token=...)
        │
        ├─ NanZi → 卡：NANZI_CARD_INIT { card_id, data, actions }
        ├─ 卡 → NanZi：NANZI_CARD_READY / RESIZE / SUBMIT
        │
        ▼
平台校验 action ∈ allowed_actions，转用户回执：
【UI卡片】
interaction_type: ui_card
card_id / card_key / action / payload
        │
        ▼
智能体续跑；写库只走已有 HTTP / MCP 工具（卡本身不写业务库）
```

### 3.4 设计原则

1. **卡只展示和收集，不写业务库。** 与现确认卡「本工具不会写入任何业务系统」一致。
2. **模型不见 URL。** 工具入参只有 `card_key`；URL 由登记表解析。
3. **一种工具、一种 SSE、一种回执前缀。** 不为每种业务卡加平台 Vue。
4. **同一会话同时最多一张 pending 业务卡。** 新卡将旧卡标为 `superseded`（对齐问卡 store）。
5. **Python 3.11 / 现有 Vue 3 栈。** MySQL 与 PostgreSQL 迁移分目录，不可混用。

---

## 4. 数据模型

主库新增一张卡种登记表。MySQL 放 `db-prod/`，PostgreSQL 放 `db-prod-pg/`，版本号以实施时两端最新 +1 为准（本稿参考点：MySQL 已至 V154，PG 已至 V55）。

### 4.1 `sys_ui_cards`（卡种，不是单据）

| 列 | 类型建议 | 说明 |
|---|---|---|
| `id` | CHAR(36) PK | UUID |
| `card_key` | VARCHAR(64) UNIQUE | 模型唯一引用，如 `inspection_confirm` |
| `name` | VARCHAR(128) | 管理端展示名 |
| `description` | TEXT | 给管理员 / 工具 description 摘要 |
| `render_type` | VARCHAR(16) | 首版固定 `iframe` |
| `url` | VARCHAR(1024) | 业务卡页，须 HTTPS 或内网白名单主机；查询串由平台追加 |
| `allowed_origins` | TEXT JSON | iframe 源白名单，如 `["https://biz.xxx.com"]` |
| `allowed_actions` | TEXT JSON | 如 `["confirm","reject","edit"]` |
| `allowed_agent_ids` | TEXT JSON | 空 = 所有已勾选本工具的智能体；非空则再限制 |
| `default_height` | INT | iframe 初始高度，默认 480，范围 240–1200 |
| `token_ttl_seconds` | INT | card_token TTL，默认 600，范围 60–1800 |
| `is_active` | TINYINT/BOOL | 停用后工具调用失败 |
| `created_by` / `updated_by` | 用户 id | |
| `created_at` / `updated_at` | DATETIME | |

索引：`uk_card_key`；`idx_is_active`。

### 4.2 Redis：pending 实例（不落业务单据表）

键设计对齐 `UserQuestionStore`：

```
ai:ui-card:{user_id}:{conversation_id}:{card_id}
ai:ui-card:active:{user_id}:{conversation_id}
ai:ui-card:token:{card_token}
```

记录字段：

```json
{
  "card_id": "uc_ab12...",
  "card_key": "inspection_confirm",
  "conversation_id": "...",
  "user_id": "...",
  "agent_id": "...",
  "tool_call_id": "...",
  "title": "请确认巡检结果",
  "data": {},
  "actions": ["confirm", "reject"],
  "render_url": "https://biz.../cards/inspection",
  "status": "pending",
  "created_at": 0,
  "expires_at": 0
}
```

`status`：`pending` | `submitted` | `superseded` | `expired`。  
TTL 默认 1800s（长于 card_token，避免页还开着实例已丢）。

### 4.3 `card_token`（短时、一次性会话凭证）

- 平台在 `show_ui_card` 成功时签发，写入 Redis `ai:ui-card:token:{token}`。
- 载荷：`card_id`、`conversation_id`、`user_id`、`card_key`、`allowed_origin`。
- iframe URL 只带 `embed=1` 与 `card_token`，**不带**平台主 API Key、用户密码。
- 业务页可用 token 调平台只读接口拉取 `data`（防 postMessage 被截断 / 刷新丢失）；或仅信 INIT 消息。首版两种都支持，以 INIT 为主、token 拉取为辅。
- 提交成功或过期后 token 失效（GETDEL 或标记 used）。

不把完整 `data` 永久写入主库。历史回放：助手消息侧持久化 SSE 快照（`card_key` / `title` / `data` 摘要 / `status`），与现问卡事件随会话历史走同一套消息表即可；超大 `data` 只存 Redis + 回放时若过期则显示「卡片已过期，请重新发起」。

### 4.4 权限资源

新增（迁移种子，与 `V30-register_ui_resources` 同模式）：

- 菜单：`menu:ui_cards`（对话卡片，侧栏与 MCP 工具集并列）
- 按钮：`element:ui_cards:create` / `edit` / `delete`

智能体是否可用本能力：版本 `tools` JSON 勾选 `show_ui_card`，并在「工具能力」中勾选可用 `card_key`（现有 `ai_agents.tools` 列表即可承载工具名；卡种白名单见 §8.1）。

---

## 5. 平台运行时协议

### 5.1 工具 `show_ui_card`

新增系统工具，注册进 `ToolRegistry`，`tool_policy` 元数据对齐提问卡：

| 项 | 值 |
|---|---|
| `capability` | `ui_card` |
| `source` | `platform_control` |
| `side_effect` | `control` |
| `confirmation` | `ask` |
| `nudge_mode` | `explicit` |
| 并行 | 与 `request_user_confirmation` / `ask_user_question` 一样，**禁止与写库工具同轮并行提交**（沿用现有交互工具隔离） |

**入参（模型可见）：**

```python
class ShowUiCardArgs(BaseModel):
    card_key: str          # 已登记且启用
    title: str             # 气泡标题，≤ 80 字
    data: dict             # 灌给业务页的 JSON，序列化后 ≤ 64KB
    actions: list[str]     # 非空，且 ⊆ 登记 allowed_actions
```

禁止字段：`url`、`html`、`src`、任意脚本。校验失败返回 `status=error`，**不**中断为 awaiting_user。

**成功返回（工具输出 / 供 SSE 组装）：**

```json
{
  "status": "awaiting_user",
  "interaction_type": "ui_card",
  "card_id": "uc_...",
  "card_key": "inspection_confirm",
  "title": "请确认巡检结果",
  "message": "已向用户展示业务卡片，请等待提交后再继续。",
  "actions": ["confirm", "reject"],
  "data": {},
  "render": {
    "type": "iframe",
    "url": "https://biz.example.com/cards/inspection?embed=1&card_token=uct_...",
    "origin": "https://biz.example.com",
    "height": 480
  }
}
```

`render.url` 只出现在 SSE / 前端，**不要**把带 token 的完整 URL 再喂回模型（工具结果对模型可只回 `card_id/card_key/message`，完整 render 走独立 SSE 字段）。推荐：工具对模型的可见文本精简，SSE 带完整 render。

### 5.2 SSE `ui_card`

在 `assistant_agent_runner` / ChatBI `react_stream` 工具收口处，与现有两卡并列：

```python
# 现有
build_business_confirmation_sse(...)
build_user_question_sse(...)
# 新增
build_ui_card_sse(tool_name, tool_output, tool_call_id)
```

事件示例：

```json
{
  "type": "ui_card",
  "tool_call_id": "...",
  "card_id": "uc_...",
  "card_key": "inspection_confirm",
  "title": "请确认巡检结果",
  "actions": ["confirm", "reject"],
  "data": {},
  "render": { "type": "iframe", "url": "...", "origin": "...", "height": 480 },
  "status": "pending"
}
```

**必须**把 `ui_card` 加入 `_turn_status_signal` → `awaiting_user`（与 `user_question` 相同）。建议同时补上 `business_confirmation`，避免确认卡终态不一致。

调用成功后 `persist_ui_card_event`（仿 `persist_user_question_event`）。

### 5.3 用户回执 `【UI卡片】`

前端提交后发送一条用户消息（可对 UI 折叠为「已确认：xxx」）：

```
【UI卡片】
interaction_type: ui_card
card_id: uc_...
card_key: inspection_confirm
action: confirm
payload: {"ok":true,"items":[...]}
```

取消 / 驳回：

```
【UI卡片】
interaction_type: ui_card
card_id: uc_...
card_key: inspection_confirm
action: reject
payload: {"reason":"数据不完整"}
```

规则：

- 前缀常量：`UI_CARD_MESSAGE_PREFIX = "【UI卡片】"`。
- 服务端解析回执时**不信任**前端传来的 `data` 原文；以 Redis pending 为准校验 `card_id`、`action`、未过期、提交者是同一 `user_id`。
- `payload` 大小上限 64KB；非法 action 拒绝续跑并提示前端。
- 路由：回执消息固定回到**同一智能体**（对齐业务确认回执的 follow-up，禁止被路由到别的专家体）。
- 提交后 pending → `submitted`，iframe 外壳切只读摘要（action + payload 关键字段）。

### 5.4 提示词

在 `agent_prompts.py` 增加一节（仅当版本工具列表含 `show_ui_card` 时注入）：

- 复杂定版确认 / 查看必须调用 `show_ui_card`，禁止用 markdown 表格或编造 HTML 代替。
- 简单选择走 `ask_user_question`，扁平录入确认走 `request_user_confirmation`。
- 工具返回 `awaiting_user` 后必须停止；收到 `【UI卡片】` 后再按 `action` 决定是否调用写入类 HTTP/MCP 工具。
- `action=reject` 时禁止写库，只用文字确认已驳回。
- 同一 `card_key` 在用户未给出新数据前不要连弹。

---

## 6. 前端宿主

### 6.1 新增组件

`frontend/src/components/UiCardHost.vue`

- 外壳：标题、折叠、pending / submitted / stale / expired。
- 内容：`iframe`（`sandbox="allow-scripts allow-forms allow-same-origin"`，按安全评审可再收紧）。
- `src` 只用 SSE 下发的登记 URL（含 card_token）。
- 监听 `message`：校验 `event.origin === render.origin`。
- 向 iframe `postMessage`（targetOrigin = 登记 origin）。

挂载点（与现两张卡并列）：

- `EmbedChat.vue`
- `AgentDebug.vue`（及主对话若与 Embed 共用消息渲染则只改一处）

SSE：`agentscopeSseHandlers.ts` 增加 `handleUiCard`，消息对象增加 `uiCard` 字段。

### 6.2 与业务卡的 postMessage 协议

所有消息带 `source` 以便过滤。

**NanZi → 业务卡**

| type | 载荷 | 时机 |
|---|---|---|
| `NANZI_CARD_INIT` | `{ card_id, card_key, title, data, actions, locale, theme }` | iframe `load` 后，或收到 READY 后再发一次（防竞态） |
| `NANZI_CARD_THEME` | `{ theme, styleVars }` | 宿主主题变化（可选，首版可省略） |
| `NANZI_CARD_DISABLE` | `{ reason }` | 已提交 / 过期 / 被替代 |

**业务卡 → NanZi**

| type | 载荷 | 说明 |
|---|---|---|
| `NANZI_CARD_READY` | `{ card_id }` | 页已就绪，可收 INIT |
| `NANZI_CARD_RESIZE` | `{ card_id, height }` | 高度 240–1200，宿主设 iframe 高 |
| `NANZI_CARD_SUBMIT` | `{ card_id, action, payload }` | **唯一**结束交互的通道 |
| `NANZI_CARD_ERROR` | `{ card_id, message }` | 展示在外壳，不自动续跑 |

约定：

- 业务页**不得**自己调 NanZi 对话 API 续跑。
- `card_id` 必须与 INIT 一致，否则丢弃。
- 同一 `card_id` 只接受一次有效 SUBMIT。

### 6.3 业务页最低实现要求

1. `?embed=1` 使用精简布局（无业务站顶栏）。
2. `Content-Security-Policy: frame-ancestors <NanZi 源>`（例如 `http://203.33.158.210:9001` 或生产域名）。
3. 只认父窗口 `source` + `card_id`。
4. 按钮的 `action` 必须是 INIT 下发的 `actions` 子集。
5. 登录：优先消费 `card_token`（平台校验后返回 data + 业务方可换自己的短会话）；禁止把业务主站 Cookie 策略改成全站 `SameSite=None` 而不做 origin 限制。

平台可提供一段可复制的 `nanzi-card-bridge.js`（或文档级伪代码）给业务接入，**不**强制业务技术栈。

### 6.4 Embed 宿主通知（可选增强）

业务站若同时 Embed 了 NanZi，卡片仍画在 **NanZi iframe 内部**，不要求外层宿主参与。  
可选上行（写进 embed 协议附录，非首版阻断）：`UI_CARD_OPENED` / `UI_CARD_SUBMITTED`，便于宿主刷单据列表。

---

## 7. 安全

| 风险 | 控制 |
|---|---|
| 模型钓鱼任意 URL | 工具无 url 参数；URL 只来自 `sys_ui_cards` |
| 点击劫持 / 恶意外链 | `allowed_origins` + iframe `src` 主机必须匹配 URL 解析后的 origin |
| token 泄露 | TTL 短、一次性、不进模型上下文、不进日志明文（只打 card_id） |
| 伪造回执 | 服务端用 Redis pending 校验 card_id / user / action / 未提交 |
| XSS | 平台不渲染业务 HTML；`data` 只 JSON 传递 |
| 过大 payload | data / 回执 payload 各 64KB |
| 越权智能体 | 版本未勾选工具则不可见；`allowed_agent_ids` 再限制 |
| 停用卡种 | `is_active=0` 后调用失败；已 pending 的卡允许提交或标 expired（配置项，默认允许提交至 TTL） |

`frame-ancestors` 由**业务页**配置；平台侧对登记 URL 做 HTTPS / 内网主机校验（可配 `ui_card_url_allow_http` 仅开发环境）。

---

## 8. 管理端与调试

### 8.1 后台页「对话卡片」（与 MCP 工具集并列）

卡片是**外部 UI 能力目录**，MCP 是**外部 API 能力目录**，给同一批人用（给智能体接业务系统）。登记入口放在侧栏「智能体开发平台」组，紧挨「MCP 工具集」，不要埋进「系统管理 → 系统配置」。

| 对照 | MCP 工具集 | 对话卡片 |
|---|---|---|
| 侧栏位置 | 智能体开发平台 | **同组并列** |
| 登记的是 | 业务 API / 工具 | 业务对话页（iframe） |
| 装配 | 版本里勾 MCP 工具 | 版本里勾 `show_ui_card` + `card_key` |
| 数据表 | `mcp_*` | `sys_ui_cards`（**不混进 MCP 表**） |

- 路由：`/dashboard/ui-cards`
- 菜单名：**对话卡片**（不要叫「MCP 卡片」）
- 权限：`menu:ui_cards`（与 `menu:mcp_management` 一样可单独授权）
- 页面形态：列表 / 新建 / 编辑 / 停用；字段即 §4.1。首版不必做「市场 / 上架」，那是 MCP 的重量，卡片用不到。

保存时校验：

- `card_key`：`^[a-z][a-z0-9_]{1,63}$`
- `url` 可解析且 origin ∈ `allowed_origins`
- `allowed_actions` 非空，元素 `^[a-z][a-z0-9_]{0,31}$`

智能体编辑页：工具多选中出现 `show_ui_card`（系统工具，不必进 `sys_api_tools`）；已登记卡种在「工具能力」里单独一组勾选。

不放系统配置的原因：系统配置是运维向（模型、HTTP 工具、KV、诊断）；MCP 虽曾是系统配置 Tab，已升到开发平台。对话卡片的使用路径是「智能体中心 → 接外部能力」，与 MCP 同路，不应再走运维埋点。

### 8.2 Widget / 组件调试台

在 `/dashboard/widget-debug` 增加「业务卡」面板：

- 选一条已登记卡，填示例 `data`，预览外壳 + iframe（不走模型）。
- 展示 INIT / SUBMIT 日志，方便业务联调。

平台另提供一个**内置 demo 卡页**（同域静态页，`card_key=platform_demo_confirm`），用于流水线与验收，不依赖外部业务系统。

---

## 9. 业务系统对接契约（给对接方）

业务侧交付三件事：

1. **一张 embed 页**（任意技术栈），实现 §6.2 / §6.3。
2. **在「对话卡片」菜单登记** `card_key` + URL + actions + origins（与登记 MCP 服务同一工作流）。
3. **写库工具**（MCP 或 `sys_api_tools`）供智能体在收到 `action=confirm` 后调用。

推荐提示词片段（业务自己配到智能体）：

```
需要用户确认巡检单时，调用 show_ui_card：
- card_key=inspection_confirm
- data 包含 task_id、items[]
- actions=["confirm","reject"]
等待【UI卡片】回执。
若 action=confirm，再调用 upsert_inspection（HTTP/MCP）。
若 action=reject，不要调用写入工具。
```

数据流与 Embed `business_context` 的关系：

- `business_context` / `UPDATE_CONTEXT`：告诉智能体「当前单据是谁」。
- `show_ui_card.data`：告诉卡片「这一屏画什么」。
- 二者互补：上下文用于推理，data 用于渲染；不要让模型把未登记的页面地址塞进 data。

---

## 10. 代码改造清单（实施时按此改，本稿不落代码）

### 10.1 后端

| 模块 | 动作 |
|---|---|
| `db-prod/Vxxx-create_sys_ui_cards.sql` | 建表 + 权限种子 + demo 卡 |
| `db-prod-pg/Vxxx-create_sys_ui_cards.sql` | 同上（PG 语法） |
| `app/models/ui_card.py` | ORM |
| `app/schemas/ui_card.py` | CRUD / 公开只读 |
| `app/api/portal/ui_cards.py` | 管理 CRUD |
| `app/api/.../ui_card_session.py` | `GET /api/v1/ui-cards/session`（card_token 换 data，只读） |
| `app/services/ai/tools/ui_card_tools.py` | `show_ui_card` |
| `app/services/ai/ui_card.py` | SSE / 回执解析 / 前缀常量 |
| `app/services/ai/ui_card_store.py` | Redis pending + token |
| `app/services/ai/tools/registry.py` | 注册系统工具 |
| `app/services/ai/tool_policy.py` | metadata |
| `app/services/ai/agent_prompts.py` | 条件注入 |
| `app/services/ai/agent_service.py` | `_turn_status_signal` 增加 `ui_card` |
| `assistant_agent_runner.py` / `chatbi/react_stream.py` | yield SSE + persist |
| `router_service.py` | 回执钉死原智能体 |

### 10.2 前端

| 模块 | 动作 |
|---|---|
| `components/UiCardHost.vue` | 宿主 |
| `utils/uiCard.ts` | 解析事件、拼回执、stale 标记 |
| `utils/agentscopeSseHandlers.ts` | `handleUiCard` |
| `EmbedChat.vue` / `AgentDebug.vue` | 挂载 + submit |
| 管理页 `views/UiCards.vue` | 登记 CRUD |
| `Dashboard.vue` | 菜单 |
| `WidgetDebugger.vue` | 预览联调 |
| 同域 demo 页 | `platform_demo_confirm` |

### 10.3 测试（用户明确要求测试时再写/跑）

契约级（不引入业务系统）：

- 工具：非法 key / 超限 data / 非法 action。
- store：pending 互斥、token 一次性、回执校验。
- SSE：`type=ui_card` 且终态 `awaiting_user`。
- 前端契约：`EmbedChat` / `AgentDebug` 含 `UiCardHost`；回执前缀稳定。

`tests/CHECKLIST.md` 在**开工提交前**按 PR 模板补对应项。

---

## 11. 分阶段落地

### P0 — 最小可用（先打通闭环）

1. 表 + 管理 CRUD + 系统工具 + Redis store。  
2. SSE + `_turn_status_signal` + 回执。  
3. `UiCardHost` 挂到 Embed / Debug。  
4. 同域 demo 卡：两个按钮 confirm/reject。  
5. 权限菜单。

验收：对话里模型调 `show_ui_card(card_key=platform_demo_confirm)` → 出卡 → 点确认 → 出现 `【UI卡片】` → 模型根据 action 回复。

### P1 — 业务可接

1. card_token 只读拉取 data。  
2. origin / HTTPS 校验、高度协议。  
3. Widget 调试台预览。  
4. 对接文档附录（可并入本文 §9，不另开无关 md）。  
5. 首个真实业务卡（一张最复杂的确认页）。

### P2 — 体验与治理（需要时再做）

1. 历史消息可靠回放（未过期重开 iframe 只读；过期出摘要）。  
2. Embed 上行 `UI_CARD_*`。  
3. 卡种按租户隔离（若上多租户）。  
4. 评估「平台 formSchema 轻量卡」（仅字段型，仍不替代 iframe）。

---

## 12. 验收标准

- [ ] 未登记 / 停用 / 未授权智能体调用 `show_ui_card` 失败且不出 iframe。  
- [ ] 模型参数无法指定 URL。  
- [ ] 出卡后本轮 `execution_status=awaiting_user`，不自动写业务库。  
- [ ] 仅 `allowed_actions` 能提交；非法 action 被服务端拒绝。  
- [ ] 确认回执后续可调 HTTP/MCP；驳回回执不写库（靠提示词 + 可加服务端闸门，对齐取消确认卡）。  
- [ ] Embed 与 Debug 行为一致。  
- [ ] demo 卡不依赖外部域名即可验收。  
- [ ] MySQL / PG 迁移成对提交。  
- [ ] 现有提问卡、确认卡、Embed Ticket 行为无回归。

---

## 13. 风险与取舍

| 风险 | 应对 |
|---|---|
| 跨域 iframe 登录麻烦 | card_token 换业务短会话；不要求业务 Cookie 穿透 |
| 业务页改版导致高度跳动 | RESIZE + max-height，外壳可滚动 |
| 模型乱选 card_key | 工具 description 列出**当前智能体可用**的 key 摘要（从登记表查，不把 URL 给模型） |
| 把复杂卡做成平台 Vue | 评审拒绝；P2 的 formSchema 仅覆盖字段型 |
| 卡片里直接调业务写接口 | 协议禁止；审计靠「回执后工具调用」追踪 |
| Redis 丢失 pending | 回执校验失败，提示用户重新出卡；不盲信前端 |

---

## 14. 结论

本平台要达到 yunshu 级对话内定制页，**不应复制「平台内写死 Vue 卡」**，而应补第三种交互：

`interaction_type = ui_card`  
`工具 = show_ui_card`  
`渲染 = 已登记业务 iframe`  
`恢复 = 【UI卡片】回执`

A 层通用卡继续承担简单交互；B 层把复杂版式留在业务系统；C 层 Embed 继续负责「助手进业务站」。三者叠加后，基础平台保持薄，业务系统保持自定义能力。

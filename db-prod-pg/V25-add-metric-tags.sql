-- -----------------------------------------------------------------------------
-- V25: meta_metrics 增加 tags 字段
-- 用途：为业务指标增加标签列表，辅助检索分类，与 meta_datasets.tags 保持一致
-- -----------------------------------------------------------------------------
ALTER TABLE "meta_metrics"
    ADD COLUMN IF NOT EXISTS "tags" JSON;
# 自动化测试与交付变更清单 (Automation Test & Delivery Checklist)

> 说明：本清单记录近期功能交付与自动化测试覆盖情况。历史已稳定的旧版本记录定期清理瘦身，完整变更可追溯 Git 历史。

| 特性 / 修复项 | 涉及核心文件 | 方案设计与测试闭环说明 | 验收状态 | 交付日期 |
| :--- | :--- | :--- | :--- | :--- |
| 对话附件点击 PDF 改为下载不再跳画布 (Chat Attachment PDF Downloads Instead Of Canvas) | `frontend/src/utils/workspaceFilePreview.ts`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/frontend/test_canvas_content_save_contract.py`, `tests/CHECKLIST.md` | **气泡里点 PDF 会整页跳进 ChatCanvas**：`openChatAttachmentFile` 把 pdf 当可预览文件交给工作区画布。现对话附件一律走下载，与 xlsx 等一致；工作区文件浏览器仍可预览 PDF。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 输入区附件与气泡卡片对齐并支持清空 (Composer Attachments Five-Up Cards With Clear All) | `frontend/src/components/chat/UserMessageAttachments.vue`, `frontend/src/components/embed/ChatInput.vue`, `tests/frontend/test_chat_surface_refactor_contract.py`, `tests/CHECKLIST.md` | **输入区附件不再用灰色小条**：与对话气泡共用白底描边文件卡；输入区一行五个、左对齐，单卡可移除；顶部「清空附件」一次去掉已添加附件（保留内部 knowledge_settings）。气泡仍是一行四个、右对齐。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 用户气泡不再展示附加系统元数据说明 (Hide User Bubble System Metadata Expander) | `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/frontend/test_chat_surface_refactor_contract.py`, `tests/CHECKLIST.md` | **用户侧只看自己写的话**：气泡继续用分隔符拆出发送正文，不再渲染「附加系统元数据说明」折叠区；附件卡仍在气泡外。发给模型的上下文原文不变。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 用户消息附件改为 Ant Design 四列文件卡 (User Message Attachments Antd Four-Up Cards) | `frontend/src/components/chat/UserMessageAttachments.vue`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/frontend/test_chat_surface_refactor_contract.py`, `tests/CHECKLIST.md` | **发起消息附件不再铺在蓝色气泡里**：文件卡移出气泡，白底、`#d9d9d9` 描边、8px 圆角，一行四个；名称/大小按 Ant Design 文本色。嵌入对话与智能体调试共用组件。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 解析快照作权威源并临时放宽工具观察上限 (Resolve Snapshot Is Source Of Truth, Temp 20k Tool Cap) | `app/services/ai/hitl_continuation.py`, `app/services/ai/runtime/agentscope/hitl_tool_result.py`, `app/services/ai/runtime/agentscope/stream_reconcile.py`, `app/services/ai/runtime/agentscope/tool_result.py`, `app/services/ai/pipeline/steps/assemble_step.py`, `app/services/ai/tools/user_confirmation_tools.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/services/ai/executors/assistant_executor.py`, `tests/services/ai/test_hitl_continuation.py`, `tests/ai/runtime/test_agentscope_tool_evidence.py`, `tests/ai/grounding/test_tool_result_envelope.py`, `tests/CHECKLIST.md` | **彻底挡住 11 变 9**：① 工具观察默认上限临时改为 20000，解析结果仍先瘦身、禁止中切 JSON。② 快照记录 `found_claimed/found_records/integrity`；汇总与名单不一致则标 incomplete，观察文本要求重查，出卡不按快照扩写。③ 完整快照出卡时，仅当字段拆开后**全部是快照对象名**才扩成可下发全称；数量字段只同步纯数字或最多 2 字原后缀，且不改已回写的名称字段。长文本里偶然出现一个名称不会被整段替换。未匹配只在实际对齐过名称字段时并入 `risk_note`。不再追加 `resolved_ids`、不把 ID 写进名称。④ 有未完成 todo 的普通追问注入精简快照（不是回执口吻）；新问题仍清事实。⑤ 跨轮工具上下文对解析类工具不再 800 字再切。可下发=found+主键，且若有 enabled/matchCount 则须启用且唯一匹配。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 解析类工具观察不再硬切丢失已匹配主键 (Compact Entity Resolve Observation Without Mid-JSON Cut) | `app/services/ai/hitl_continuation.py`, `app/services/ai/runtime/agentscope/hitl_tool_result.py`, `app/services/ai/runtime/agentscope/event_stream.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/services/ai/tools/user_confirmation_tools.py`, `tests/services/ai/test_hitl_continuation.py`, `tests/CHECKLIST.md` | **`enterprise_resolve` 返回 found=11，模型只看到 9 家**：完整 `records` 被 4000 字硬切，统计字段还在、名单被截断；续跑事实也记的是残缺文本。现对解析类工具先暂存完整 JSON，观察文本改为瘦身字段（name/found/enabled/matchCount/主键），禁止从中间切断；超长时已匹配全留、未匹配缩成名称列表。出卡仍按已解析名称补 ID，`resolved_ids` 带上全部已匹配主键；续跑提示不再因 40 条上限丢掉已匹配 ID。日期解析等非实体 resolve 仍走原截断。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 技能推荐问不再写明文 quick 协议 (Skills Recommend Questions Without Bare quick: Prefix) | `app/services/ai/agent_prompts.py`, `frontend/src/utils/quickButtons.ts`, `app/services/ai/runtime/agentscope/stream_reconcile.py`, `app/services/ai/agent_service.py`, `tests/ai/test_prompt_assembler.py`, `tests/ai/test_quick_suggestion_layout.py`, `tests/frontend/test_business_confirmation_contract.py`, `frontend/scripts/quickButtons.test.ts`, `tests/CHECKLIST.md` | **出确认卡前模型把技能里的 `quick:谁还没交` 抄进回复**：SKILL.md 回执模板带协议前缀，确认当轮整份预载就会泄漏。平台改为只用技能给的中文原句，禁止抄 `quick:` 字面量；确认完成前/出卡/取消/失败不输出推荐问。**推荐问不出现**：前端只认 `[标签](quick:…)`，文末中文短句不会变按钮。现只把成功回执文末 2–4 条动作短句提升为协议；确认卡当轮仍剥离。不写死具体问句，也不兼容旧的明文 `quick:` 行。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 技能文件变更后下一轮自动重挂载 (Remount Skills Next Turn On Content Revision) | `app/services/ai/skill_revision.py`, `app/services/ai/skills/injector.py`, `app/services/ai/hitl_continuation.py`, `app/services/ai/agent_prompts.py`, `app/api/portal/endpoints/skills.py`, `app/api/portal/endpoints/personal_skills.py`, `app/services/ai/tools/system_executive_tools.py`, `app/services/ai/runners/assistant_agent_runner.py`, `tests/services/ai/test_hitl_continuation.py`, `tests/services/ai/test_skill_resolver_cache.py`, `tests/ai/test_prompt_assembler.py`, `tests/test_system_executive_tools.py`, `tests/CHECKLIST.md` | **同会话改完技能立刻追问仍像没更新**：预载提示写「无需再读」，历史里还是旧 SKILL.md，sidecar 变更也不触发重挂。现按目录 mtime/size 指纹记 `content_rev`；写入时清 meta cache；下一轮只对「已有 stored rev 且与磁盘不一致」的已记技能重挂、强制预载并要求重读 SKILL.md/附属文件，时间线打「已重新挂载」。不监听目录、不在本轮热替换。无 stored rev 的旧会话不误重挂；本轮未激活则沿用上一轮技能快照。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| AI 执行链、性能与架构优化 (AI Runtime Performance And Architecture Optimization) | `app/services/ai/tools/user_confirmation_tools.py`, `app/services/ai/runtime/agentscope/tools.py`, `app/services/ai/turn_status.py`, `app/services/ai/turn_finalizer.py`, `app/services/ai/hitl_continuation.py`, `app/services/ai/runtime/agentscope/tool_call_args.py`, `app/services/ai/pipeline/`, `app/services/ai/runtime/agentscope/resume.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/api/v1/endpoints/chat.py`, `app/services/sso_user.py`, `app/services/metadata_service.py`, `app/core/cancellation.py`, `frontend/src/types/chat.ts`, `frontend/src/utils/sseLineParser.ts`, `frontend/src/utils/chatRunStatus.ts`, `frontend/src/utils/chatMessageRenderKey.ts`, `frontend/src/components/chat/ChatMessageRow.vue`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `frontend/src/router/index.ts`, `tests/ai/tools/test_user_confirmation_tool.py`, `tests/ai/pipeline/test_pipeline_steps.py`, `tests/services/ai/test_hitl_continuation.py`, `tests/frontend/test_chat_surface_refactor_contract.py`, `tests/CHECKLIST.md` | **AI 正确性**：确认工具顶层数组/字符串字段在 AgentScope schema 前统一修复；轮次状态、Todo 收尾与主链/resume 共享规则；HITL 业务访问收敛到 coordinator，生产 Redis 故障不再静默退化到单进程状态；runner 工具参数恢复抽成独立模块。**性能**：聊天 SSE 不再持有请求级 DB 会话；历史 DB 回退有界；SSO 改异步 HTTP；元数据关系统计消除 N+1；后台任务统一异常监督。**前端架构**：共享消息类型、SSE parser、run_status reducer、消息行边界与技能徽章缓存；重管理路由懒加载。已补现有行为/契约覆盖。 | ✅ 代码已落地；代码诊断、`vue-tsc --noEmit`、`git diff --check` 通过；未代跑自动测试与启动脚本 | 2026-09-19 |
| Excel inspect/profile 摘要列出真实表头，禁止用用户说法筛列 (Excel Inspect Summary Lists Exact Headers) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `tests/ai/tools/test_excel_document_tool.py`, `tests/CHECKLIST.md` | **模型用「轨迹完整率(%)」筛「完整率」**：不是匹配太严，是摘要只写「包含 N 个工作表」，模型按用户/技能说法编列名。撤回模糊别名匹配（会误配）。inspect/profile 的 summary 列出逐字表头；提示词要求 filter 的 header 必须复制摘要/headers，禁止自行加「轨迹」「(%)」。对不上仍报错并列出当前表头。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 续跑提示带上推进后的任务清单 (HITL Continuation Prompt Lists Advanced Todos) | `app/services/ai/hitl_continuation.py`, `tests/services/ai/test_hitl_continuation.py`, `tests/CHECKLIST.md` | **模型把确认步写回进行中**：回执提示原先只说「清单仍有效」，不带状态。现列出推进后的 `[status] 内容`，并明确禁止把已完成项改回进行中或整表重开。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| Excel 同列多条 eq 自动收成 in (Excel Filter Fold Same-Column Eq Into In) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `tests/ai/tools/test_excel_document_tool.py`, `tests/CHECKLIST.md` | **筛 9 家企业连失败「最多 8 条」**：提示词要求专有全称用 eq，模型给每名一条。解析后把同一列 eq/in（及 ne/not_in）合并；折叠后再检查 8 条上限；原始条数上限 80。提示词改为单名 eq、多名一条 in。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 确认卡回执后任务清单推进并在成功收尾 (Advance Todos After HITL Confirm And Complete On Success) | `frontend/scripts/processTimeline.test.ts`, `frontend/src/utils/processTimeline.ts`, `frontend/src/utils/agentscopeSseHandlers.ts`, `frontend/src/utils/inflightConversation.ts`, `frontend/src/utils/hitlHistory.ts`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `app/api/v1/endpoints/chat.py`, `app/services/ai/hitl_continuation.py`, `app/services/ai/tools/todo_tools.py`, `app/services/ai/pipeline/steps/assemble_step.py`, `app/services/ai/pipeline/steps/context_step.py`, `app/services/ai/pipeline/steps/finalize_step.py`, `app/services/ai/runtime/agentscope/process_timeline_snapshot.py`, `tests/ai/pipeline/test_pipeline_steps.py`, `tests/ai/runtime/test_process_timeline_snapshot.py`, `tests/frontend/test_chat_shared_helpers_behavior.py`, `tests/frontend/test_chat_surface_refactor_contract.py`, `tests/frontend/test_hitl_history_hydrate_contract.py`, `tests/frontend/test_inflight_conversation_behavior.py`, `tests/frontend/test_inflight_conversation_contract.py`, `tests/services/ai/test_hitl_continuation.py`, `tests/CHECKLIST.md` | **确认后清单停在出卡步、跑完或刷新仍显示进行中**：任务清单按 assistant 消息持久化；成功回执轮若没收到 `todo_update`，从最近一条出卡助手消息恢复并收尾后写入最新 assistant。确认/取消只修改卡片所属消息，禁止反向扫描并误改无关旧任务；历史续跑只接受回执与下一条用户消息之间的 assistant；最新 agent 没有清单时不再展示更早轮清单。终态 success 后迟到的旧 `todo_update` 整包忽略，服务端终态只替换本地 todo、保留本地日志/HITL；completed/cancelled 均为不可回退终态。取消轮移除异步重复落库并保留系统助手身份；空 `todo_write` 同步清除 Redis 续跑快照。SSE 尾包、刷新恢复和历史 `status` 均已对齐。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 嵌入对话水印默认跟随系统开关，缺字段不再强开 (Embed Chat Watermark Respects Disabled Flag) | `frontend/src/views/EmbedChat.vue`, `app/services/embed_service.py`, `tests/frontend/test_embed_url_agent_lock_contract.py`, `tests/CHECKLIST.md` | **关了仍出水印**：系统开关 `embedchat_watermark_enabled=false` 后，iframe 换票 `user_info` 原先没有 `watermark`，前端用 `缺省则 true` 继续平铺。现仅 `enabled === true` 才渲染；换票带上当前水印配置；INIT_CONFIG 换票后补拉 `/me`。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 对话输入支持批量粘贴文件并限制数量/大小 (Chat Input Batch File Paste With Caps) | `frontend/src/utils/chatFileIntake.ts`, `frontend/src/components/embed/ChatInput.vue`, `frontend/src/components/ConfirmModal.vue`, `tests/frontend/test_chat_shared_helpers_behavior.py`, `tests/frontend/test_confirm_modal_zindex_contract.py`, `tests/CHECKLIST.md` | **一次复制多个文件不再只收第一个**：粘贴同时读 `clipboardData.files` 与 `items` 去重；选择/拖拽走同一套。单次最多 10 个实体文件（含已挂上传附件的余量），单文件不超过 20MB，危险后缀仍拦截。超限/失败用平台 `ConfirmModal`（警告态、仅「知道了」），不再用浏览器 `alert()`。弹窗视觉对齐 Ant Design `Modal.warning`：416 宽、左状态图标、标题正文左对齐、底栏主按钮右对齐、遮罩 45%。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| HITL 出卡载荷不走 4000 字截断 (Keep HITL Card Payload Intact For SSE) | `app/services/ai/runtime/agentscope/hitl_tool_result.py`, `app/services/ai/runtime/agentscope/tools.py`, `app/services/ai/runtime/agentscope/event_stream.py`, `app/services/ai/business_confirmation.py`, `app/services/ai/user_question.py`, `tests/ai/tools/test_user_confirmation_tool.py`, `tests/ai/tools/test_user_question_tool.py`, `tests/ai/runtime/test_event_stream_observability.py`, `tests/CHECKLIST.md` | **确认卡/提问卡被 4000 字截断后 JSON 残缺，SSE 解析失败不出卡**：运行时工具观察仍截断/改成短摘要给模型；完整 `awaiting_user` 载荷走 ContextVar，在 `TOOL_RESULT_END` 写回 `tool_outputs` 再出卡。解析顺带识别审计 `{raw: json}` 包装。提问卡同样处理。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 技能附属文件按 catalog skill_id 按需读取 (Read Skill Sidecar Files By Catalog Id) | `app/services/ai/tools/system_executive_tools.py`, `app/services/ai/skill_resolver.py`, `app/services/ai/agent_prompts.py`, `app/services/ai/skills/injector.py`, `app/services/ai/runtime/agentscope/workspace.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/utils/skill_metadata.py`, `app/services/ai/tools/resource_catalog_tools.py`, `frontend/src/composables/chat/useChatAttachments.ts`, `tests/services/ai/test_skill_discovery_tools.py`, `tests/ai/runtime/test_agentscope_workspace.py`, `tests/ai/test_prompt_assembler.py`, `tests/services/ai/test_agent_service_skill_hint.py`, `tests/utils/test_skill_metadata.py`, `tests/ai/runtime/test_workspace_prompt.py`, `tests/frontend/test_chat_shared_helpers_behavior.py`, `tests/CHECKLIST.md` | **不预载附属正文**：`read_skill_instruction` 增加可选 `file`（缺省 SKILL.md），从技能目录正本读相对路径，拒绝穿越；成功/缺失都只附同目录文件名。已匹配技能块列出文件名并禁止 Read 拼会话 `skills/`。会话预置在 Frontmatter 目录名与 catalog 文件夹名不同时 hardlink 双名兜底。读附属文件不重复打「已启用流程」。挂载隐式指令（前后端）、工作区提示与目录清单不再把技能导向 Read `/workspace/skills` 或 `/app/data/skills/.../SKILL.md`。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 业务确认卡对齐 Ant Design Form/Card (Business Confirmation Card Ant Design Style) | `frontend/src/components/BusinessConfirmationCard.vue`, `tests/frontend/test_business_confirmation_contract.py`, `tests/CHECKLIST.md` | **去掉浅蓝表格皮肤**：卡片按 Ant Design Card，外框用可见 `#d9d9d9` 描边；字段按 Form 横向（标签右对齐+冒号）；输入框 outlined 32px/`#d9d9d9`/主色描边；只读/禁用字段白底、正文色不变（不再用 `#f5f5f5` 灰底浅字）；日期选择器沿用原生 date 但触发器按 DatePicker；风险提示按 Alert warning；底栏 default + primary 按钮右对齐。不引入 antd 依赖。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 确认卡日期字段用日期选择器 (Confirmation Card Date Picker) | `frontend/src/components/ConfirmationDatePicker.vue`, `frontend/src/components/BusinessConfirmationCard.vue`, `frontend/src/utils/businessConfirmation.ts`, `app/services/ai/business_confirmation.py`, `app/services/ai/tools/user_confirmation_tools.py`, `app/services/ai/agent_prompts.py`, `tests/frontend/test_business_confirmation_contract.py`, `tests/ai/tools/test_user_confirmation_tool.py`, `tests/CHECKLIST.md` | **日期控件不靠字段名词表**：`value_type=date/datetime`，或值本身是 `YYYY-MM-DD` / `YYYY年M月D日` / 带时刻。原生 `input[type=date]` 弹层无法换肤，确认卡改为 Vue + `date-fns` 的 Ant Design 风格面板（日历/时分列、今天/此刻、Teleport 防裁切），提交值仍是 `YYYY-MM-DD` / `YYYY-MM-DD HH:mm`。卡片宽度保持原 `42/48/52rem`。不写死业务词；空值必须由模型标类型。不引入 antd。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-19 |
| 空会话拉可复用结果不再 404 (Empty Conversation Reusable Results Return Empty List) | `app/api/v1/endpoints/chat.py`, `tests/api/v1/test_reusable_result_api.py`, `tests/CHECKLIST.md` | **切会话/新建会话不再刷 404**：前端一写入 `conversationId` 就 GET `/reusable-results`，此时常还没有审计历史、Redis 消息和 active 标记。归属失败改为与 `/artifacts/counts` 一样返回 200 空列表，不读 Redis，避免泄露他人结果。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 确认卡取消后停止执行链 (Stop Pipeline On Business Confirmation Cancel) | `app/services/ai/pipeline/steps/context_step.py`, `app/services/ai/pipeline/steps/preflight_step.py`, `tests/ai/test_user_question_cancellation.py`, `tests/CHECKLIST.md` | **点取消不再让模型继续跑**：提问卡取消会在进路由/模型前短路；确认卡取消原先只靠提示词和禁止再弹卡，主助手仍会整轮执行。现与提问卡对齐：收到「用户已取消」立即返回「已取消本次业务确认，本次任务已停止。」，跳过路由与工具，并清 HITL 续跑上下文。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| HITL 回执轮续跑技能/附件/已解析 ID/todo (HITL Continuation Context Across Confirm And Question Cards) | `app/services/ai/hitl_continuation.py`, `app/services/ai/skills/injector.py`, `app/services/ai/pipeline/steps/assemble_step.py`, `app/services/ai/prompt_assembler.py`, `app/services/ai/agent_prompts.py`, `app/services/ai/tools/user_confirmation_tools.py`, `app/services/ai/tools/todo_tools.py`, `app/services/ai/runners/assistant_agent_runner.py`, `frontend/src/components/BusinessConfirmationCard.vue`, `tests/services/ai/test_hitl_continuation.py`, `tests/CHECKLIST.md` | **确认卡/提问卡是新一轮，但必须接着原任务**：会话 Redis 记下本轮已启用技能、本轮附件的可读绝对路径、解析类工具返回的主键、todo。确定回执预载 SKILL.md 并打「已启用流程」，不再用回执正文去匹配别的技能；调试技能白名单不会滤掉粘回来的流程。注入续跑事实（禁止臆造主键），恢复附件可读路径与任务清单；取消回执不粘并清上下文。出确认卡时若字段值命中已解析名称则补上工具返回的主键，长文本用多行框。普通新问题覆盖原任务文案，只快照本轮技能/附件。多次 resolve 合并 records；工具输出夹杂说明/代码块仍能拆 JSON。平台层不写死企业/下发领域词。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 运行时读取技能后补打「已启用流程」时间线 (Emit Enabled Skill Flow Log After read_skill_instruction) | `app/services/ai/skills/injector.py`, `app/services/ai/runners/assistant_agent_runner.py`, `tests/services/ai/test_agent_service_skill_hint.py`, `tests/CHECKLIST.md` | **模型自己读 SKILL.md 也要出条**：斜杠选中/自动扫描会在组装阶段打「已启用流程」紫条；只调 `read_skill_instruction` 时时间线只剩匿名「工具完成」，追问里写了 skill_id 才看起来像启用了。成功读取后补 `已启用流程: {名称}`，工具标题带上 `· {skill_id}`。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 用迁移清掉已发布版本里的幽灵技能 ID (SQL Unbind Ghost industry-dispatch-task From Published Versions) | `db-prod/V166-unbind-deleted-industry-dispatch-skill.sql`, `db-prod-pg/V67-unbind-deleted-industry-dispatch-skill.sql`, `tests/CHECKLIST.md` | **历史白名单补摘**：删除技能时的解绑只对之后生效，主智能体已发布 v10、主智能体dev 已发布 v4（及归档 v9/v3）仍含 `industry-dispatch-task`。迁移从 `skills` JSON 去掉该 ID；摘空则关闭 `skills_custom`。仍存在的 `task` 保留。PG 已 MCP 执行 V67；MySQL 需自跑 V166。 | ✅ PG 已 MCP 落地；MySQL 需自跑；未代跑启动脚本 | 2026-09-18 |
| 修复 ask_user_question 畸形入参被 jsonschema 直接拒绝 (Repair Malformed ask_user_question Tool Input) | `app/services/ai/tools/user_question_tools.py`, `app/services/ai/runtime/agentscope/tools.py`, `app/services/ai/runtime/agentscope/agent_runtime.py`, `app/services/ai/agent_prompts.py`, `tests/ai/tools/test_user_question_tool.py`, `tests/CHECKLIST.md` | **根因**：qwen 等模型把 `[{id,label}…]` 选项数组和 XML 残留 `"context">超长附件摘要` 整段当工具入参；AgentScope 在调用工具前 `_json_loads_with_repair` + `jsonschema.validate` 要求 JSON 对象，于是报 `Input validation failed`，`ainvoke` 根本进不去。超长 `context` 即便修成对象也会撞 1000 字上限。**修复**：校验前把选项数组/`questions` 别名/缺 question 收成 `{question, options, context}`，缺标题时合成「请确认以下任务参数」并允许多选；`context` 截断而非抛错；提示词写明必须传 JSON 对象。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 删除公共技能时清除智能体白名单绑定 (Unbind Agent Skills On Platform Skill Delete) | `app/services/ai/agent_manager.py`, `app/api/portal/endpoints/skills.py`, `frontend/src/views/SkillsManagement.vue`, `frontend/src/views/AgentManagement.vue`, `tests/services/ai/test_skill_explicit_bindings.py`, `tests/test_skills_management.py`, `tests/frontend/test_agent_type_form_contract.py`, `tests/frontend/test_skill_publication_contract.py`, `tests/CHECKLIST.md` | **删目录同时摘绑定**：`DELETE /skills/{id}` 先从全部智能体版本的 `skills` JSON 去掉该 ID；若自定义白名单被摘空则关闭 `skills_custom`，避免锁死成零公共技能。编辑器已选计数/保存只保留磁盘上仍存在的公共技能，去掉已删 ID 造成的 `2/1`。技能工作台确认文案写明会解绑。删除当时已发布版本里的幽灵 ID 由 V166/V67 补清。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 切会话不丢进行中的流式正文 (Keep Inflight Messages Across Conversation Switch) | `frontend/src/utils/inflightConversation.ts`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/frontend/test_inflight_conversation_behavior.py`, `tests/frontend/test_inflight_conversation_contract.py`, `tests/CHECKLIST.md` | **有缓存只还原、生成中不拉会话、空闲再合并**：切走把 `messages` 放进内存 Map，切回直接挂回同一批对象，有缓存禁止历史整表覆盖。`run-status` 轮询不再打 `/conversation/{id}`；无缓存且仍在跑只插「正在生成」占位并写入 inflight。空闲一律 `mergeCompletedRunIntoMessages`。iframe 重复 `INIT_CONFIG`（常带 `agent_id`、不带 `conversation_id`）先 stash，已有现场会话不新开、不拉 `/active`、不整表替换历史。刷新/iframe 被销毁再挂仍无 SSE 接续。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 嵌入会话放行本人额度查询 (Embed Allow GET /quota/me) | `app/services/embed_api_guard.py`, `app/api/portal/endpoints/quota.py`, `tests/api/v1/test_embed_ticket.py`, `tests/CHECKLIST.md` | **403 不是二级目录配错**：Uvicorn 日志里的 `/zhiyuan/api/portal/quota/me` 只是原始 URL，鉴权前会剥前缀。嵌入 iframe 流结束后会刷额度，但白名单未放行该只读接口。现允许 `GET /quota/me`，额度按签发人平台账号算，禁止 system/users 管理面。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 手动停止生成后任务清单应收尾 (Cancel Open Todos On Manual Stop) | `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `frontend/src/utils/processTimeline.ts`, `tests/frontend/test_chat_surface_refactor_contract.py`, `tests/CHECKLIST.md` | **点停止不再让清单转圈**：`stopGeneration` 会立刻 `abort` SSE，前端收不到后端 `todo_update`/`run_status=cancelled`。停止时本地先把未完成项标 `cancelled`，不再依赖 `isThinking` 才改文案。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 确认卡取消后任务清单应收尾 (Cancel Open Todos When HITL Cancelled) | `app/services/ai/runtime/agentscope/process_timeline_snapshot.py`, `app/services/ai/agent_service.py`, `app/services/ai/pipeline/steps/context_step.py`, `app/services/ai/pipeline/steps/finalize_step.py`, `frontend/src/utils/processTimeline.ts`, `frontend/src/components/chat/ChatTodoCard.vue`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/ai/runtime/test_process_timeline_snapshot.py`, `tests/services/ai/test_agent_service_stream_content.py`, `tests/ai/test_user_question_cancellation.py`, `tests/frontend/test_chat_shared_helpers_behavior.py`, `tests/CHECKLIST.md` | **取消不再让清单停在「进行中」**：提问卡/确认卡取消或 `run_status=cancelled` 时，未完成项标 `cancelled`（已完成保持 completed），下发 `todo_update` 并写入本轮时间线；前端立即收尾上一轮清单、展示「已取消」并自动折叠。等待权限/外部执行仍保留进行中。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 嵌入应用列表与编辑弹窗信息架构 (Embed Apps Admin UX) | `frontend/src/views/EmbedApps.vue`, `frontend/src/api/embedApp.ts`, `app/api/portal/endpoints/embed_apps.py`, `app/schemas/embed_app.py`, `tests/CHECKLIST.md` | **列表可扫读、弹窗分组、宿主不再露 UUID**：列表对齐角色管理（顶栏搜索/状态分段、桌面表格、移动卡片）；智能委派显示 `default_entry_agent_name`；编辑弹窗分「基本信息 / 对话入口 / 安全与接入」，开关替代勾选，自定义下拉替代原生 select，底栏固定。契约文案保持不变。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 嵌入应用显式指定智能委派宿主，空则不委派 (Embed Explicit Delegation Host) | `db-prod/V164-embed-apps-default-entry-agent.sql`, `db-prod/V165-embed-apps-backfill-delegation-host.sql`, `db-prod-pg/V65-embed-apps-default-entry-agent.sql`, `db-prod-pg/V66-embed-apps-backfill-delegation-host.sql`, `app/models/embed_app.py`, `app/schemas/embed_app.py`, `app/services/embed_app_service.py`, `app/services/embed_identity.py`, `app/services/embed_service.py`, `app/services/ai/context_manager.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/services/ai/tools/agent_delegate_tool.py`, `app/services/ai/agent_roster.py`, `app/services/ai/prompt_assembler.py`, `app/services/ai/agent_prompts.py`, `app/services/ai/agent_service.py`, `app/services/ai/pipeline/steps/assemble_step.py`, `app/services/ai/router_service.py`, `app/api/portal/endpoints/embed_apps.py`, `frontend/src/utils/delegationHost.ts`, `frontend/src/views/EmbedApps.vue`, `frontend/src/views/EmbedChat.vue`, `frontend/src/components/embed/ExpertCascadeMenu.vue`, `frontend/src/components/embed/ChatInput.vue`, `frontend/src/components/embed/ChatSettings.vue`, `docs/md/embed_integration_guide.md`, `tests/api/v1/test_embed_ticket.py`, `tests/frontend/test_embed_routing_preference_contract.py`, `tests/services/ai/test_agent_context_manager.py`, `tests/ai/test_agent_roster.py`, `tests/ai/test_prompt_assembler.py`, `tests/CHECKLIST.md` | **两套世界拆开**：站内/调试只认平台 Main（`is_main_general_agent`），委派名单恢复全站系统智能体。嵌入只认应用 `default_entry_agent_id`；空=不智能委派（1 专家直达，多个手选），禁止回落 Main 或合成 General Chat。花名册与 `sub_agent_call` 候选共用 `list_delegation_source_agents`（嵌入=角色目录）。提示词工具清单显式传入 `user_info`，不再靠上下文兜底误判。要用平台主助手就在下拉里选中它。旧应用角色已授权 Main 且宿主为空时回填 `sys-agent-chat`（V165/V66）。技能扫描仍只给平台 Main。语义路由撤回 `agents[0]` 特例。 | ✅ 代码已落地；PG 已 MCP 执行 V65/V66；MySQL 需自跑 V164/V165；未代跑真实服务与启动脚本 | 2026-09-18 |
| 嵌入智能委派必须以角色内 Main 为宿主 (Embed Smart Delegation Requires Role Main) | `app/services/ai/context_manager.py`, `app/services/ai/agent_service.py`, `app/services/ai/tools/agent_delegate_tool.py`, `app/services/ai/runners/assistant_agent_runner.py`, `frontend/src/views/EmbedChat.vue`, `frontend/src/components/embed/ExpertCascadeMenu.vue`, `frontend/src/components/embed/ChatSettings.vue`, `frontend/src/views/EmbedApps.vue`, `tests/services/ai/test_agent_context_manager.py`, `tests/CHECKLIST.md` | **已被上一行彻底方案替代**：嵌入不再用「角色含 Main 则隐式委派」。站内仍只认平台 Main。 | ♻️ 已并入显式宿主方案 | 2026-09-18 |
| 刷新后还原全部待操作卡 (Restore HITL Cards After Refresh) | `app/services/ai/runtime/agentscope/process_timeline_snapshot.py`, `frontend/src/utils/hitlHistory.ts`, `frontend/src/utils/processTimeline.ts`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/ai/runtime/test_process_timeline_snapshot.py`, `tests/frontend/test_hitl_history_hydrate_contract.py`, `tests/CHECKLIST.md` | **刷新不再丢掉待操作 UI**：业务确认卡、提问卡、工具权限卡、外部执行卡、落地拦截卡在出卡时把完整 payload 写入 `process_timeline`（`kind=hitl`，不截断字段）；历史加载按时间线 hydrate，已回执的确认/提问标成已提交，后续已继续的权限/外部执行标过期。工具权限/外部执行恢复后仍依赖 Redis pending（约 600s）才能真正 resume。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 对话待操作卡底部按钮右对齐且主操作在右 (Chat Card Actions Right-Aligned Primary Last) | `frontend/src/components/BusinessConfirmationCard.vue`, `frontend/src/components/UserQuestionCard.vue`, `frontend/src/components/GroundingBlockedCard.vue`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/frontend/test_business_confirmation_contract.py`, `tests/frontend/test_user_question_contract.py`, `tests/frontend/test_execution_stage_card_contract.py`, `tests/CHECKLIST.md` | **平台卡片统一**：业务确认卡、提问卡、拦截卡、外部执行卡底部操作右对齐；次要/取消在左、主操作在最右。工具权限卡原本已是该顺序，仅补契约锁定。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 一级根路径与二级目录 /zhiyuan 反代 (Root And /zhiyuan Subpath Deploy) | `app/core/app_prefix.py`, `app/core/openapi.py`, `app/services/mcp/platform_mcp.py`, `app/services/embed_service.py`, `k8s_zhiyuan/`, `frontend/src/utils/appBase.ts`, `docker/nginx-zhiyuan.example.conf`, `tests/frontend/test_app_base_subpath_contract.py`, `tests/api/v1/test_embed_ticket.py`, `tests/CHECKLIST.md` | **二级目录必须设 `APP_ROOT_PATH=/zhiyuan`**：K8s 用 `k8s_zhiyuan/`。本机直连 `http://localhost:8001/zhiyuan` 时中间件会剥前缀并注入 SPA base（反代未剥也能进页面）。`install.sh` 会写入前缀并选用对应 Ingress。MCP issuer 在 mount 前绑定；平台 Origin 的 `download_url_prefix` 自动补 `/zhiyuan`。iframe 换票 Origin 是平台站点本身（公网 HTTP 常无 `Sec-Fetch-Site`），`/zhiyuan/embed` Referer 与 `APP_PUBLIC_URL` 视为同源，不能拿宿主白名单卡换票；Nginx 用 `$http_host` 保留端口。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入「更多」指令库分组且删除后不关闭 (Embed Command Drawer Split Groups Stay Open) | `frontend/src/components/embed/ChatInput.vue`, `tests/frontend/test_shortcut_more_drawer_contract.py`, `tests/CHECKLIST.md` | **个人/系统分开展示**：更多弹层桌面用固定宽度一行 5 个卡片（绿=个人、蓝=系统提示词），不依赖 Tailwind 网格；确认删除对话框不算点击外部，删完指令库保持打开。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入应用列表独立维护常用提示词 (Embed App Prompt Modal On List) | `frontend/src/views/EmbedApps.vue`, `app/schemas/embed_app.py`, `docs/md/embed_integration_guide.md`, `tests/frontend/test_embed_routing_preference_contract.py`, `tests/CHECKLIST.md` | **提示词不进编辑表单**：列表行提供「提示词」入口，弹窗高度固定、内容区滚动；每条名称+多行内容，删除用图标；单独保存 `shortcut_prompts`，编辑应用本身不再覆盖提示词。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入快捷指令条固定且仅保留新会话/历史 (Embed Pin Shortcut Bar New+History Only) | `frontend/src/views/EmbedChat.vue`, `frontend/src/components/embed/ChatInput.vue`, `tests/frontend/test_embed_url_agent_lock_contract.py`, `tests/CHECKLIST.md` | **去掉上移折叠**：嵌入页快捷指令始终在输入框上方并保留「快捷指令」标题（不可点击上移）；系统胶囊只留「新会话」「历史」，新会话直接开普通会话、无类型下拉；「更多」不再展示 System · 系统功能。输入 `/` 弹出层对齐专家选择：上下键循环当前 Tab 项、左右键切换指令/技能 Tab、Enter 选择 / Esc 关闭（默认指令），列表固定五行；嵌入页「+」直接上传本地文件，不再展开技能/MCP/专家等菜单。智能体调试页仍可折叠并保留全套系统指令与加号菜单。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 嵌入应用个人快捷指令绑定业务入口 (Embed Personal Slash Commands Per App) | `db-prod/V163-slash-commands-embed-app-key.sql`, `db-prod-pg/V64-slash-commands-embed-app-key.sql`, `app/services/slash_command_service.py`, `app/api/portal/endpoints/slash_commands.py`, `frontend/src/views/EmbedChat.vue`, `frontend/src/components/embed/ChatInput.vue`, `docs/md/embed_integration_guide.md`, `tests/api/v1/test_embed_ticket.py`, `tests/frontend/test_embed_url_agent_lock_contract.py`, `tests/CHECKLIST.md` | **「+」自建指令也跟嵌入应用走**：`slash_commands.embed_app_key` 在嵌入会话中强制写入当前 `app_key`；列表/改删/排序只返回该入口下本人指令，不带站内 admin/system 全局指令，也不串到其他子系统。站内调试仍用 `embed_app_key IS NULL` 的全局指令。 | ✅ 代码已落地；PG 已 MCP 执行 V64；MySQL 需自跑 V163；未代跑启动脚本 | 2026-09-17 |
| 嵌入应用按入口配置常用提示词 (Embed App Shortcut Prompts) | `db-prod/V162-embed-apps-shortcut-prompts.sql`, `db-prod-pg/V63-embed-apps-shortcut-prompts.sql`, `app/models/embed_app.py`, `app/schemas/embed_app.py`, `app/services/embed_app_service.py`, `app/services/embed_service.py`, `app/api/portal/endpoints/embed_apps.py`, `app/api/portal/endpoints/auth.py`, `frontend/src/views/EmbedApps.vue`, `frontend/src/views/EmbedChat.vue`, `frontend/src/components/embed/ChatInput.vue`, `docs/md/embed_integration_guide.md`, `tests/api/v1/test_embed_ticket.py`, `tests/CHECKLIST.md` | **业务入口各自一套提示词，个人指令仍可自建**：嵌入应用 `shortcut_prompts` 只给该 iframe；iframe 另拉本人且绑定当前应用的快捷指令，保留「+」独享维护，不展示 admin/system 全局指令。站内调试仍用全局快捷指令。 | ✅ 代码已落地；PG 已 MCP 执行 V63；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入顶栏就绪状态跟在智能体名称后 (Embed Header Status Inline After Agent Name) | `frontend/src/views/EmbedChat.vue`, `tests/frontend/test_embed_url_agent_lock_contract.py`, `tests/CHECKLIST.md` | **顶栏单行**：「准备就绪 / 正在处理您的请求…」从名称下方改到名称右侧；锁定徽标与退出仍在同一行，名称过长时截断名字、状态不换行。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入对话工具批准默认自动批准 (Embed Chat Default Approval Mode Allow) | `frontend/src/views/EmbedChat.vue`, `frontend/src/components/embed/ChatInput.vue`, `tests/frontend/test_expert_and_approval_close_contract.py`, `tests/CHECKLIST.md` | **嵌入页默认「自动批准」**：`config.approvalMode` 与发消息 `permission_options.approval_mode` 缺省由 `ask` 改为 `allow`；胶囊未传值时也显示自动批准。智能体调试页仍默认请求批准。本机若曾保存过「请求批准」，localStorage 仍会覆盖默认值。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-17 |
| iframe 专家中心不展示自定义专家 Tab (Hide Custom Experts Tab in Embed Iframe) | `frontend/src/components/embed/ExpertCascadeMenu.vue`, `frontend/src/utils/embedHost.ts`, `tests/frontend/test_expert_cascade_tabs_and_search_contract.py`, `tests/CHECKLIST.md` | **业务系统嵌套只开放平台专家**：`isEmbeddedInIframe()` 为真时隐藏「系统/自定义」双 Tab 与自定义专家列表，标题/底部计数只含平台专家；顶层 `/embed/chat` 与智能体调试页仍保留自定义 Tab。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入应用改为关联角色，iframe 入口锁定改为开关 (Embed Apps Role Binding & Unlockable Agent Switcher) | `db-prod/V160-embed-apps-role-and-lock.sql`, `db-prod-pg/V61-embed-apps-role-and-lock.sql`, `db-prod/V161-embed-apps-drop-shadow-and-tenant.sql`, `db-prod-pg/V62-embed-apps-drop-shadow-and-tenant.sql`, `app/models/embed_app.py`, `app/schemas/embed_app.py`, `app/services/embed_app_service.py`, `app/services/embed_identity.py`, `app/services/embed_service.py`, `app/services/ai/agent_manager.py`, `app/api/portal/endpoints/embed_apps.py`, `frontend/src/views/EmbedApps.vue`, `frontend/src/views/EmbedChat.vue`, `docs/md/embed_integration_guide.md`, `tests/api/v1/test_embed_ticket.py`, `tests/CHECKLIST.md` | **智能体范围跟角色、iframe 默认可切换**：① 去掉嵌入应用 `allowed_agent_ids` 勾选，改为 `role_id` 关联角色资产；签发人须属于该角色（管理员除外），Ticket `agent_id` 若传入必须在角色授权内。② 新增 `lock_entry_agent`（默认关）：关闭时 Ticket 可不传 `agent_id`，会话不锁定入口，iframe 显示智能委派/切换；开启时仍强制 `agent_id` 并锁定。③ 兑换响应仅在锁定时返回 `agent_id`。未绑定应用的旧 identity Ticket 仍按有 `agent_id` 即锁定。④ 管理端不再展示身份字段勾选、映射账号、租户隔离；保存时固定全量 claims。⑤ MySQL `V161` / PG `V62` 删除 `create_shadow_user`、`isolate_datasets_by_tenant` 列，绑定应用后不写平台用户、不按租户切数据集。 | ✅ 代码已落地；PG 已 MCP 执行 V61；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入技能目录按应用角色智能体技能并集过滤 (Embed Skills Catalog Uses Published Allowlist) | `app/api/portal/endpoints/skills.py`, `app/services/ai/agent_manager.py`, `app/services/ai/skill_resolver.py`, `frontend/src/components/embed/SkillCascadeMenu.vue`, `frontend/src/components/embed/ChatInput.vue`, `frontend/src/views/EmbedChat.vue`, `tests/api/v1/test_embed_ticket.py`, `tests/frontend/test_personal_skills_experience_contract.py`, `tests/ai/test_skill_resolver.py`, `tests/test_skills_management.py`, `tests/CHECKLIST.md` | **开发面与运行面分离**：iframe 技能菜单不再请求 `GET /agents/{id}/active-config`。`GET /api/portal/skills` 在嵌入会话中按应用角色绑定智能体的已发布 `skills_custom`/`skills` **去重并集**过滤公共技能（任一智能体未自定义则仍是全部已启用公共技能）；不再按当前会话单个智能体过滤，也不再跨智能体 403。**嵌入 `/` 与技能中心不展示个人技能**。站内调试/技能管理页行为不变。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-18 |
| 彻底移除对话卡片登记与 iframe 运行时 (Remove Registered UI Cards Runtime) | `app/api/portal/api.py`, `app/services/ai/tools/registry.py`, `frontend/src/views/Dashboard.vue`, `frontend/src/utils/agentscopeSseHandlers.ts`, `data/skills/ui-card-demo/SKILL.md`, `tests/CHECKLIST.md` | **产品决定不再保留独立卡种运行时**：删除 `/dashboard/ui-cards`、`show_ui_card`、iframe 宿主、权限种子、同域演示页与演示技能；提问卡 / 业务确认卡保留。PG 已 MCP 删表并清权限、去掉版本工具里的 `show_ui_card`；**建表/删表迁移脚本一并移除**（原 MySQL `V155/V160`、PG `V56/V61`），避免 `apply-sql --all` 再次建表。 | ✅ 代码与 PG 已落地；MySQL 若仍有表需手工 DROP；未代跑启动脚本 | 2026-09-17 |
| 同步上游 main 并错开撞号的迁移脚本 (Sync Upstream Main and Renumber Colliding Migrations) | `db-prod/V158-add_metadata_quality_score.sql`, `db-prod/V159-align-sandbox-k8s-namespace.sql`, `db-prod-pg/V59-add_metadata_quality_score.sql`, `db-prod-pg/V60-align-sandbox-k8s-namespace.sql`, `tests/CHECKLIST.md` | **把 RandyChen1985/nanzi-ai-agent-platform 的 main 合进本 fork**：保留本仓库嵌入身份与对话卡片改动；上游质量分/沙箱命名空间迁移与本仓库 `V155/V156`、`V56/V57` 撞号，分别改到 MySQL `V158/V159`、PG `V59/V60`。 | ✅ 冲突已解并合入；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入记忆浏览抽屉对齐 session_owner，放行 /memory/my/summaries (Embed Memory Browser Session Owner) | `app/api/portal/endpoints/memory.py`, `app/services/embed_api_guard.py`, `tests/api/v1/test_embed_ticket.py`, `tests/test_memory_my_portal.py`, `tests/CHECKLIST.md` | **读写同一把会话钥匙**：① `/my/*` 记忆列表/删除/LTM/每日摘要用 `require_user_id`（嵌入 `e:…`），不再 `int(签发人)`；② 嵌入 owner 展示名用会话声明，禁止 `int(e:…)`；③ 白名单只放行抽屉用的 `GET/DELETE /memory/my/summaries*` 与 `DELETE /memory/my/session-memory`，管理面与 LTM 仍拒绝。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-17 |
| 嵌入工具工作区与门户 Redis 对齐 session_owner，放行 ChatBI 监控/导出 (Embed Workspace Tools Prefs Redis and ChatBI Allowlist) | `app/services/ai/tools/resource_catalog_tools.py`, `app/services/ai/tools/advanced_auxiliary_tools.py`, `app/services/ai/tools/agent_delegate_tool.py`, `app/api/portal/endpoints/portal_prefs.py`, `app/api/v1/endpoints/fs.py`, `app/services/embed_api_guard.py`, `tests/api/v1/test_embed_ticket.py`, `tests/api/portal/test_portal_prefs.py`, `tests/CHECKLIST.md` | **补齐上一轮遗漏**：① 目录清单/SQLite scratchpad/文件检索/子智能体 dispatch 不再用签发人 `ctx.user_id` 拼工作区身份，带上 `session_owner`；② `portal-prefs` 与工作区最近文件/浏览偏好 Redis 用会话 owner；③ 嵌入白名单增加 ChatBI 监控、明细导出、markdown-theme。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| 嵌入会话记忆/ChatBI Redis/产物列表与提示词对齐 session_owner (Embed Session Memory and ChatBI Redis Isolation) | `app/services/ai/conversation_identity.py`, `app/services/ai/tools/memory_search_tool.py`, `app/services/ai/agent_service.py`, `app/api/portal/endpoints/chatbi_briefs.py`, `app/api/portal/endpoints/chatbi_monitors.py`, `app/api/portal/endpoints/saved_reports.py`, `app/api/v1/endpoints/chat.py`, `app/services/workbench_home_service.py`, `app/services/ai/memory_index_service.py`, `app/services/ai/scheduler_service.py`, `tests/ai/test_conversation_identity.py`, `tests/CHECKLIST.md` | **读写同一把会话钥匙**：① 跨会话记忆检索/预加载/工作台记忆计数用 `session_owner`（`e:…`），不再读签发人 `user_id=1`；② ChatBI 简报/监控/黄金报表 Redis 栈与 last_data_result 用会话 owner，DB 外键仍为平台整型；③ 审计导出 `register_artifact` 不再 `int(e:…)`，产物列表按会话归属隔离；④ 提示词展示 `external_subject`；⑤ 调度合并扫描 `memory:summary:e:*`。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| 嵌入会话存储身份与聊天历史对齐，HITL/artifact/state 不再误用签发人 user_id=1 (Embed Session Storage Identity Unification) | `app/services/ai/conversation_identity.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/services/ai/runners/data_agent_runner.py`, `app/services/ai/runtime/agentscope/resume.py`, `app/services/ai/runtime/agentscope/workspace.py`, `app/services/ai/agent_service.py`, `app/services/ai/tools/session_status.py`, `app/services/ai/runtime/agentscope/tools.py`, `app/services/ai/runners/chatbi/followup_data.py`, `app/api/v1/endpoints/browser.py`, `tests/CHECKLIST.md` | **会话 Redis 钥匙与聊天历史同一把**：① `_runtime_user_id` / HITL persist / resume peek / reusable / compaction / workspace 优先 `session_owner`（`e:…`），嵌入不再把提问卡写到 `conversation:1:` 而回执查 `conversation:e:…`；② 控制面 ACL、配额、默认 MCP、SQL 改写、forbidden_tools 仍用平台整型 ID（`e:…` 不可 `int()`，回落到 `AgentContext.user_id`）；③ 浏览器 profile 用 session_owner 稳定哈希整型，避免多个业务用户共用签发人行。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| 嵌入应用平台化：claims 白名单、会话外部 subject、API 硬隔离、MCP 下沉权限与租户数据集隔离 (Embed Apps Phase 2/3) | `db-prod/V156-create_embed_apps.sql`, `db-prod-pg/V57-create_embed_apps.sql`, `app/models/embed_app.py`, `app/services/embed_app_service.py`, `app/services/embed_api_guard.py`, `app/services/embed_service.py`, `app/api/portal/endpoints/embed_apps.py`, `frontend/src/views/EmbedApps.vue`, `docs/md/embed_integration_guide.md`, `tests/api/v1/test_embed_ticket.py`, `tests/CHECKLIST.md` | **二期应用实体 + 三期权限下沉/租户**：① `sys_embed_apps` 登记允许智能体、域名、是否要求 identity、claims 白名单、JIT 影子、`nanzi_sql_rewrite`/`mcp_only`、按 `tenant_id` 隔离数据集；② Ticket 传 `app_key`，会话 `session_owner` 认外部 subject，可关影子；③ 嵌入 session 拒绝用户管理/MCP 注册等管理面；④ `mcp_only` 跳过 SQL 行级改写；数据集/知识库 `tenant_id`；⑤ 管理端「嵌入应用」页。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| 嵌入执行以业务用户 claims 为准，南孜仅作智能体开发平台控制面 (Embed Business-Authoritative Identity) | `app/services/embed_identity.py`, `app/services/embed_service.py`, `app/api/v1/endpoints/embed.py`, `app/services/ai/agent_service.py`, `app/services/ai/agent_manager.py`, `app/services/ai/context_manager.py`, `app/services/quota_service.py`, `app/services/ai/tools/mcp_factory.py`, `docs/md/embed_integration_guide.md`, `tests/CHECKLIST.md` | **嵌套与运行时数据权限跟业务用户，不跟南孜目录合成一套**：① Ticket 新增 `identity`（subject/部门/租户/extra_data），宿主服务账号签发，JIT 影子账号（无密码无 Key，禁止登录管理端）；② 嵌入 session 锁定 `agent_id`，智能体 ACL/配额认签发人；③ SQL 行级与 MCP `user_id` 认业务 subject，`role=admin` 不能升级为平台管理员；④ 旧 `username` 代客模式保留。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| MCP 向导第二步保存按钮转圈并防重复提交 (MCP Wizard Step-2 Save Loading Guard) | `frontend/src/components/system/McpServerRegistry.vue`, `tests/CHECKLIST.md` | **探测成功后点保存会转圈**：`saving` 期间禁用保存/返回/取消，按钮文案为「正在保存…」或「正在添加…」，避免连点重复写入。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| 编辑已有 MCP 再点连接时复用已保存 Token，避免误报缺少 Authorization (MCP Verify Reuses Stored Token) | `app/api/portal/endpoints/mcp.py`, `frontend/src/components/system/McpServerRegistry.vue`, `tests/CHECKLIST.md` | **第二次编辑不会回显 Token**：verify 带 `existing_server_id`，未填新 Token 时用库里已加密保存的 Authorization；关掉开关才不发送。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| MCP 出站 Header 改为 UTF-8 bytes，避免中文身份/Token 触发 ascii codec (MCP UTF-8 Header Encoding) | `app/services/mcp/mcp_auth_policy.py`, `app/services/ai/tools/mcp_client.py`, `tests/CHECKLIST.md` | **httpcore 只接受 ASCII 字符串 Header**：含中文的 `Authorization` 或 `X-Nanzi-User-Context` 以 UTF-8 bytes 发送，连接/发现工具不再报 `ascii codec can't encode`。身份 JSON 保持明文（`ensure_ascii=False`）。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| MCP 用户身份改为 Header 明文 JSON，去掉加签验签 (MCP Plaintext User Identity Header) | `app/services/mcp/mcp_auth_policy.py`, `app/services/mcp/user_context_assertion.py`, `app/services/mcp/echo_server.py`, `app/api/portal/endpoints/mcp.py`, `frontend/src/components/system/McpServerRegistry.vue`, `FAQ.md`, `tests/CHECKLIST.md` | **开启「用户身份传递」后只发明文 `X-Nanzi-User-Context` JSON + `X-Request-ID`**：不再签发 JWS、不再要求 Audience/Issuer/JWKS/私钥。业务 MCP 直接解析 `user_id`。密钥类字段仍过滤。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| 对话气泡打开上传文件改为鉴权拉取，避免 fs/preview 401 (Chat Attachment Open Uses Authenticated Fetch) | `frontend/src/utils/workspaceFilePreview.ts`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/CHECKLIST.md` | **xlsx 等非图片附件不再用无 Token 的 `<a href=/api/v1/chat/fs/preview>` 新开页**：点击后走 axios 带登录态，Office 文件下载，pdf/文本进画布，与工作区打开文件同一路径。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| Excel profile/filter 按列基数自适应 TopN，不全局改为 1000 (Adaptive TopN by Column Cardinality) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `tests/CHECKLIST.md` | **低基数分类列最多回 200（能全量则全量），高基数列仍 50**：按 `unique_count/non_empty` 判断，不认具体列名；返回 `omitted_unique`、`top_values_row_coverage`、`top_values_complete`。摘要标明名单不完整；提示词要求未列出对象必须 `filter+eq`，禁止当成 0 条。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| Excel filter 对命中行按全部列做 top_values，避免 contains 总行数误记到一家 (Filter Matched-Values Cover All Columns) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `tests/CHECKLIST.md` | **按地区筛完也能直接读各企业条数**：`matched_values` 覆盖命中行的全部列而非仅筛选列；summary 在多值列时声明 `matched_count` 不是单一对象条数。提示词同步。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| Excel 分析 TopN 与 filter 样本统一为 50 (Excel Analysis TopN and Filter Sample Unified to 50) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `tests/CHECKLIST.md` | **profile/filter 的 top_values 由 15 改为 50**，与 filter 样本行上限一致；read_range 仍为 1000 行浏览窗口。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| Excel read_range 单次行上限由 200 调整为 1000 (Excel read_range Row Cap 200 → 1000) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `tests/CHECKLIST.md` | **浏览窗口加大、分析仍走 profile/filter**：`_MAX_RANGE_ROWS` 与提示词同步为 1000；超限仍截取并返回 `next_range`。统计/筛选不走此上限。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| Excel 跳过标题行识别表头，filter 回传命中值分布避免 contains 混对象 (Excel Skip Title Header Row & Filter Matched-Value Breakdown) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `tests/CHECKLIST.md` | **首行常为通栏标题、下一行才是列名**：inspect/profile/filter 按「仅一格且文字较长」跳过标题，不依赖具体列名；filter 额外返回 `matched_values` 分列命中分布。提示词要求专有全称用 `eq`，禁止 `contains` 截短名称。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| Excel filter 工具内筛选行，只回命中条数与样本 (Excel In-Tool Row Filter: Count + Sample, Optional Export) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `app/services/ai/tool_nudge_policy.py`, `app/services/ai/runners/assistant_agent_runner.py`, `frontend/src/views/AgentManagement.vue`, `frontend/src/utils/processTimeline.ts`, `frontend/src/components/chat/ChatExecutionTimeline.vue`, `tests/CHECKLIST.md` | **3000 行条件筛选不再灌进模型**：① `excel_document_read` 新增 `action=filter`，用结构化 `filters`（header/column + op + value，and/or）在工具进程扫描，禁止 eval；② 返回 `matched_count` 与最多 50 行样本，需要全量时传 `output_filename` 导出 xlsx（最多 5000 行）；③ 提示词禁止为此写 Python/翻页 `read_range`。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| Excel profile 聚合统计、达步数上限强制收口、禁止整表翻页 (Excel Profile Action, Max-Iters Wrap-Up & No Full-Sheet Paging) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/runtime/agentscope/event_stream.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/services/ai/runners/chatbi/react_stream.py`, `app/services/ai/agent_prompts.py`, `app/services/ai/executors/prompts.py`, `frontend/src/views/AgentManagement.vue`, `tests/CHECKLIST.md` | **分析类 Excel 不再靠 20 步翻页**：① `excel_document_read` 新增 `profile`，一次返回各表列分布、唯一值数与 TopN，可省略表名扫描整簿；② `EXCEED_MAX_ITERS` 改为时间线警告，助手链路强制一轮无工具收口总结，失败则给出可继续提示；③ 提示词要求分析/统计走 profile，禁止 `read_range` 整表翻页。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| Excel 工作表名唯一前缀匹配，失败时列出真实表名 (Excel Sheet Unique-Prefix Resolve & List Names on Miss) | `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/agent_prompts.py`, `tests/CHECKLIST.md` | **「6月疲劳」对不上「6月疲劳驾驶」不再只报不存在**：① 精确匹配失败后，若请求名是唯一前缀/包含则自动落到那张表；② 零匹配或一词多表时错误里列出当前工作簿全部表名，便于模型立刻用完整名重试；③ 提示词要求 `sheet_name` 用 inspect 完整名，禁止缩写。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| 会话临时文件可直接覆盖、Excel 超限截取分页、错误保留 /workspace 合同路径 (Session Scratch Overwrite, Excel Range Truncation & Keep Workspace Contract Paths) | `app/services/ai/runtime/agentscope/workspace.py`, `app/services/ai/tools/excel_document_tool.py`, `app/services/ai/error_response_service.py`, `app/services/ai/agent_prompts.py`, `tests/CHECKLIST.md` | **打断「写脚本失败 → 读不存在结果 → 整列读 Excel 超限」死循环**：① `sessions/` 下已有临时文件再次 Write 时先移除再写，避开 AgentScope「必须先 Read 再覆盖」；② 错误文本不再把 `/workspace/...` 打成 `[internal path]`；③ `excel_document_read` 超 200 行/50 列改为截取首段并返回 `next_range`，不再直接失败。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| 撤回对话卡片技能按需挂载，恢复为智能体可勾选系统工具 (Revert Skill-Gated show_ui_card; Restore Selectable Agent Tool) | `app/services/ai/ui_card.py`, `app/services/ai/agent_service.py`, `app/services/ai/tools/registry.py`, `app/services/ai/prompt_assembler.py`, `app/services/ai/tool_capability.py`, `app/services/ai/runtime/agentscope/agent_runtime.py`, `frontend/src/views/AgentManagement.vue`, `frontend/src/views/UiCards.vue`, `data/skills/ui-card-demo/SKILL.md`, `tests/CHECKLIST.md` | **按产品要求撤回按需挂载**：`show_ui_card` 重新出现在智能体「工具能力」列表，由版本勾选决定是否绑定；技能不再动态塞工具 schema，也不再把该工具排除出 tools fingerprint。 | ✅ 已撤回按需挂载；未代跑真实服务与启动脚本 | 2026-09-16 |
| 刷新后恢复提问卡轮次：awaiting_user 写入审计且聊天页读 Redis 会话 (Persist HITL Awaiting-User History & Reload From Redis) | `app/services/ai/pipeline/steps/finalize_step.py`, `app/api/v1/endpoints/chat.py`, `frontend/src/views/EmbedChat.vue`, `tests/ai/pipeline/test_pipeline_steps.py`, `tests/CHECKLIST.md` | **刷新不再丢掉出卡轮**：① 提问卡/确认卡/对话卡片的 `awaiting_user` 作为本轮终态写入 `AgentExecutionHistory`（权限确认等仍会在同一 trace 恢复的状态继续跳过完结审计）；② EmbedChat 刷新改为读 `/api/v1/chat/conversation/{id}`（Redis，空则回退审计库），与调试页一致，避免只看到最后一条回执；③ 修正 GET 装饰器误挂到 `_merge_latest_audit_assistant` 导致缺 body.history 的 4001。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| HITL 回执后仍缺输入必须再出卡，禁止只写预告正文 (HITL Receipt Must Re-Ask Missing Details via Card) | `app/services/ai/agent_prompts.py`, `app/services/ai/tools/user_question_tools.py`, `frontend/src/utils/userQuestion.ts`, `tests/CHECKLIST.md` | **多步提问向导不断档**：用户答完一张提问卡后，若原任务还缺执行细节，模型须本轮再调 `ask_user_question` / `show_ui_card` / `request_user_confirmation` 出下一张卡；禁止只回复「还需要确认以下执行细节：」然后结束。回执协议与工具描述同步约束。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| HITL 回执不误复用提问卡、出卡轮仍保存会话指纹 (HITL Receipt Skip Reusable Artifact & Persist State on Interrupt) | `app/services/ai/session_tool_artifact.py`, `app/services/ai/reusable_result.py`, `app/services/ai/runners/assistant_agent_runner.py`, `tests/CHECKLIST.md` | **根除回执轮假空白**：① `ask_user_question` / `request_user_confirmation` / `show_ui_card` 不写入会话可复用快照；② `【用户回答】` 等回执不再命中「继续」类复用意图，避免注入 `<backend_tool_result_context>` 后被前端剥空；③ 出提问卡/对话卡片中断时仍保存 AgentScope 状态与 tools fingerprint，避免下一轮指纹对不上而重置会话。 | ✅ 代码已落地；未代跑真实服务与启动脚本 | 2026-09-16 |
| HITL 回执气泡可读化、按需工具不重置会话、回执空回复兜底 (HITL Receipt Display, Skill-Gated Fingerprint & Empty-Reply Fallback) | `frontend/src/utils/hitlReceiptDisplay.ts`, `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `app/services/ai/runtime/agentscope/stream_reconcile.py`, `app/services/ai/runners/assistant_agent_runner.py`, `tests/CHECKLIST.md` | **修复卡片回执协议暴露与续跑空白**：① 提问卡/确认卡/对话卡片回执仍以协议发给模型，用户气泡只显示「已选择：…」「已确认业务数据」等摘要；② 回执轮若未出新卡且可见正文为空，补一句明确提示，避免只剩时间线。原「show_ui_card 不计入 fingerprint / 技能按需挂载」已按产品要求撤回。 | ✅ 代码已落地；按需挂载已撤回；未代跑真实服务与启动脚本 | 2026-09-16 |
| 对话卡片工具改为技能按需挂载，不再作为智能体可勾选系统工具 (Skill-Gated show_ui_card Implicit Mount) | `app/services/ai/ui_card.py`, `app/services/ai/agent_service.py`, `app/services/ai/tools/registry.py`, `app/services/ai/prompt_assembler.py`, `app/services/ai/tool_capability.py`, `app/services/ai/runners/assistant_agent_runner.py`, `frontend/src/views/AgentManagement.vue`, `frontend/src/views/UiCards.vue`, `data/skills/ui-card-demo/SKILL.md`, `tests/CHECKLIST.md` | **避免双开关与上下文常驻**：① 从智能体「系统工具」列表移除 `show_ui_card`，旧版本勾选在解析时忽略；② 本轮技能注入文本含 `show_ui_card` / `【UI卡片】` 时才挂载工具 schema 并注入「业务对话卡片」提示词，未命中技能的对话不占工具上下文；③ 出卡门禁仍是卡种启用 + `allowed_agent_ids`。 | ❌ 已按产品要求撤回 | 2026-09-16 |
| 自定义对话流卡片：登记卡种 + iframe 宿主 + 统一 HITL 回执 (Registered UI Cards in Chat with iframe Host & 【UI卡片】 Receipt) | `db-prod/V155-create_sys_ui_cards.sql`, `db-prod-pg/V56-create_sys_ui_cards.sql`, `app/models/ui_card.py`, `app/api/portal/endpoints/ui_cards.py`, `app/services/ai/tools/ui_card_tools.py`, `app/services/ai/ui_card.py`, `app/services/ai/ui_card_store.py`, `frontend/src/views/UiCards.vue`, `frontend/src/components/UiCardHost.vue`, `frontend/src/views/UiCardDemo.vue`, `frontend/src/utils/agentscopeSseHandlers.ts`, `tests/CHECKLIST.md` | **复杂业务确认页由业务系统自定义、平台只登记卡种**：① **双库迁移** 新增 `sys_ui_cards` 与 `menu:ui_cards` / `element:ui_cards:*` 权限种子，以及同域 demo 卡 `platform_demo_confirm`（PG 已通过 MCP `execute_sql` 落地）；② **工具 `show_ui_card`** 仅接受已登记 `card_key`+`data`，禁止模型传 URL/HTML，成功后 SSE `type=ui_card` 且终态 `awaiting_user`；③ **Redis pending + 一次性 card_token**，回执前缀 `【UI卡片】`，路由粘性到上一轮智能体，preflight 校验 action/未过期/payload≤64KB；④ **管理端** 侧栏「智能体开发平台」与 MCP 工具集并列「对话卡片」登记页；⑤ **对话宿主** `UiCardHost` iframe + postMessage（INIT/READY/RESIZE/SUBMIT），EmbedChat / AgentDebug 提交回执；⑥ **同域演示页** `/embed/ui-card-demo` 用于闭环验收。 | ✅ 代码与 PG 种子已落地；未代跑真实服务与启动脚本 | 2026-09-15 |
| 图片读取工具 (read_image) 确定性触发与实体词边界防误触治理：隔离通用字面相关度匹配与证据兜底强推、专设独立实体词与动词解析器、为 png/gif 等英文后缀增加全词边界正则校验防子串误伤（如 gift 卡）、剔除视觉/图谱等抽象概念词、增加用法说明咨询类问询防御 (read_image Deterministic Tool Nudge & Word Boundary Anti-False-Positive Governance) | `app/services/ai/tool_nudge_policy.py`, `tests/ai/test_tool_nudge_policy.py`, `tests/CHECKLIST.md` | **图片读取工具确定性前置路由与防路径幻觉防护闭环**：① **痛点与隔离治理**：`read_image` 强依赖物理存在的图片文件路径，原先参与通用字面相关度（Relevance Score）时，因工具 Description 中富含“查看/分析/识别/检查”等高频动词，导致模型闲聊或分析普通文字时被虚高打分并 `force_first_call=True`，进而引发虚构不存在路径的幻觉；本次将其从通用相关度与证据型 fallback 候选池中硬性排除（`excluded.add(_IMAGE_TOOL_NAME)`）；② **确定性解析器与英文词边界正则防御**：构建 `_resolve_image_tool_nudge` 专属解析器；编写专用的 `_contains_image_entity` 函数，对 `png`, `jpg`, `gif`, `chart`, `ocr` 等英文实体词采用 `(?<![a-z0-9])term(?![a-z0-9])` 正则全词边界校验，彻底根治用户输入 `gift` 卡、`charter` 计划等日常词汇时因包含 `gif`/`chart` 子串而被误判触发 `read_image` 的潜在 Bug；③ **抽象概念词收窄与用法解释防御**：从实体词中剔除“视觉”、“图谱”等容易在技术选型或知识图谱问询中被误伤的高频抽象词，聚焦“截图/照片/图表/折线图/柱状图/动图”等具象视觉实体；增加 `_IMAGE_EXPLANATION_TERMS`（“是什么”、“怎么用”、“说明”、“介绍”等），对纯用法咨询予以放行不强推首调；④ **提示词双重防线**：Nudge message 明确提示“若问题里没有给出图片文件，先结合实际工作区/上传产物找对应的图片路径，找不到时如实说明，不要编造图片路径或凭记忆描述图片内容”；⑤ **测试闭环**：在 `test_tool_nudge_policy.py` 中补充普通文本闲聊、gift卡子串、抽象图谱概念、用法解释问答等多组防误触断言，以及图片实体/动图/文件的正向首调用断言；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 单测用例扩展覆盖；未代跑真实服务与启动脚本 | 2026-09-16 |
| EmbedChat 历史会话侧边栏 UI/UX 全面升级与顶栏状态排版轻量化：顶栏全量按钮升级为非原生深色磨砂 Tooltip 气泡（对齐 AI 消息操作栏规范）、顶栏字号精细化（text-[13px] font-semibold 消除突兀感）、消息气泡智能体胶囊中性微质感灰底与微圆点对齐重构（消除高饱和蓝色割裂感）、顶栏严格等高水平对齐 (h-12)、纯净分栏侧面板图标（与外层汉堡菜单解耦消除噪点）、全局顶部左右折叠菜单左侧大面积空白消除与垂直基线精准对齐 (X=12px)、侧边栏对偶收起图标、移除冗余按钮保持紧凑、高亮当前激活会话、搜索 300ms 防抖与一键清空、移除杂乱彩圈数字水印重构卡片视觉、卡片悬浮操作（单会话删除/导出Markdown/回溯日志）、时间分组折叠面板 (Accordion) 与微光骨架屏动效 (EmbedChat History Sidebar UI/UX Overhaul & Header Typography Lightweight Refinement: Full Header Dark Backdrop-Blur Custom Tooltips, text-[13px] font-semibold Header Scaling, Agent Status Neutral Micro-Texture Capsule with Status Dot, Strict h-12 Header Alignment, Pure Split-Panel Icon & Complementary Collapse Button, Global Top Header Left Padding Alignment at X=12px, Active Session Highlight, 300ms Search Debounce & Clear, Watermark Removal & Card Redesign, Hover Actions for Delete/Export/Logs, Date Accordion & Shimmer Skeleton) | `frontend/src/views/Dashboard.vue`, `frontend/src/components/ChatHistorySidebar.vue`, `frontend/src/views/EmbedChat.vue`, `tests/CHECKLIST.md` | **EmbedChat 历史会话侧边栏、全局顶栏与消息状态交互可用性与视觉品质全链路重构闭环**：① **顶栏全量按钮统一升级为非原生深色磨砂自定义 Tooltip 气泡**：全面移除浏览器原生滞后、生硬的 `title` 提示，严格对齐 AI 消息底部操作栏（`MessageActionMenus.vue`）的设计规范——采用向下平滑微浮现（`top-full mt-1.5 duration-150`）、顶部 45° 小菱形尖角、`bg-gray-900/90 dark:bg-gray-800/95 shadow-lg backdrop-blur-sm` 高质感黑透磨砂气泡，覆盖顶栏左侧「展开/收起会话历史 (⌘H)」及右侧「全屏/退出全屏」、「快捷指令」、「查看帮助」、「对话设置」、「打开服务端浏览器」全部 6 个功能按钮；② **顶栏智能体名称与状态排版轻量化重塑**：彻底解决原 `text-sm font-black`（900 极粗）过重导致在 48px 顶栏中显得头重脚轻的突兀感，调整为 `text-[13px] font-semibold` 优雅适中的字重，副标题收敛为 `text-[11px] font-normal`，去除过宽字母间距，视觉焦点更自然；③ **消息头部智能体状态胶囊中性高质感重构**：告别原先割裂且刺眼的纯蓝块（`bg-blue-50/80`），采用与下方执行进度组件和谐统一的现代中性浅灰底微边框（`bg-gray-50/90 dark:bg-gray-800/80 border-gray-200/70`），并引入 `w-1.5 h-1.5` 主题色指示圆点（`bg-primary/70`），智能体名称温润清晰，`· 为您服务` 弱化为浅灰，不抢正文风头；④ **全局顶部折叠按钮与会话顶栏垂直严格对齐 (X=12px)**：定位并修复了外层 `Dashboard.vue` 顶栏使用 `lg:px-8`（32px）导致顶部左右折叠按钮（`☰`）左侧突兀留白 20px+ 的严重视觉错位断层，将顶栏 padding 统一收敛为 `px-4` 并为按钮加上 `-ml-1 p-1.5`，使最上层折叠按钮与下层历史会话按钮在左侧起点 `X=12px` 绝对垂直对齐；⑤ **顶栏高度水平严格对齐 (h-12)**：修正侧边栏 Header 原 `h-14`（56px）与主区顶栏 `h-12`（48px）的 8px 错位断层，统一设置为 `h-12` + `backdrop-blur-md`，底边线横向绝对水平齐平；⑥ **顶栏与侧边栏对偶图标纯净打磨**：顶栏采用**极简大圆角矩形 + 垂直贯穿分栏线图标**，剔除右侧细小横线噪点，小尺寸下清晰利落；侧边栏内由生硬的模态框 `✕` 升级为**对偶的带向左收起箭头的面板折叠图标**，形成自然完整的展开/收起动线；⑦ **精简顶部空间、去除冗余**：彻底移除侧边栏上方多余的新建会话占位块与空行，搜索框紧随标题栏，大幅提高垂直空间利用率；⑧ **修复当前会话选中态高亮 (P0)**：解决原先传参 `active-trace-id=""` 写死且用 `trace_id` 判定导致无法感知当前会话的 Bug，新增 `activeConversationId` 属性，与当前 `conversationId` 严格对齐，激活态增加左侧竖向强调色条、卡片浅背景微高亮与 Ring 投影；⑨ **搜索 300ms 防抖与一键清空 (P1)**：搜索输入增加 300ms Debounce 防抖机制，杜绝连续按键频繁触发 API 请求，并新增一键快速清空 `✕` 按钮；⑩ **清理视觉杂质与卡片信息重构 (P1)**：彻底移除原右下角杂乱且侵占操作空间的旋转彩圈数字水印，卡片整体回归克制现代风格；新增单条卡片悬浮快捷操作——**单条会话删除**（带卡片内联防误触确认气泡，并联动后端 `batch-delete` 接口删除）、**一键导出 Markdown 对话记录**（调用已有 `downloadMarkdownFile` 工具快速保存）与**回溯日志**；⑪ **时间分组折叠面板 (Accordion) 与微光骨架屏动效 (P2)**：分组 Header 支持点击展开/折叠，默认折叠更早时间段，减轻长列表滚动疲劳；首屏加载替换为 4 组带有脉冲微光的卡片骨架屏（Shimmer Skeleton），消除白屏闪烁感；⑫ **质量与类型验证**：`npx vue-tsc --noEmit` 0 报错通过；严格遵守执行边界，未编译前端构建或代跑 `./dev.sh` 脚本。 | ✅ `vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-16 |
| 大模型调用明细指标多厂商 Prompt Cache 缓存字段归一化解析与未缓存/缓存读取输入构成拆分展示 (Model Call Stats Prompt Caching Multi-Vendor Normalization & Uncached vs Cache-Read Breakdown) | `app/services/ai/runtime/agentscope/middleware.py`, `app/api/v1/endpoints/chat.py`, `frontend/src/components/chat/ChatModelCallStatsModal.vue`, `tests/services/ai/test_agentscope_middleware_usage_details.py`, `tests/CHECKLIST.md` | **大模型调用明细指标多厂商上下文缓存解析与未缓存/缓存读取构成精细化拆分闭环**：① **多厂商 Usage 缓存字段归一化解析**：在 `middleware.py` 的 `_extract_usage_details` 中引入 `_resolve` 安全解析 helper，兼容 dict 与属性对象两种形态；补全了 OpenAI 规范（`prompt_tokens_details.cached_tokens`）、Anthropic 规范（`input_token_details.cache_read`）、聚合中转网关（顶层 `cached_tokens`）以及 AgentScope 内部原生缓存字段的瀑布流优先级提取，杜绝缓存命中漏记为 0；② **输入构成精细化与向后兼容**：在非流式与流式两条统计记录中新增 `uncached_input_tokens = max(0, in_tokens - cache_tokens)`，同时保留 `input_tokens` 为 gross 总输入口径；在 `chat.py` 的 `ModelCallStatDetail` 中定义为 `Optional[int] = Field(None, ...)`，确保老数据反序列化 100% 向后兼容；③ **前端指标面板与明细构成升级**：在 `ChatModelCallStatsModal.vue` 中新增「未缓存输入」与「缓存读取」两栏结构化卡片与命中率精准算法（`缓存 / (未缓存 + 缓存)`），并在单轮明细中对缓存命中条目拆分展示 `缓存 XXX` 与 `未缓存 YYY` 独立轻量徽标；编写 `uncachedTokens` 自动对老数据执行 `gross - cache` 兼容兜底；④ **测试与质量核验**：新增 `test_agentscope_middleware_usage_details.py` 覆盖 11 项多厂商解析与非流式单测（全绿）；`vue-tsc --noEmit` 0 报错；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 后端 11 项单测全绿；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-16 |
| AI 内容底部操作栏重排与视觉体验升级：点赞点踩前移至重新生成后、复制/重试纯图标化、继续分析按钮移动端纯图标响应式紧凑化、全局替换为向下方弹出深色磨砂自定义 Tooltip 气泡（防遮挡上方操作）、按钮尺寸与 SVG 图标规范化统一（h-7 w-7 与 w-3.5 h-3.5 1.75 描边）、时间戳添加 whitespace-nowrap 防移动端折行、Token 用量图二数据库图标风格 (Message Action Bar Reorder & Aesthetic Upgrade: Move Feedback Behind Regenerate, Icon-Only Copy/Regenerate, Responsive Mobile-Icon Continue Analysis, Bottom-Popping Custom Dark Backdrop-Blur Tooltips, Unified Icon Button Specs, Timestamp Non-Wrapping & Database Cylinder Token Usage) | `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `frontend/src/components/chat/MessageActionMenus.vue`, `frontend/src/components/chat/MessageContinueAnalysis.vue`, `frontend/src/components/chatbi/ChatBIContinueAnalysis.vue`, `frontend/src/utils/tokenFormat.ts`, `tests/frontend/test_message_token_usage_contract.py`, `tests/CHECKLIST.md` | **消息底部操作栏布局重排、纯图标化、尺寸归一、移动端时间防折行、继续分析响应式纯图标与防遮挡向下自定义 Tooltip 闭环**：① **操作栏顺序重排**：将点赞、点踩按钮从操作栏右端移至【重新生成】正后方，形成紧凑的「复制 → 重新生成 → 点赞 → 点踩 → 数据/文件 → Token用量 → 业务分析 → 更多」高频聚合动线；② **纯图标化与规格归一化**：复制、重新生成全面移除文字内联标签；针对此前复制/重试（14px）与点赞/踩（16px）视觉参差不齐的问题，全部统一为 `h-7 w-7`（28px 正方形热区）与 `w-3.5 h-3.5` + `stroke-width="1.75"` 描边规范，彻底解决大大小小问题；③ **继续分析移动端纯图标化（省出 60px+ 宽度）**：`MessageContinueAnalysis.vue` 与 `ChatBIContinueAnalysis.vue` 引入高质感 Sparkles 矢量图标，在移动端（`props.isMobile` / `sm:hidden`）自动隐藏文字与箭头，仅保留 `h-7 w-7` 图标按钮，直接呼出全宽抽屉，使操作栏全量按钮在小屏单行完全展示、绝不挤压溢出；桌面端保留「图标 + 继续分析 + ⌄」完整形态；④ **移动端时间戳防折行**：为操作栏时间文本添加 `whitespace-nowrap shrink-0`，彻底杜绝小屏下「昨天 10:09」因空格被挤压折成两行撑高操作栏的问题；⑤ **高质感向下弹出自定义 Tooltip 气泡**：全量移除 4 个操作按钮上的浏览器原生 title 属性，Tooltip 调整为 `top-full mt-1.5`（朝下方平滑弹出，顶部小倒角箭头），彻底避免向上弹出遮挡上方的快捷提问药丸按钮或消息内容；⑥ **Token 用量紧凑展示**：保持数据库圆柱图标 + `用量 XX.XK tok` 纯净无框设计；⑦ **双页面对齐与自动化契约测试**：`EmbedChat.vue`、`AgentDebug.vue`、`MessageActionMenus.vue` 与继续分析组件均同步对齐；契约测试 5 项全绿；`vue-tsc --noEmit` 0 报错；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 前端契约测试 5 项全绿；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-16 |
| 移除当前模型身份前置硬编码正则直通拦截并收窄公共文档检索范围：仅在明确询问使用手册/部署排查时触发，杜绝模型测速与闲聊误搜文档 (Remove Regex Bypass for Model Identity & Narrow Platform Docs Retrieval to Explicit Manuals/Deployment Troubleshooting) | `app/services/ai/pipeline/steps/route_step.py`, `app/services/ai/intent_service.py`, `app/services/ai/tool_nudge_policy.py`, `app/services/ai/agent_prompts.py`, `tests/ai/test_request_decision.py`, `tests/ai/test_execution_observability.py`, `tests/ai/test_tool_nudge_policy.py`, `tests/CHECKLIST.md` | **前置正则直通拦截下线与公共文档检索范围精准收窄闭环**：① **直通拦截下线**：从 `route_step.py` 中彻底移除 `looks_like_current_model_query` 的直通拦截判断与提前返回逻辑，并在 `intent_service.py` 中清理 `_CURRENT_MODEL_NON_IDENTITY_MARKERS` 与 `_CURRENT_MODEL_IDENTITY_PATTERNS`，将 `looks_like_current_model_query` 改为稳定返回 `False`，确保所有关于模型评价、速度测试、日常问答的用户请求均无阻碍流入正常流水线与大模型推理；② **公共文档检索范围严格收窄**：在 `tool_nudge_policy.py` 中新增 `looks_like_explicit_platform_docs_query`，严格限制仅当用户明确询问“平台使用手册/操作手册/部署文档/Docker或K8s部署/安装步骤/报错排查/故障排查/FAQ”等明确文档或部署排查意图时，才向模型注入 `platform_docs_nudge`（Grep 公共文档强引导）；硬性排除运行时状态（当前模型、模型速度、会话状态、用户信息）与普通闲聊；③ **提示词与工具指引对齐**：在 `agent_prompts.py` 中收窄“平台使用手册与部署排查”守则，明确运行时状态与闲聊不得检索公共文档，并在 `_PLATFORM_TOOL_ONE_LINERS` 中强约束“询问当前/本轮使用的模型时直接调用 get_current_model 工具，严禁检索文档”；④ **测试与质量验证**：在 `test_tool_nudge_policy.py` 中新增明确文档查询触发 Grep nudge 与模型/闲聊绝不触发文档 nudge 的双重断言；全量单测用例与断言已更新，`py_compile` 语法校验 0 错误；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 语法编译通过；测试断言已更新覆盖；未代跑真实服务与启动脚本 | 2026-09-16 |
| Agent Debug 执行详情抽屉整合执行步骤/组装Prompt/运行时上下文、组装Prompt一键复制/折叠与token估算及平台帮助FAQ指引精简（ExecutionDebugDrawer 三标签观察台、raw_prompt 调试事件补齐 assemble 全量 system_prompt、Prompt 消息按中文×0.8+其余×0.25 口径 token 估算、全局守则删除重复跨会话注入细节） (Agent Debug Execution Detail Drawer, Assembled Prompt Copy/Collapse & Token Estimate, FAQ Prompt Slimming) | `frontend/src/components/chat/ExecutionDebugDrawer.vue`, `frontend/src/views/AgentDebug.vue`, `app/services/ai/pipeline/steps/assemble_step.py`, `app/services/ai/agent_prompts.py`, `tests/CHECKLIST.md` | **智能体调试与组装 Prompt 观测闭环**：① **执行详情抽屉（ExecutionDebugDrawer）**：新增右侧抽屉观察台，三个标签页——执行步骤（按 trace_id 拉取 `/chat/logs`，折叠展示完整 `tool_input`/`tool_output` JSON，识别截断标记与 raw 解包）、组装 Prompt、运行时上下文（`agentContext` 快照）；AgentDebug 消息头按钮收敛为「执行详情」单一入口，移除与抽屉功能重复的【完整日志】与【Prompt】快捷按钮（TraceLogViewer 完整日志仍保留在 turn 步骤区/历史跳转入口）；② **后端 assemble 事件补齐全量系统提示词**：在 `assemble_step.py` 的 `raw_prompt` debug 事件中新增 `system_prompt` 字段，暴露组装完成的全量系统级提示词（平台全局守则 + Agent 基础指令 + 技能/能力/安全边界 + 历史边界），前端渲染为组装 Prompt 首条 System 消息；③ **组装 Prompt 一键复制/折叠/token 估算**：每条组装 Prompt 消息头部新增一键复制按钮（成功后短暂显示『已复制』）；消息支持折叠，默认展开首条全量系统提示词，点击标题栏或收起态正文切换；每条消息展示 `≈N tok` token 估算，复用 PromptStudio 相同口径（中文×0.8+其余×0.25）；④ **平台帮助与 FAQ 指引精简**：将全局守则中冗长的公共文档路径/防盲猜/兜底说明压缩为一句，避免每轮重复注入，所需能力已由 `_PLATFORM_FILE_PATH_ANTI_BLIND_GUESS_RULES`（绑定文件工具即注入）完整覆盖，不丢失行为，保留 `data/docs` 检索、`list_accessible_directories` 兜底与 FAQ 链接；⑤ **质量验证**：前端契约测试 11 项全绿、`vue-tsc --noEmit` 0 报错，已核对死代码清理；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 前端契约 11 项全绿；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-15 |
| AgentDebug 调试页顶部模式选择栏/清空/运行逻辑清理、消息头部完整日志/Prompt按钮瘦身、沙箱打通与会话接口装饰器/TDZ 修复（抽离 `useSandboxWorkspace` 跨页面复用组合式函数，修复 `loadGreeting` 声明提升顺序彻底消除白屏，纠正后端 `/conversation/{id}` 装饰器错位彻底解决 400 Bad Request） (AgentDebug Cleanup, Redundant Header Logs/Prompt Buttons Removal, Shared `useSandboxWorkspace` Composable, TDZ ReferenceError Fix & Backend Conversation Router Decorator Alignment) | `frontend/src/composables/chat/useSandboxWorkspace.ts`, `frontend/src/views/AgentDebug.vue`, `frontend/src/views/EmbedChat.vue`, `app/api/v1/endpoints/chat.py`, `tests/api/v1/test_active_conversation.py`, `tests/frontend/test_chat_surface_refactor_contract.py`, `tests/CHECKLIST.md` | **智能体调试页顶层与消息头冗余交互清理、沙箱运行环境打通与前后端连环白屏/400 缺陷根治闭环**：① **清理消息头冗余按钮**：移除 Agent 消息上方与【执行详情】功能重复的【完整日志】和【Prompt】两个冗余操作按钮，精简信息流视觉干扰；② **修复后端会话历史接口装饰器错位**：在 `app/api/v1/endpoints/chat.py` 中，`@router.get("/conversation/{conversation_id}")` 装饰器此前误贴在内部辅助函数 `_merge_latest_audit_assistant` 之上，导致 FastAPI 参数校验将其两个参数作为必填项，请求直接报 `400 Bad Request`，现将辅助函数前置，装饰器重新正确包络 `get_conversation_history`，恢复 200 正常响应；③ **修复前端 `loadGreeting` TDZ 暂存死区导致页面崩溃白屏**：`AgentDebug.vue` 内部 `loadGreeting` 与 `resetDebugThinkingOverrides` 声明在第 1500+ 行，但在上方 `loadSessionHistory` 捕获异常（如上述 400）时被提前调用，触发 JavaScript `ReferenceError: Cannot access 'loadGreeting' before initialization` 导致组件销毁白屏；现将其移至 550 行（所有调用点之前），彻底消除 TDZ 崩溃；④ **清理顶部冗余模式切换栏与操作按钮**：移除 AgentDebug 顶部「🤖 智能委派 / 🎯 指定智能体」卡片式选择栏与右侧【清空】/【运行逻辑】按钮，收敛并复用底部输入框 `ChatInput` 现有的专家/智能委派胶囊选择器；⑤ **抽取跨页面复用 `useSandboxWorkspace` 组合式函数**：将此前仅在 `EmbedChat.vue` 中内联实现的 350+ 行沙箱生命周期与终端弹窗逻辑抽取为通用 Composable，彻底打通 AgentDebug 沙箱操作与 Docker/K8s 终端；⑥ **全链路质量与测试验证**：`npx vue-tsc --noEmit` 0 报错；`tests/api/v1/test_active_conversation.py` 新增接口 200 路由契约单测通过；前端契约测试 3 项全绿；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 后端路由测试全绿；`vue-tsc --noEmit` 0 报错；前端契约测试全绿；未代跑真实服务与启动脚本 | 2026-09-15 |
| Docker & K8s 双沙箱脚本终端 UI/UX 颜色编码与排版美化升级（自适应 TTY 与 NO_COLOR、双线艺术横幅 BANNER、属性面板/镜像表格格式化高亮、Dockerfile 语法微高亮、成功双线卡片与金色交互确认） (Sandbox CLI Color Coding & Layout Aesthetic Upgrade: Adaptive TTY & NO_COLOR, Double-line Banners, Formatted Cards & Golden Prompts) | `sandbox/docker/prebuild_docker_sandbox.py`, `sandbox/docker/build-docker-sandbox-image.sh`, `sandbox/k8s/build-k8s-sandbox-image.sh`, `k8s_deploy/build-k8s-sandbox-image.sh`, `tests/CHECKLIST.md` | **双沙箱构建脚本全流程现代化 CLI 颜色编码与结构化视觉排版闭环**：① **自适应 TTY 与色彩控制规范**：Python 与 Bash 端均严格遵从 `isatty()` 与标准环境变量 `NO_COLOR` / `FORCE_COLOR`，在非交互管道与重定向文件输出时自动降级回退为纯文本，防止单测与管道解析被 ANSI 转义符污染；② **高质感终端视觉排版**：全面升级 Docker 与 K8s 脚本的艺术字横幅 BANNER、双线边框卡片（Cyan/Green/Blue）、属性面板键值对着色（暗灰键名、亮白/青色数值）；③ **镜像表格与演练卡片**：`--list` 命令输出结构化表头与镜像 ID/Tag/大小/创建时间列对齐，Tag 自动按 `latest`/哈希分类着色；`--dry-run` 模式提供 Dockerfile FROM/RUN/COPY/WORKDIR 等指令的语法微高亮；④ **交互确认与成功引导卡片**：无参直接回车时，提供金色粗体 `[y/N]` 交互确认提示防误触；构建完成输出亮绿三步配置指引双线卡片；⑤ **自动化测试验证**：`test_docker_prebuild_cli.py` 与 `test_k8s_deploy_docs_contract.py` 全绿通过，语法检查无报错；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 自动化测试全绿；bash -n 语法通过；演练与帮助界面实测通过；未代跑真实服务与启动脚本 | 2026-09-15 |
| Docker & K8s 双沙箱脚本统归集至 sandbox/ 目录（docker/k8s）、新增 Docker 前置环境预检与场景化安装/排障引导、构建模板动态注入排障工具链（git/tree/ping/netstat/ps/telnet）开箱即用 + 全面对齐 --dry-run/--list/-y 交互防误触 (Sandbox Ops Reorganized to sandbox/docker & sandbox/k8s with Pre-flight Docker Check & Scenario-based Guidance) | `sandbox/docker/build-docker-sandbox-image.sh`, `sandbox/docker/prebuild_docker_sandbox.py`, `sandbox/docker/README.md`, `sandbox/k8s/build-k8s-sandbox-image.sh`, `sandbox/k8s/README.md`, `k8s_deploy/build-k8s-sandbox-image.sh`, `scripts/prebuild_docker_sandbox.py`, `app/services/ai/runtime/agentscope/docker_template_patch.py`, `app/services/ai/runtime/agentscope/docker_prebuild.py`, `app/services/ai/runtime/agentscope/docker_workspace.py`, `app/main.py`, `tests/ai/runtime/test_docker_template_patch.py`, `tests/ai/runtime/test_docker_prebuild_cli.py`, `tests/CHECKLIST.md` | **代码沙箱运维体系全域规范化：双沙箱目录（`sandbox/docker` 与 `sandbox/k8s`）统一归集、Docker 前置环境预检与场景化引导、排障工具链开箱即用与 CLI 体验双向对齐全链路闭环**：① **运维脚本归集与重命名规范**：新建 `sandbox/` 统一顶层目录，Docker 沙箱脚本归集重命名为 `sandbox/docker/build-docker-sandbox-image.sh`，与 K8s 沙箱脚本 `sandbox/k8s/build-k8s-sandbox-image.sh` 全面对齐；② **Docker 前置环境预检与场景化引导**：两套沙箱构建脚本均加入 `check_docker_environment` 预检机制——执行前区分检测“未安装 Docker CLI”、“Docker 守护进程未启动”与“当前非 root 用户无权限”，并分别给出精确的一键安装命令、服务启动命令（`systemctl start docker` / Docker Desktop）、加组赋权命令（`usermod -aG docker`）以及 K8s 纯 containerd 节点的外部构建机打包替代方案；演练（`--dry-run`）与帮助模式自动放行；Python 侧捕获 `aiodocker` 缺失并给出环境激活提示；③ **排障工具链开箱即用**：动态注入 `git`、`tree`、`iputils-ping`、`net-tools`、`procps`、`telnet`，镜像 Tag 自动随 Dockerfile 内容哈希对齐；④ **新增 `--dry-run` 与 `--list`**：演练预览 Dockerfile 上下文，扫描本地 Docker / containerd 已构建镜像；⑤ **全量测试验证**：`test_docker_prebuild_cli.py` 2 项测试全绿，环境预检模拟与演练命令实测通过；未代跑真实服务与启动脚本。 | ✅ 11 项沙箱与 CLI 测试全绿；环境预检与 `--dry-run` 模拟实测通过；未代跑真实服务与启动脚本 | 2026-09-15 |
| K8s 沙箱预置镜像补齐常用排障工具链（tree/telnet/netstat等）+ 脚本无参数安全引导与交互确认 + 新增 `--list` 双层镜像探测与前端指引文案双向对齐 (K8s Sandbox Prebuilt Image Toolchain, Safe Interactive CLI, `--list` Image Inspector & SystemConfig Copy Alignment) | `k8s_deploy/build-k8s-sandbox-image.sh`, `k8s_deploy/README.md`, `frontend/src/views/SystemConfig.vue`, `tests/CHECKLIST.md` | **K8s 沙箱预置镜像基础排障能力扩充、构建防误触交互引导与双层镜像探测及平台配置联动优化闭环**：① **预置常用网络与排障工具链**：在 `build-k8s-sandbox-image.sh` 生成的 Dockerfile 基础镜像中，除原有 AgentScope 网关依赖外，补充安装 `tree`（目录树）、`telnet`（端口连通探测）、`net-tools`（`netstat`/`ifconfig`）、`iputils-ping`（`ping`）、`dnsutils`（`dig`/`nslookup`）、`iproute2`（`ip` 现代路由工具）、`procps`（`ps`/`top`/`kill`）、`git`、`jq`、`unzip`、`wget`、`less` 等常用系统工具，解决沙箱内执行命令与排查网络时常见报 `command not found` 的问题；② **脚本防误触交互引导与免确认机制**：重构脚本参数解析——无参数直接敲回车时不再直接静默跑构建，而是先打印完整 `--help` 帮助与配置概览，在交互终端提示 `💡 未指定参数，是否以默认配置立即开始构建？[y/N]`，回车或输入 n 退出防误触，输入 y 启动；新增 `-y`/`--yes` 支持免交互脚本调用；③ **新增 `--list` / `-l` 镜像探测与状态核验**：脚本支持 `--list`，自动双层探测本地 Docker daemon（`docker images`）与 K8s 节点底层容器运行时（K3s containerd、系统 containerd、crictl），展示已就绪的沙箱镜像清单，并重构自动导入调用支持普通用户下探测 sudo 提升导入成功率；④ **构建产物与平台 Web 控制台双向打通指引**：脚本执行前与构建完成（及 `--dry-run`）后均给出明确指引，精确告知前往平台【系统设置】→【参数配置】→【沙箱配置】→ `sandbox_k8s_image` 填入镜像名称并点击右上角【保存变更 (⌘S)】保存生效；⑤ **前端界面文案与按钮语义优化**：在 `SystemConfig.vue` 中将自定义镜像旁的 `构建镜像` 按钮调整为 `如何构建镜像？`（避免用户误认为点击直接触发后台构建），并在提示卡片与指引弹窗中同步补充内置排障工具说明、`--list` 核验与 `-y` 免交互命令以及保存变更指引；⑥ **验证**：`build-k8s-sandbox-image.sh --list`、`--dry-run` 与无参调用验证通过；前端 `npx vue-tsc --noEmit` 静态类型检查 0 报错通过；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 脚本 `--list`、`--dry-run` 与交互保护实测通过；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-15 |
| 同命名空间下平台/沙箱展示划分：平台视角一律正向标签过滤 + events 合并去重（Platform vs Sandbox Views Split by Positive Label Selectors + Merged Event View） | `k8s_deploy/nanzi-k8s.sh`, `k8s_deploy/README.md`, `tests/test_k8s_deploy_docs_contract.py`, `tests/CHECKLIST.md` | **彻底消除同命名空间导致的平台/沙箱互相混显**：① **现象**：沙箱默认与平台同命名空间后，`status` 第 3 节「NanZi 平台应用资源」会同时列出平台 Deployment Pod（`nanzi-ai-agent-xxxx-yyyy`）与沙箱 Pod（`as-ws-admin--1`）；`events` 命令更会因两个命名空间变量相等而把同一份事件打印两次（"NanZi 平台事件" + "沙箱执行事件"）；② **选择器策略（关键考据）**：改为一律使用**正向标签选择器**，避免依赖 `key!=value` 对"无该标签对象"的匹配语义——先核实 `deployment.yaml`/`service.yaml`/`ingress.example.yaml`/`pvc.yaml` 四类平台资源**自身都带** `app.kubernetes.io/name=nanzi-ai-agent`，故平台视角用 `PLATFORM_APP_LABEL`、沙箱视角用 `SANDBOX_LABEL`（`app.kubernetes.io/managed-by=agentscope`），两侧对称且语义无歧义；③ **平台侧过滤覆盖 5 处**：`status` 第 3 节（`pod,svc,ingress`，标题标注「不含沙箱」）、`restart-pod` / `restart-pod-force` / `restart-all` 三处"最新 Pod 运行状态"列表、`health` 第 3 节平台 Pod 三项检查（未 Running 判定 / Running 计数 / CrashLoopBackOff 探测，标题标注「不含沙箱」、文案改为"平台 Pod"并指向第 5 节）；`restart-k3s` 的 `get pods -A`（全集群汇总）保持不过滤；④ **events 去重**：新增 `[ "$SANDBOX_NAMESPACE" = "$NAMESPACE" ]` 分支——同命名空间时合并为单个「命名空间事件」小节（事件不支持按标签过滤），仅在沙箱独立命名空间的场景才分别打印两个小节；⑤ **共享卷退化路径同步**：`print_shared_workspace_volume()` 的兜底列举由互补标签改为同一 `PLATFORM_APP_LABEL`（平台 PVC 带该标签），脚本内不再出现任何 `!=` 选择器；⑥ **文档**：`status` 示例输出第 3 节标题同步，并在 README 增加「沙箱与平台同命名空间时的输出划分」说明（平台视角 / 沙箱视角 / events 合并三段）；⑦ **验证**：`bash -n` 通过；假 `kubectl` 端到端跑 `status`/`sandboxes`/`health`/`events` 四条路径并核对调用日志——平台侧 4 类调用全部带 `name=nanzi-ai-agent`、沙箱侧全部带 `managed-by=agentscope`、**不存在任何无过滤的 Pod 列举**；新增 `test_k8s_ops_script_excludes_sandbox_pods_from_platform_views` 与 `test_k8s_ops_script_merges_events_when_namespace_shared` 两项契约测试，连同既有契约共 50 项全绿；严格遵守执行边界，未在真实集群执行脚本、未代跑真实服务与启动脚本。 | ✅ `bash -n` 语法通过；假 `kubectl` 验证四条路径 + 调用日志核对；文档契约 + K8s 沙箱测试 50 项全绿；未在真实集群执行脚本、未代跑真实服务与启动脚本 | 2026-09-14 |
| 沙箱监控补显平台共享数据卷（过滤后仍可看到沙箱复用的那块 PVC）并加契约测试（Surface Shared Platform Data Volume in Sandbox Ops Output + Contract Test） | `k8s_deploy/nanzi-k8s.sh`, `k8s_deploy/README.md`, `tests/test_k8s_deploy_docs_contract.py`, `tests/CHECKLIST.md` | **过滤显示后补齐"沙箱用的是哪块盘"的信息可见性**：① **需求**：上一步按 AgentScope 标签过滤后，`sandboxes` 只列沙箱自有资源，而共享模式下沙箱并无独立 PVC，运维反而看不到实际承载用户工作区的平台数据卷（`nanzi-ai-agent-data`），无从确认容量/状态/存储类；② **实现**：新增常量 `PLATFORM_APP_LABEL="app.kubernetes.io/name=${DEPLOYMENT}"`（**刻意定义在 `DEPLOYMENT` 之后**，避免 `set -u` 下先引用未定义变量导致脚本中断）与复用型函数 `print_shared_workspace_volume()`——以 `kubectl get pod -l "$PLATFORM_APP_LABEL" -o jsonpath` 定位平台 Pod 的 `spec.volumes[*].persistentVolumeClaim.claimName`（即挂载在 `/app/data` 的那块卷），再按 claim 名输出该 PVC 的 `-o wide` 行并附一条 `subPath: agent_workspaces/{user_key}` 复用说明；③ **三级降级（任一失败都不中断脚本）**：识别到 claim → 展示该 PVC 行；识别不到但命名空间存在平台 PVC → 退化为按同一 `PLATFORM_APP_LABEL` 列出这些 PVC 供参考；两者皆无 → 输出"平台可能未以 PVC 方式提供数据目录，或为非默认部署"的说明；④ **接入点**：`status` 第 4 节（沙箱工作区资源）与 `sandboxes` 子命令新增的「🗄 平台共享数据卷（沙箱通过 subPath 复用）」小节均调用该 helper，`sandboxes` 帮助行改为「监控沙箱 Pod、沙箱独立 PVC 与共享的平台数据卷（不混入平台自身 Deployment Pod）」，`k8s_deploy/README.md` 的帮助清单、`status` 示例输出与命令说明同步；⑤ **验证方式（不碰真实集群）**：用记录调用参数并返回可控输出的假 `kubectl` 跑通三条路径——`status` 第 4 节、`sandboxes` 共享卷小节、以及 `sh nanzi-k8s.sh`（验证 `sh` → bash 兼容层下 `set -u` 无隐患），并逐一验证三种降级分支的输出；⑥ **契约测试**：新增 `test_k8s_ops_script_surfaces_shared_platform_data_volume`，断言平台标签常量存在且定义顺序在 `DEPLOYMENT` 之后、helper 已定义并在两处调用、平台标签退化路径存在、"共享数据卷"与小节标题存在；连同既有过滤契约共 48 项全绿；严格遵守执行边界，未在真实集群执行脚本、未代跑真实服务与启动脚本。 | ✅ `bash -n` 语法通过；假 `kubectl` 端到端验证三条路径与三种降级分支；文档契约 + K8s 沙箱测试 48 项全绿；未在真实集群执行脚本、未代跑真实服务与启动脚本 | 2026-09-14 |
| 沙箱运维脚本按 AgentScope 标签过滤显示（避免同命名空间下混入平台自身 Pod/PVC）并加契约测试（Filter Sandbox Ops Listing by AgentScope Label + Contract Test） | `k8s_deploy/nanzi-k8s.sh`, `k8s_deploy/README.md`, `tests/test_k8s_deploy_docs_contract.py`, `tests/CHECKLIST.md` | **修复「沙箱命名空间对齐平台」带来的监控显示副作用**：① **现象**：`nanzi-k8s.sh sandboxes` 在 `Namespace: nanzi-ai-agent` 下同时列出沙箱 Pod `as-ws-admin--1` 与平台自身后端 Pod `nanzi-ai-agent-6d85d695bf-wp9qh`（Deployment→ReplicaSet 哈希→随机后缀），前者被误认为"多出来的沙箱"；根因是沙箱命名空间默认与平台同命名空间（共享平台 PVC 的前置条件，PVC 为命名空间级资源），而脚本原先按命名空间整表列举 Pod/PVC，不再具备区分度；② **过滤实现**：新增常量 `SANDBOX_LABEL="app.kubernetes.io/managed-by=agentscope"`（沙箱 Pod 由适配器与 AgentScope 原生路径均写入该标签，沙箱动态 PVC 亦然），并在三处列举点统一加 `-l "$SANDBOX_LABEL"`：`status` 第 4 节（`pod,pvc` 计数与展示）、`sandboxes` 子命令（Pod 与"沙箱独立 PVC"两段，并在无独立 PVC 时提示"共享模式复用平台数据卷属正常"、附一行说明平台自身资源请见 `status`）、`health` 第 5 节（沙箱 Pod 计数/异常 Pod/未 Bound PVC 判定），避免把平台 Pod 计入沙箱健康度；③ **文案**：`status` 与 `health` 标题标注「仅 AgentScope 托管资源」，`sandboxes` 帮助行改为「监控沙箱 Pod 与沙箱独立 PVC（仅 AgentScope 托管资源，不混入平台自身 Pod/PVC）」，`k8s_deploy/README.md` 的示例输出与帮助清单同步；④ **契约测试**：新增 `test_k8s_ops_script_filters_sandbox_listing_by_agentscope_label`，断言标签常量存在、`"$SANDBOX_LABEL"` 出现次数 ≥ 8，且六种不带过滤的裸列举写法（`kubectl get pod/pvc/pod,pvc -n "$SANDBOX_NAMESPACE"` 与 `health_get get pods/pvc -n "$SANDBOX_NAMESPACE" --no-headers`）均不存在；⑤ **验证**：`bash -n k8s_deploy/nanzi-k8s.sh` 语法通过，文档契约与 K8s 沙箱测试共 47 项全绿；严格遵守执行边界，未在真实集群执行脚本、未代跑真实服务与启动脚本。 | ✅ `bash -n` 语法通过；文档契约 + K8s 沙箱测试 47 项全绿；未在真实集群执行脚本、未代跑真实服务与启动脚本 | 2026-09-14 |
| 修复 K8s 沙箱挂载说明漏改的第二处文案（引导卡片）并新增前端契约测试防漂移（Fix Stale K8s Workspace-Mount Guidance Card & Add Anti-Drift Frontend Contract Test） | `frontend/src/views/SystemConfig.vue`, `tests/frontend/test_sandbox_policy_icons_contract.py`, `tests/CHECKLIST.md` | **同一语义变更存在两处独立文案区块，此前只改了其一**：① **问题定位**：`sandbox_k8s_existing_pvc` 的说明在前端有**两个互不相干的区块**——`configLongDescriptions` 里的 `【参数作用】` 长说明，以及配置项下方的 `💡 参数作用与是否必填 / 📂 留空（默认处理）/ 🔗 填写的格式 / 🎯 最终映射路径` 引导卡片；前次变更只更新了前者，引导卡片仍残留三处过期内容：subPath 仍写 `agent_workspaces/{user_key}/sandbox`（实际已改为整个工作区根 `agent_workspaces/{user_key}`）、公共文档仍写挂载至 `/workspace/docs`（实际为 `/workspace/public/docs`）、留空语义仍写「走独立临时 PVC 模式」（实际已改为「自动探测共享平台数据卷」）；② **连带修正**：`sandbox_k8s_storage_class` 的长说明与引导卡片的「生效前提」原先写「仅在上方 existing_pvc 留空时生效」，在新语义下留空=共享、不再是动态卷模式，改为「仅在回退为动态独立卷模式（填了 `none`，或留空但自动探测失败）时生效」；`sandbox_k8s_delete_pvc_on_close` 短说明补充「仅对动态独立卷生效，共享的平台/已有 PVC 受保护绝不删除」；输入框 placeholder 由 `例如 nanzi-app-data（可选，留空为独立临时卷）` 改为「留空自动共享平台数据卷；填 none 强制独立临时卷；也可填具体 PVC 名」，并移除 `nanzi-app-data`/`agent-shared-pvc` 这类会把部署变量误当固定卷名的示例；③ **防漂移契约测试**：在 `tests/frontend/test_sandbox_policy_icons_contract.py` 新增 `test_k8s_workspace_mount_guidance_reflects_current_behaviour`，硬性断言 `subPath: agent_workspaces/{user_key}</code>` 存在且旧 `/sandbox` 子目录写法不存在、`/workspace/public/docs` 存在且 `/workspace/docs` 不存在、`自动探测共享模式` 与 `none` 存在且旧「留空为独立临时卷」「留空（即动态创建独立卷模式）时生效」不存在、示例卷名 `nanzi-app-data`/`agent-shared-pvc` 不存在，并要求两处文案块均出现「完整工作区」（`count >= 2`），从机制上防止两处文案再次只改其一；④ **验证**：新增契约测试连同前端沙箱契约共 13 项全绿，K8s 沙箱 38 项与文档契约 8 项保持全绿，`vue-tsc --noEmit` 零报错；另经 `git stash` 对照确认前端全量套件中的 44 项失败在改动前既已存在（属既有环境/历史问题，与本改动无关）；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 前端沙箱契约 13 项 + K8s 沙箱 38 项 + 文档契约 8 项全绿；`vue-tsc --noEmit` 零报错；已用 stash 对照证明其余 44 项失败为既有问题；未代跑真实服务与启动脚本 | 2026-09-14 |
| K8s 沙箱共享数据卷零配置：自动探测平台自身 PVC（不再手填/硬编码卷名）+ `none` 哨兵值强制隔离（K8s Sandbox Zero-Config Shared PVC via Platform Self-Detection & Explicit Isolation Sentinel） | `app/services/ai/runtime/agentscope/k8s_workspace.py`, `app/services/ai/runtime/agentscope/workspace.py`, `db-prod/V157-update-sandbox-k8s-existing-pvc-desc.sql`, `db-prod-pg/V58-update-sandbox-k8s-existing-pvc-desc.sql`, `frontend/src/views/SystemConfig.vue`, `k8s_deploy/README.md`, `k8s_deploy/upgrade.md`, `FAQ.md`, `tests/services/test_sandbox_policy_k8s.py`, `tests/CHECKLIST.md` | **让 K8s 沙箱「开箱即共享用户工作区」且不硬编码部署细节**：① **问题**：此前需管理员手工把 `sandbox_k8s_existing_pvc` 填成平台主 PVC 名，留空则静默退化为独立空卷（沙箱内看不到用户工作区）；而把 `nanzi-ai-agent-data` 直接硬编码进代码同样不可取——PVC 名是可被 kustomization/已有 PVC 覆盖的部署变量，硬编码会让自定义部署的沙箱 Pod 因引用不存在的 PVC 而永久 Pending，并与 `deployment.yaml` 形成隐式耦合；② **自动探测**：新增 `detect_platform_data_pvc()`——以 `socket.gethostname()`（K8s 内默认等于 Pod 名）配合 `resolve_platform_namespace()` 定位平台自身 Pod，调用 `read_namespaced_pod` 读取自身 spec，再用纯函数 `_claim_name_for_data_mount()` 定位挂载平台数据目录（`/app/data`）的 volume 及其 `persistentVolumeClaim.claimName`；结果按 300s TTL 缓存，任何失败（未装 SDK、无集群凭据、数据目录非 PVC、API 异常、畸形 spec）均安全返回 `None` 且永不抛异常，回退独立空卷；③ **配置语义三态**：新增 `parse_existing_pvc_config()` 与 `resolve_shared_pvc()`——留空=自动探测共享（`auto`，零配置、兼容自定义卷名）、`none`/`disabled`/`off`/`false`/`-`=强制独立空卷（`isolated`）、其它值=显式指定共享卷（`configured`）、探测失败=回退（`unavailable`）；④ **告警精确化**：`evaluate_k8s_workspace_mount_config()` 改为基于解析结果判定——仅对「探测失败回退」与「命名空间不一致却配置共享卷」告警，管理员显式 `none` 隔离不再告警；`check_k8s_rbac_status` 响应新增 `workspace_mount`（实际生效 claim 名与来源）便于排障；⑤ **安全守卫保持**：显式配置共享 PVC 而缺少已认证用户身份时仍抛 `ValueError`（禁止越权挂载持久卷），无身份的管理员连通性测试路径则不挂共享卷、照常可用；⑥ **迁移与文档**：新增 `V157`（MySQL）/`V58`（PG）仅更新 `sandbox_k8s_existing_pvc` 说明文案（不改动 value）；前端长/短说明与 `k8s_deploy/README.md`、`upgrade.md`、`FAQ.md` 同步为「留空即自动共享、`none` 为强隔离」；⑦ **测试覆盖**：新增 5 项单测（配置三态解析、Pod spec→PVC 提取含 emptyDir/缺挂载/畸形输入、四种来源解析、告警新增与取消场景、策略层零配置自动共享与哨兵值跳过探测），`tests/services/test_sandbox_policy_k8s.py` 38 项 + 文档契约 8 项 + 迁移守卫 49 项全绿，相邻 91 项后端测试与 13 项前端契约测试全绿，`vue-tsc --noEmit` 零报错；严格遵守执行边界，未代跑真实服务、迁移脚本与启动脚本。 | ✅ 38 项 K8s 沙箱测试 + 8 项文档契约 + 49 项迁移守卫 + 91 项相邻后端 + 13 项前端契约全绿；`vue-tsc --noEmit` 零报错；未代跑真实服务、迁移脚本与启动脚本 | 2026-09-14 |
| K8s 沙箱工作区挂载对齐 Docker（subPath 挂整个用户工作区根）+ 命名空间默认对齐平台并新增挂载配置校验告警（K8s Sandbox Workspace Mount Aligned with Docker, Namespace Default Aligned to Platform Namespace & Mount Config Warnings） | `app/services/ai/runtime/agentscope/k8s_workspace.py`, `app/services/ai/runtime/agentscope/workspace.py`, `k8s_deploy/sandbox-rbac.example.yaml`, `k8s_deploy/deployment.yaml`, `k8s_deploy/nanzi-k8s.sh`, `k8s_deploy/README.md`, `k8s_deploy/upgrade.md`, `FAQ.md`, `db-prod/V159-align-sandbox-k8s-namespace.sql`, `db-prod-pg/V60-align-sandbox-k8s-namespace.sql`, `frontend/src/views/SystemConfig.vue`, `frontend/src/components/chat/K8sTerminalModal.vue`, `tests/services/test_sandbox_policy_k8s.py`, `tests/test_k8s_deploy_docs_contract.py`, `tests/CHECKLIST.md` | **K8s 沙箱内看不到用户工作区问题的根因定位与全链路修复**：① **根因定位**：K8s 沙箱 `/workspace` 此前仅通过 subPath 挂载 `agent_workspaces/{user_key}/sandbox` 子目录，且 `sandbox_k8s_existing_pvc` 未配置时直接退化为 AgentScope 默认的「每工作区独立空 PVC」，导致沙箱内只有 AgentScope 自建的 `data/ sessions/ skills/` 空目录，与 Docker 沙箱（bind 整个 `agent_workspaces/{user_key}`）行为不一致；② **挂载范围对齐 Docker**：`_create_pod` 的 `user_subpath` 改为 `agent_workspaces/{user_key}`（整个用户工作区根），`_ensure_local_subpath_dirs` 同步预建该目录，使沙箱内 Bash/read/write 可查看并操作用户完整工作区（sessions/docs/历史落盘文件）；③ **命名空间默认对齐平台**：新增 `resolve_platform_namespace()`（环境变量 `NANZI_PLATFORM_NAMESPACE`/`POD_NAMESPACE` → in-cluster ServiceAccount namespace 文件 → `nanzi-ai-agent` 回退）与 `resolve_sandbox_namespace()`（留空或历史默认值 `agent-sandboxes` 均自动跟随平台命名空间），并替换后端 5 处硬编码默认值——因 Kubernetes PVC 为命名空间级资源，只有与平台同命名空间才能共享平台主 PVC；④ **配置校验与显式告警**：新增 `evaluate_k8s_workspace_mount_config()`，对「`sandbox_k8s_existing_pvc` 未配置（沙箱看不到用户工作区）」与「沙箱命名空间与平台不一致却配置了共享 PVC（Pod 将长期 Pending）」两类静默失效配置在沙箱构建时打日志告警，并在 `/admin/sandbox/k8s/check-rbac` 自检响应中新增 `warnings` 字段返回；⑤ **迁移与部署配套**：新增 MySQL `V159` / PG `V60` 迁移，仅把仍为历史默认值（或空）的 `sandbox_k8s_namespace` 对齐为 `nanzi-ai-agent`（绝不覆盖管理员显式设置）；`sandbox-rbac.example.yaml` 的 Role/RoleBinding 迁至平台命名空间并说明隔离变体；`nanzi-k8s.sh` / `deployment.yaml` / `README.md` / `upgrade.md` / `FAQ.md` / 前端配置说明与终端浮标默认命名空间同步更新；⑥ **测试覆盖**：新增 3 项单测覆盖命名空间解析（空值/历史默认值跟随平台、显式值保留、环境变量覆盖）与挂载配置告警四类场景；更新 `_create_pod` subPath 与预建目录断言；更新 K8s 文档契约断言；`tests/services/test_sandbox_policy_k8s.py` 33 项 + 文档契约 8 项全绿，相邻 91 项后端测试与 13 项前端沙箱契约测试全绿，`vue-tsc --noEmit` 零报错；严格遵守执行边界，未代跑真实服务、迁移脚本与启动脚本。 | ✅ 后端 33 项 K8s 沙箱测试 + 8 项文档契约测试 + 91 项相邻后端测试 + 13 项前端契约测试全绿；`vue-tsc --noEmit` 零报错；未代跑真实服务、迁移脚本与启动脚本 | 2026-09-14 |
| HTML 交互应用代码块优先与在线预览全链路增强（提示词强约束正文输出完整 ```html 代码块禁止擅自落盘、后端生成文件 inline 响应头解除浏览器强制下载、前端消息链接与附件抽屉拦截并在 Canvas 中实时交互运行） (HTML Interactive Application Priority & Full-Stack In-Place Canvas Preview Upgrade) | `app/services/ai/agent_prompts.py`, `architech/prompts/system_agents/generl/general_chat_v2.md`, `app/api/v1/endpoints/chat.py`, `frontend/src/components/MessageRenderer.vue`, `frontend/src/composables/chat/useWorkspaceCanvas.ts`, `frontend/src/views/EmbedChat.vue`, `tests/api/v1/test_generated_file_download.py`, `tests/ai/test_prompt_assembler.py`, `tests/frontend/test_message_renderer_contract.py`, `tests/CHECKLIST.md` | **HTML 网页生成、交互原型与文件链接在线预览体验全栈闭环体系**：① **系统提示词强约束与规范加固**：在 `agent_prompts.py` 可视化表达规范与 `general_chat_v2.md` 中增加强约束，要求 AI 在遇到编写 HTML、网页动画、交互式原型、趣味页面等需求时，**必须直接在回答正文中输出包含完整结构（`<!DOCTYPE html>`、`<html>`、`<body>` 及内联样式/脚本）的 ```html 代码块**，以便前端自动渲染为「HTML 交互应用」卡片并支持用户在 Canvas 中实时运行；严禁擅自调用写文件或发布工具将其保存为磁盘文件或提供下载链接，除非用户明确要求“保存为文件”、“下载”或“导出”；② **后端生成文件 inline 模式与防强制下载**：在 `GET /generated-files/{artifact_id}` 接口中增加 `disposition` 与 `download` 传参，针对 `text/html`、`text/plain`、`application/pdf`、`image/svg+xml` 等可直接呈现的文件类型，默认以 `content_disposition_type="inline"` 响应，彻底杜绝浏览器新标签页访问时直接弹出强制下载保存窗口，并保留 `download=true` 时切换为 `attachment`；③ **前端消息链接拦截与 Canvas 实时运行**：在 `MessageRenderer.vue` 链接点击处理中增加对 `.html` / `.htm` 链接拦截，触发 `emit('open-canvas', { type: 'html', ... })`；在 `useWorkspaceCanvas.ts` 中增强对外部/生成文件 HTTP URL 的异步文本抓取并注入 Canvas，直接以 `type: 'html'` 在 iframe 沙箱中启动交互运行，并支持一键查看源码与保存至工作区；同步在 `EmbedChat.vue` 中放开 `canPreviewFile` 与 `handlePreviewFile` 对 HTML 文件的支持；④ **多维度自动化测试覆盖**：在 `test_generated_file_download.py` 中新增 HTML 文件默认 `inline` 响应与 `download=true` 强制 `attachment` 单测（3 项全绿）；在 `test_prompt_assembler.py` 中新增系统提示词 HTML 规范注入断言（52 项全绿）；在 `test_message_renderer_contract.py` 中新增前端链接拦截与 Canvas 联动契约测试（25 项全绿）；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 后端 3 项单测全绿；提示词 52 项单测全绿；前端契约 25 项全绿；未代跑真实服务与启动脚本 | 2026-09-14 |
| PostgreSQL 迁移脚本 V48/V52 boolean 类型修复与 V25/V33 ADD COLUMN IF NOT EXISTS 幂等性加固及双重单测守卫 (PostgreSQL Migration Scripts V48/V52 Boolean Fix, V25/V33 ADD COLUMN Idempotency & Dual Regression Guards) | `db-prod-pg/V48-add_hide_login_apikey_config.sql`, `db-prod-pg/V52-add_prompt_cache_layout_configs.sql`, `db-prod-pg/V25-add-metric-tags.sql`, `db-prod-pg/V33-add-agent-version-toolcall-timeout.sql`, `tests/test_db_prod_pg_apply_sql.py`, `tests/CHECKLIST.md` | **PostgreSQL 强类型 schema 校验与迁移脚本全面幂等性加固闭环**：① **boolean 强类型修复**：PostgreSQL 严格类型校验，`system_configs.is_secret` 列为 `BOOLEAN NOT NULL DEFAULT FALSE`，修正 `V48` 与 `V52` 中误写为整型 `0` 的问题，全面纠正为标准布尔值 `FALSE`；② **ADD COLUMN 幂等性修复**：定位并修正全库仅存的两处缺少 `IF NOT EXISTS` 的 DDL 变更——`V25-add-metric-tags.sql`（`meta_metrics` 表的 `tags` 字段）与 `V33-add-agent-version-toolcall-timeout.sql`（`ai_agent_versions` 表的 `toolcall_timeout_seconds` 字段），补充 `ADD COLUMN IF NOT EXISTS`，彻底消除重复执行或断点重试时报 `column "xxx" of relation "xxx" already exists` 的问题，使 `db-prod-pg` 下全部 57 个脚本 100% 具备无损重跑与断点续跑幂等性；③ **双重自动化测试守卫**：在 `tests/test_db_prod_pg_apply_sql.py` 中新增 `test_pg_sql_files_do_not_use_integer_for_system_configs_boolean`（拦截 boolean 整型写入）与 `test_pg_sql_files_use_if_not_exists_for_add_column`（拦截缺少 IF NOT EXISTS 的 ADD COLUMN），49 项迁移单测 100% 绿灯全过；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 49 项迁移单测全绿；boolean 类型与 DDL 幂等双守卫；未代跑真实服务与启动脚本 | 2026-09-14 |
| MySQL & PostgreSQL 迁移脚本终端高颜值 UI 输出与环境校验就绪提示及交互式引导安装升级 (Database Migration Scripts Terminal UI/UX Aesthetic Upgrade: ANSI Palette, Border Cards, Environment Readiness Badges, Interactive Mode Guidance & Auto-Install Nudge) | `db-prod/apply-sql.sh`, `db-prod/apply-sql-native.sh`, `db-prod-pg/apply-sql.sh`, `db-prod-pg/apply-sql-native.sh`, `tests/test_db_prod_apply_sql.py`, `tests/test_db_prod_pg_apply_sql.py`, `tests/CHECKLIST.md` | **数据库迁移脚本（MySQL 与 PostgreSQL 双驱动、共 4 个脚本）全套终端高颜值控制台 UI 呈现、环境校验通过提示与交互式安装引导闭环**：① **ANSI 终端色彩与高质感调色板**：使用 Bash ANSI-C 标准字符串（`$'\033[...'`）统一定义 Bold、Cyan、Green、Yellow、Magenta、Dim 等色彩变量，彻底消除原先输出字面量 `\033` 的伪乱码问题；支持 TTY 检测（`[ -t 1 ]`）与 `FORCE_COLOR` 环境变量控制，非 TTY 环境自动安全回退为无样式纯文本，既保障终端交互时的视觉质感，又防止自动化管道与单测被转义符污染；② **运行环境校验通过高亮提示**：在执行迁移或进入交互模式时，前置环境校验通过后自动输出高亮就绪徽标：Python 驱动脚本输出 `✓ 运行环境校验通过: Python <version> (<path>) | 依赖库: aiomysql/psycopg [已就绪]`；免 Python 原生客户端脚本输出 `✓ 运行环境校验通过: 原生客户端 mysql/psql [<path>] [已就绪] | 免 Python 驱动模式`，给用户清晰确切的环境反馈；③ **缺失环境的智能交互式安装引导**：若缺少 Python 虚拟环境，在交互式终端下自动提示是否执行 `python3 -m venv .venv && pip install -r requirements.txt`；若缺少核心库（`aiomysql` 或 `psycopg`），交互式提示 `是否立即自动为您安装该模块？[y/N]`，确认后由脚本直接调用 pip 自动完成安装；原生客户端缺失时输出 macOS Homebrew / Ubuntu / CentOS 各系统的精准安装指令；④ **结构化卡片边框、进度方块与仪表板看板**：`--help` 采用双线青色边框横幅，无参模式采用模式选择卡片（支持 1/2/3/q），待执行文件采用圆角清单卡片，执行中实时输出 `[ N / Total ]` 彩色进度，执行完毕后输出结构化统计看板（耗时、文件数、新增表、变更表、当前表总数）与首次登录指引；⑤ **测试覆盖与回归验证**：原有 47 项单测 100% 保持绿灯全过；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 47 项迁移单测 100% 全绿；环境校验与自动引导就绪；未代跑真实服务与启动脚本 | 2026-09-14 |
| PostgreSQL 迁移脚本 apply-sql.sh 与免 Python 原生客户端脚本 apply-sql-native.sh 体验同步升级：支持 --help、--all、--spec 范围指定、--last 断点续跑、真实新增/变更数据表统计面板输出与无参交互式引导及 YES 二次确认 (PostgreSQL Migration Scripts apply-sql.sh & apply-sql-native.sh CLI & Interactive Upgrade: --help, --all, --spec vX-vY, --last Resume, Migration Summary Statistics, Guide Mode & Strict YES Confirmation) | `db-prod-pg/apply-sql.sh`, `db-prod-pg/apply-sql-native.sh`, `db-prod-pg/apply_sql.py`, `tests/test_db_prod_pg_apply_sql.py`, `tests/CHECKLIST.md` | **PostgreSQL 数据库迁移脚本双驱动（Python+psycopg 与原生 psql 命令行客户端）全能力对齐与安全交互闭环**：① **多参数支持与解析**：双脚本均全面支持 `-h`/`--help` 打印结构化帮助说明、`-a`/`--all` 重跑/执行全量脚本（`db-prod-pg/V*.sql`）、`-s`/`--spec` 指定版本范围（如 `v0-v56`、`0-56`）、单个版本（`v56`）或具体文件名（`V56-xxx.sql`）；② **断点进度记录与 `--last` 续跑**：脚本每步执行成功后自动将文件名记录至临时文件 `db-prod-pg/.last_applied_sql`（已在 `.gitignore` 忽略），支持传入 `-l`/`--last` 自动从上次断点位置选取脚本继续执行，无参交互模式下若检测到上次记录文件自动展示 `[3] last` 选项引导用户；③ **迁移结果统计看板 (Migration Summary)**：迁移执行前通过 `psql` / Python 快照查询目标库现有数据表，执行完成后再次查询比对并输出高亮统计看板——包含目标数据库、迁移脚本总数、真实新增数据表清单及计数、结构变更数据表（ALTER TABLE 涉及表）清单及计数、当前目标库表总数；④ **无参交互引导与严谨确认**：无参数执行时默认打印帮助说明，并引导用户交互式选择 `[1] all`、`[2] spec` 或 `[3] last` 模式，选择完毕后列出本次选中的脚本清单及总数；在配置数据库连接后，必须严格输入 `YES`/`yes` 才会正式执行，未确认则安全取消；⑤ **免 Python 原生脚本全新交付**：全新创建 `db-prod-pg/apply-sql-native.sh`，仅依赖系统 `psql` 客户端，支持环境变量 `PGPASSWORD` 安全传参和 `ON_ERROR_STOP=1` 错误熔断，并在执行前自动探测或创建目标数据库（UTF8 编码）；⑥ **测试覆盖**：在 `tests/test_db_prod_pg_apply_sql.py` 中编写包含 Python 驱动与 Native 客户端的参数解析、`--help`、`--spec` 范围过滤、`--last` 断点定位、交互式退出及 `\c` 数据库切换指令过滤等测试，连同 MySQL 迁移测试共 47 项单测 100% 绿灯全过；严格遵守执行边界，未代跑真实服务与启动脚本。 | ✅ 47 项双库迁移单测全绿；未代跑真实服务与启动脚本 | 2026-09-14 |
| MySQL 迁移脚本 apply-sql.sh 与原生客户端脚本 apply-sql-native.sh 体验同步升级：支持 --help、--all、--spec 范围指定、--last 断点续跑、真实新增/变更数据表统计面板输出与无参交互式引导及 YES 二次确认 (apply-sql.sh & apply-sql-native.sh CLI & Interactive Experience Upgrade: --help, --all, --spec vX-vY, --last Resume, Migration Summary Statistics, Guide Mode & Strict YES Confirmation) | `db-prod/apply-sql.sh`, `db-prod/apply-sql-native.sh`, `db-prod/apply_sql.py`, `.gitignore`, `tests/test_db_prod_apply_sql.py`, `tests/CHECKLIST.md` | **MySQL 数据库迁移脚本双驱动（Python 与系统 mysql 原生命令行）全能力对齐与安全交互闭环**：① **多参数支持与解析**：双脚本均全面支持 `-h`/`--help` 打印结构化帮助说明、`-a`/`--all` 重跑/执行全量脚本、`-s`/`--spec` 指定版本范围（如 `v1-v31`、`V1-V31`、`1-31`）、单个版本（`v155`）或具体文件名（`V155-xxx.sql`）；② **断点进度记录与 `--last` 续跑**：脚本执行每步成功后自动记录至当前目录临时文件 `db-prod/.last_applied_sql`（已纳入 `.gitignore` 防污染），支持传入 `-l`/`--last` 自动从上次位置开始继续执行，无参交互模式下若检测到上次记录文件自动展示 `[3] last` 选项引导用户；③ **迁移结果统计看板 (Migration Summary)**：迁移执行前自动记录数据表现状快照，并在全部脚本执行完成后对比呈现结构化统计看板——包含目标数据库、迁移脚本总数、执行 SQL 语句总数、真实新增数据表清单及计数、结构变更数据表（ALTER TABLE 涉及表）清单及计数、幂等跳过（已存在/已变更）操作次数、当前库表总数；④ **无参交互引导与严谨确认**：无参数执行时默认打印帮助信息，并交互式引导用户选择 `[1] all`、`[2] spec` 或 `[3] last` 模式，选完后列出待执行脚本清单与文件总数；输入数据库连接参数与字符集探测后，必须严格输入 `YES`/`yes` 才正式执行，未确认则安全取消；⑤ **跨平台 Bash 兼容加固**：使用兼容 Bash 3.2 的进程替换与 `while IFS= read` 替代 `mapfile`，完美适配 macOS 与 Linux；⑥ **测试覆盖**：在 `tests/test_db_prod_apply_sql.py` 中新增/更新 CLI 专项单测与 `--list-tables` 参数测试，全量 34 项测试 100% 全绿；未代跑真实服务与启动脚本。 | ✅ 34 项单测全绿；未代跑真实服务与启动脚本 | 2026-09-14 |
| AI 主动提问交互框 (UserQuestionCard) 紧凑度重构与消息卡片宽度对齐 (UserQuestionCard Compaction & Agent Message Bubble Alignment) | `frontend/src/components/UserQuestionCard.vue`, `frontend/src/components/BusinessConfirmationCard.vue`, `frontend/src/views/AgentDebug.vue`, `tests/CHECKLIST.md` | **AI 主动提问交互框精致紧凑化与卡片宽度严丝合缝对齐闭环**：① **移入消息卡片统一体系**：在 `AgentDebug.vue` 中将原本孤立在消息气泡外的 `UserQuestionCard` 与 `BusinessConfirmationCard` 移入统一的 Agent Message Bubble 容器内，与上方的文字、时间线共享同一卡片底色与边界，彻底根除上方气泡与下方提问卡宽度不对齐的问题；② **卡片精致与紧凑度重构**：选项间距收紧为 `space-y-1.5`，内边距收敛为 `px-2.5 py-1.5`，单选/多选指示器差异化区分（单选圆角圆圈、多选圆角方形带对勾）；补充输入框调整为单行轻量文本域（`rows="1"`）；内边距与按钮栏整体紧凑化，视觉体量大幅精炼；③ **验证覆盖**：前端契约测试 `test_user_question_contract.py` 100% 全绿通过；未代跑真实服务与启动脚本。 | ✅ 前端契约测试全绿；未代跑真实服务与启动脚本 | 2026-09-14 |
| 元数据表卡片头部操作按钮组改为常显展示 (Metadata Table Card Header Action Buttons Always Visible) | `frontend/src/views/MetadataTables.vue`, `tests/CHECKLIST.md` | **元数据表卡片头部操作按钮交互优化闭环**：① **移除鼠标悬停隐藏限制**：移除数据表卡片操作区（编辑表结构、删除表、智能发现推荐指标）的 `opacity-0 group-hover:opacity-100 transition-opacity` 样式，改为默认常显展示，避免鼠标移出或悬停才触发的交互阻碍，大幅提升表资产快速管理效率；② **测试与验证**：前端 `vue-tsc --noEmit` 0 报错通过，17 项前端契约测试全部通过；未代跑真实服务与启动脚本。 | ✅ 前端契约 17 项全绿；`vue-tsc` 0 报错；未代跑真实服务与启动脚本 | 2026-09-14 |
| 数据集质量治理分（0-100，只读展示：类型一致性 30 + 结构覆盖 25 + 备注完备度 25 + 表完整性 20，巡检结算写入，仅供展示与治理优先级排序，不接入 AI 检索/编排权重） (Dataset Quality Governance Score, Read-Only, Inspection-Settled) | `app/services/metadata_quality_score_service.py`, `app/models/metadata.py`, `app/schemas/metadata.py`, `app/services/metadata_inspection_service.py`, `frontend/src/api/metadata.ts`, `frontend/src/views/MetadataDatasets.vue`, `db-prod/V158-add_metadata_quality_score.sql`, `db-prod-pg/V59-add_metadata_quality_score.sql`, `tests/services/test_metadata_quality_score_service.py`, `docs/superpowers/plans/2026-09-14-dataset-quality-score.md`, `tests/CHECKLIST.md` | **数据集级质量治理分与治理优先级排序闭环**：① **纯函数评分服务 `compute_quality_score`**：仅消费巡检已产出的汇总计数（`mismatch_count` / `stale_count + new_count` / `missing_comment_count` / `missing_tables_count`）换算为 0-100 分与可解释的维度明细（`score/level/level_label/dimensions`），不访问数据库、不扫描数据内容；权重公开（类型一致性 30、结构覆盖 25、备注完备度 25、表完整性 20），等级 ≥90 优秀 / ≥75 良好 / ≥60 一般 / <60 较差；② **持久化字段与双库幂等迁移**：`meta_datasets` 新增 `quality_score` / `quality_breakdown` / `quality_scored_at`，提供 MySQL `V158` 与 PostgreSQL `V59` 迁移；③ **巡检结算写入**：单数据集 `inspect_dataset` 与全库 `inspect_all_datasets` 两条路径在告警提交前结算并写入分数；④ **前端只读展示与排序**：`GET /datasets` 经 Pydantic `from_attributes` 自动带出字段，数据集卡片与列表新增质量分徽标（按分段着色）、悬停展示各维度得分与问题计数、列表支持按质量分排序（未评分排最后）；所有角色只读可见，无新增权限依赖；⑤ **测试**：新增 7 项纯函数单测，巡检 10 项回归全绿，`vue-tsc --noEmit` 0 报错。 | ✅ 7 项评分单测 + 10 项巡检测试全绿；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-14 |
| 元数据物理类型归一化误判根除、漂移去重区分类型、有界并发外部库保护等代码审查专项修复 (Metadata Drift Code Review Hardening: normalize_column_type Prefix Matching, Alert Deduplication by drift_type, Bounded Concurrency & Full Test Suite Green) | `app/services/metadata_drift_service.py`, `app/services/metadata_service.py`, `frontend/src/api/metadata.ts`, `frontend/src/components/metadata/MetadataDriftAlertsDrawer.vue`, `frontend/src/views/MetadataTables.vue`, `tests/services/test_metadata_drift_service.py`, `tests/services/test_recommend_column_semantic.py`, `tests/frontend/test_metadata_icon_contract.py`, `tests/CHECKLIST.md` | **元数据漂移告警与表编辑 AI 推荐审查缺陷深度修复与测试全绿闭环**：① **根除 `normalize_column_type` 类型误判 (C1)**：废除高风险子串匹配与裸 `long`/`int`，采用规范的前缀令牌与词首匹配规则（如 `t.startswith(...)`），防止 `longtext`/`longblob` 误判为 `Int64`（纠正为 `String`）、防止 `point`/`multipoint` 因含 `int` 误判为 `Int64`（归入 `String`），支持 ClickHouse `nested(...)` 优先归入 `JSON`；测试 `test_normalize_column_type_mapping` 增加边界用例并全面转绿 (C2)；② **重构批量并发 AI 补全测试 (C3)**：改用基于 SQL 语句智能分流的 `mock_db.execute`，彻底解决 `test_batch_resolve_alerts_add_column_auto_ai` 因脆弱顺序错位导致的 `'MetaDataset' object has no attribute 'columns'` 崩溃，确保批量并发 AI 真实链路被完整断言覆盖；并在 core 入口初始化 `column_updated = False` 根除局部变量未绑定报错；③ **告警去重补充 `drift_type` (I1)**：在 `record_drift_alert_core` 查询 WHERE 条件中增加 `drift_type == drift_type`，防止同一列的 `type_mismatch` 吞没 `missing_comment`，并新增定向单测验证；④ **保持精细化治理交互**：批量操作栏明确不提供粗暴的“一键补备注”按钮，保持由管理员在表编辑中按字段人机协同 AI 推荐或单条精细化治理，避免低质备注盲目回填；⑤ **`update_comment` 空变更跳过保护 (I3)**：当描述与同义词均未发生实质变更时，安全跳过变更日志记录与向量同步，保持告警待处理状态，杜绝虚假解决与无谓计算；⑥ **全流程有界并发与防注入 (I4, M2, M5)**：将外部数据源列信息读取与数据采样一并纳入 `asyncio.Semaphore(4)` 保护，并发前串行初始化 `adapter_cache` 消除竞态，并对列名/表名进行严格标识符正则校验（`^[A-Za-z0-9_]+$`）；⑦ **公共 helper 与类型定义加固 (M1, M3, M4)**：移除未使用的 `update` 导入，抽取 `_parse_llm_json_response` 统一解析 Markdown 围栏与 JSON，修正前端 `MetaDriftAlert.drift_type` 联合类型声明防止宽化；⑧ **契约规范与测试 100% 绿灯**：移除 `MetadataTables.vue` 中的 📊 Emoji 以满足契约规范；后端 40 项单测全部通过，前端 `vue-tsc --noEmit` 0 报错通过，17 项前端契约测试全绿通过。 | ✅ 后端 40 项单测全绿；前端契约 17 项全绿；`vue-tsc` 0 报错；未代跑真实服务与启动脚本 | 2026-09-14 |
| 元数据表编辑界面字段「AI 推荐语义」与一键回填体系（支持源表结构与真实数据采样推断、空字段名输入防御置灰、源表不存在此字段友好提示防御、弹窗微调与一键应用回填表单） (Metadata Table Column AI Semantic Recommendation, Source Sampling & In-Place Form Auto-Fill) | `app/schemas/metadata.py`, `app/services/metadata_service.py`, `app/api/portal/endpoints/metadata.py`, `frontend/src/api/metadata.ts`, `frontend/src/views/MetadataTables.vue`, `tests/services/test_recommend_column_semantic.py`, `tests/CHECKLIST.md` | **元数据表编辑字段 AI 推荐语义与数据采样回填闭环体系**：① **源表存在性与空输入防御**：在编辑表与创建表弹窗的字段行操作列提供 SparklesIcon「AI 推荐语义」按钮；若字段名为空通过 `:disabled` 自动置灰并展示“请先填写物理字段名”Tooltip；当源表未建或字段在物理表中不存在时，后端精准返回 `physical_exists=False`，前端弹窗给出醒目琥珀色警示“源表中不存在此字段，无法推荐语义”，彻底防御虚假推荐；② **真实数据采样与大模型推断**：当物理字段存在时，直连物理数据源提取前 3 条真实采样数据与原生注释，结合同表其他已有字段的业务术语作为上下文，由大模型推断业务名称（`term`）、业务描述（`description`）与同义词（`synonyms`）；③ **人机协同微调与一键应用**：弹窗中以卡片展示物理信息、采样数据和兄弟字段，提供可直接编辑的表单；点击「应用到表单」自动将确认后的 Term 与 Description 回填到对应行输入框中，用户在主弹窗统一点击「保存修改」提交生效，符合常规表单操作心智；④ **自动化测试覆盖**：在 `test_recommend_column_semantic.py` 中编写完整单元测试，覆盖正常采样与推荐、物理字段不存在拦截、空数据源拦截及异常保护；前端 `vue-tsc` 0 报错通过。 | ✅ 后端与前端代码已就绪；新增定向测试覆盖；未代跑真实服务与启动脚本 | 2026-09-14 |
| 元数据物理新增字段批量录入自动并发 AI 补全中文名与描述、字段备注缺失巡检与人机协同治理（批量 add_column 智能分流物理注释与 Semaphore(4) 并发 AI 语义推断、missing_comment 巡检与物理注释批量回填、抽屉弹窗人机协同治理与自动向量同步闭环） (Metadata Schema Drift Batch Ingestion with Concurrent AI Semantic Completion, Missing Comment Inspection & HITL Governance) | `app/schemas/metadata.py`, `app/api/portal/endpoints/metadata.py`, `app/services/metadata_drift_service.py`, `app/services/metadata_inspection_service.py`, `frontend/src/api/metadata.ts`, `frontend/src/components/metadata/MetadataDriftAlertsDrawer.vue`, `tests/services/test_metadata_drift_service.py`, `tests/services/test_metadata_inspection_service.py`, `tests/CHECKLIST.md` | **元数据物理新增字段一键录入并发 AI 自动补全与字段备注缺失巡检治理闭环**：① **批量录入新增字段并发 AI 语义推断**：在 `batch_resolve_alerts` 与 `batch_resolve_alerts_global` 中引入 `auto_ai_complete=True` 与 `_prepare_add_column_ai_metadata`；针对待录入字段实现智能分流：物理库已有注释的直接采纳物理注释（0 耗时）；物理库无注释的字段，通过 `asyncio.gather` 结合 `asyncio.Semaphore(4)` 协程池并发调用大模型，在 5~8 秒内快速推断准确的中文业务术语（`term`）与业务描述（`description`），一步到位补齐资产；带超时与异常降级（默认英文字段名兜底），保证主事务不中断；② **单字段人机协同 AI 深度分析模态框**：针对 `new_in_db` 告警提供「录入元数据」弹窗，展示物理类型、样例值、同表兄弟字段参考，由 AI 生成建议的中文名与描述供管理员确认微调后再入库；③ **字段备注缺失巡检 (`missing_comment`)**：巡检引擎新增规则，检出元数据已收录但备注为空或备注直接等于英文字段名（未认真维护）的字段；支持「批量补充备注（物理库注释优先）」快速回填，以及单字段「补充备注」AI 语义推断；④ **同步与向量更新闭环**：`add_column` 与 `update_comment` 处置后均自动记录结构化变更日志（`ChangelogService`），并自动标记数据集已修改并同步本地 Redis 向量知识库（`_try_sync_local_vector`）；⑤ **单元测试覆盖**：在 `test_metadata_drift_service.py` 中新增 `test_batch_resolve_alerts_add_column_auto_ai` 单元测试，验证物理注释优先采纳与无注释字段并发 AI 补全及统计信息。 | ✅ 后端与前端代码已就绪；新增定向测试覆盖；未代跑真实服务与启动脚本 | 2026-09-14 |
| 元数据物理字段补齐与类型校准全面归一化为 6 大通用标准类型（String, Int64, Float64, DateTime, Boolean, JSON） (Metadata Column Type Normalization to 6 Universal Platform Types) | `app/services/metadata_drift_service.py`, `tests/services/test_metadata_drift_service.py`, `tests/CHECKLIST.md` | **元数据物理字段类型向 6 大通用标准大类收敛与归一化闭环**：① **构建统一的类型映射引擎 (`normalize_column_type`)**：将物理数据库（MySQL / PostgreSQL / ClickHouse / Oracle / SQL Server）原生碎片化的具体方言类型（如 `varchar`, `text`, `varchar2` 统一归为 `String`；`int`, `bigint`, `tinyint`, `integer` 等统一归为 `Int64`；`decimal`, `numeric`, `float`, `double` 等统一归为 `Float64`；`date`, `datetime`, `timestamp` 统一归为 `DateTime`；`bool`, `boolean`, `bit` 统一归为 `Boolean`；`json`, `jsonb`, `object` 统一归为 `JSON`），剔除修饰符与长度精度；② **新增字段录入 (`add_column`) 自动归一化**：巡检检出物理新增字段后，通过适配器提取的原生数据类型先经过归一化转换，存入 `MetaColumn.type` 的值直接为这 6 大标准大类，与前端编辑下拉选项 100% 对齐；③ **类型不一致智能校准 (`sync_type`) 自动归一化**：直连物理库或从巡检告警文本中提取物理类型时，同步转换为规范大类并更新元数据；④ **AI 分析弹窗与前端视图统一**：`analyze_new_column_ai` 返回的 `physical_type` 同步输出规范大类，消除用户认知割裂；⑤ **单元测试覆盖**：更新 `test_resolve_alert_sync_type` 断言为 `Int64`，新增 `test_normalize_column_type_mapping` 测试用例，全面覆盖 6 大类数十种常见原生数据库类型映射。 | ✅ 后端代码已就绪；新增定向测试覆盖；未代跑真实服务与启动脚本 | 2026-09-14 |
| 元数据管理右上角下拉菜单按钮尺寸对齐、巡检换用心电脉冲波（Activity Pulse）与 AI 推荐规范化 SparklesIcon 星星图标（全局巡检全面升级为心电图脉冲波折线矢量图标彻底消除板夹像复制按钮及准心像扫码框的困扰与闪电重复、表卡片智能推荐发现与 AI 补全描述全面由闪电替换为代表 AI 的 SparklesIcon 星星矢量图标、定时巡检替换为标准时钟 clock 矢量图标彻底消除 Emoji 撑高行高导致的尺寸不一） (Metadata Dropdown Alignment, Activity Pulse Inspection & AI SparklesIcon Normalization) | `frontend/src/views/MetadataDatasets.vue`, `frontend/src/views/MetadataTables.vue`, `frontend/src/components/metadata/MetadataDriftAlertsDrawer.vue`, `tests/CHECKLIST.md` | **元数据管理下拉菜单尺寸对齐、心电脉冲波巡检与 AI 推荐图标规范化闭环**：① **消除尺寸与行高不一致**：将右上角下拉菜单中的【全局巡检】与【定时巡检】从原生系统 Emoji（`⚡` 与 `⏱️`）替换为标准 `h-4 w-4 shrink-0` SVG 矢量图标，彻底消除因 Emoji 字体基线和排版盒高度撑大行高的问题，使 4 个菜单项在高度、文字排版、hover 底色及垂直对齐上保持像素级一致；② **巡检图标升级为心电脉冲波 (Activity Pulse)**：彻底淘汰容易引起“复制到剪贴板”歧义的板夹图标与“像扫码拍照”的取景框，统一采用极具健康体检直觉、代表系统与物理库健康度探针的「心电脉冲波 (`Activity Pulse`)」矢量折线图标，全站数据集卡片、表格行、详情页操作栏及治理抽屉控制台图标保持高度统一；③ **AI 智能推荐与补全描述图标规范化（换用代表 AI 的星星 SparklesIcon）**：将数据表卡片操作区「为此表智能发现推荐指标」悬浮按钮、详情页操作栏与内联「AI 补全描述」按钮、AI 生成弹窗标题及编辑弹窗中的「AI 辅助生成」按钮从原先误用的闪电图标全面替换为 Heroicons 官方代表 AI 的 `SparklesIcon` 星星矢量图标，彻底消除视觉误解与闪电滥用。 | ✅ 前端代码已就绪；未代跑真实服务与启动脚本 | 2026-09-14 |
| NanZi AI Agent Platform v1.0.15.0 版本发布文档套件交付 (NanZi AI Agent Platform v1.0.15.0 Release Notes, WeChat HTML & Visual Cover Specs Delivery) | `docs/release/1.0.15/release_log.md`, `docs/release/1.0.15/release_log_wechat.html`, `docs/release/1.0.15/release-cover-info.md`, `tests/CHECKLIST.md` | **v1.0.15.0 完整发布文档矩阵与全渠道交付闭环**：① **GitHub Release 官方版本说明 (`release_log.md`)**：全面梳理自 `4f004dff` 到 `40098a61` 共 118 个提交、268 个文件变更的全部特性，覆盖 7 大技术模块（K8s 原生 Pod 安全沙箱、按需惰性拉起与预热回收、Prompt Cache 稳定前缀与真实结果优先防编造、元数据物理巡检与整表下线、等保三级与 2FA、飞书 Schema 2.0 卡片通知、ChatBI 10 万行导出）、9 组双数据库平滑迁移与 Docker / 源码两种部署升级指引；② **微信公众号排版文章 (`release_log_wechat.html`)**：深度定制内联样式，重构深奥架构术语为通俗易懂的业务价值表达，单独拆分飞书 Schema 2.0 与 5 大通知渠道卡片，移动端防挤压横向滑动表格与全套截图占位符；③ **封面视觉规格与生图提示词 (`release-cover-info.md`)**：交付三大场景引流文案、设计关键词与生图 Prompt。 | ✅ 文档矩阵全量交付与校对完成；未代跑真实服务与启动脚本 | 2026-09-13 |
| Agent Debug 页面顶部冗余模式选择栏及清空/运行逻辑按钮移除与底部 ChatInput 路由胶囊全面对齐 (Agent Debug Top Mode Selector, Clear History & Logic Flow Removal & Bottom ChatInput Routing Alignment) | `frontend/src/views/AgentDebug.vue`, `tests/frontend/test_chat_surface_refactor_contract.py`, `tests/CHECKLIST.md` | **Agent Debug 页面顶部冗余组件精简与底部专家路由统一闭环**：① **移除冗余顶部模式选择与死代码清理**：彻底清理 `AgentDebug.vue` 顶部早期遗留的 `Mode Selector Bar`（包含 `🤖 智能委派 (Auto)`、`🎯 指定智能体 (Specific)` 及其专属下拉菜单 `agentDropdownRef` 和全局 `handleDocumentClick` 监听），大幅释放页面纵向有效调试与历史滚动区域；② **移除顶部右侧清空与运行逻辑按钮**：彻底删除顶部右侧操作栏中冗余的【清空（垃圾桶）】与【运行逻辑】按钮及多余分隔线，仅保留【全屏】、【导出 Markdown】与【调试配置 (齿轮)】，会话清空统一由底部快捷指令条 `/new 新会话` 承载，界面更紧凑清爽；③ **对齐底部输入框专家路由胶囊**：在 `<ChatInput>` 上绑定 `:routing-mode="debugMode === 'specific' ? 'expert' : 'auto'"`、`:expert-agent-id="agentParams.agent_id || ''"`，并补齐 `@switch-to-auto` 与 `@switch-to-expert` 事件响应，使调试页与主对话页 `EmbedChat` 在底部胶囊及 `@` 呼出专家体验上实现 100% 统一；④ **契约测试防护**：在 `tests/frontend/test_chat_surface_refactor_contract.py` 中新增断言测试，断言顶部模式栏、清空会话与运行逻辑按钮均已移除，防止后续代码误引入回退。 | ✅ 前端 `vue-tsc` 0 报错；新增契约测试覆盖；未代跑真实服务与启动脚本 | 2026-09-15 |
| Agent Debug 页面快捷指令条冗余 Emoji 消除与图标契约测试对齐 (Agent Debug System Shortcut Duplicate Emoji Removal & Icon Contract Alignment) | `frontend/src/views/AgentDebug.vue`, `tests/frontend/test_chat_input_icon_contract.py`, `tests/CHECKLIST.md` | **Agent Debug 页面快捷指令与主对话 EmbedChat 图标规范全对齐闭环**：① **消除重复 Emoji**：将 `AgentDebug.vue` 中 `SYSTEM_SLASH_COMMANDS` 各项 `label` 内的硬编码 Emoji（💬、🕒、📊、📚、💻 等）彻底剥离，改为纯净文本，彻底解决底层 `ChatInput.vue` 渲染 SVG 图标组件与 label 内 Emoji 双重图标并存的问题；② **前端契约测试防护**：在 `tests/frontend/test_chat_input_icon_contract.py` 中引入 `AGENT_DEBUG` 并断言 `SYSTEM_SLASH_COMMANDS` 不包含 Emoji，防止后续代码回归。 | ✅ 前端契约测试覆盖；未代跑真实服务与启动脚本 | 2026-09-15 |
| 平台公共文档绝对路径引导加固与目录工具 list_accessible_directories 强约束引导优化 (Platform Public Docs Absolute Host Path Enforcement & Always-Available list_accessible_directories Tool Guidance) | `app/services/ai/agent_prompts.py`, `tests/ai/test_directory_catalog_tool.py`, `tests/CHECKLIST.md` | **公共文档绝对路径与目录工具始终可用提示词闭环**：① **平台公共文档绝对路径强引导**：在全局帮助提示词中明确强调「公共文档须用宿主侧绝对路径读取（Docker 部署为 `/app/data/docs/...`，本地开发为项目根 `data/docs/...`），切勿把 `data/docs` 当作个人工作区下的相对路径，避免被文件工具误解析到个人工作区并报找不到目录」；② **目录发现工具强引导**：将防盲猜规范与目录指引由“若绑定了/可调用”全面强化为“直接调用 **list_accessible_directories**（平台内置、始终可用）”，杜绝模型盲猜路径或因犹豫而试错；③ **单测断言同步更新**：同步更新 `tests/ai/test_directory_catalog_tool.py` 中关于目录工具与公共文档提示词的断言。 | ✅ 定向单测断言同步；未代跑真实服务与启动脚本 | 2026-09-13 |
| AgentScope 按需沙箱与运行时工具权限预检 NoneType 崩溃修复、Bash 节点拉起绑定与执行时间线全层级（含 subStep in child.children）跳秒动效全链路打通 (AgentScope Lazy Sandbox Permission NoneType Bugfix, Bash-Scoped Node Parent Binding & Prewarm Tick Animation Across All Timeline Depths) | `app/services/ai/runtime/agentscope/workspace.py`, `app/services/ai/runtime/agentscope/tools.py`, `app/services/ai/runtime/agentscope/middleware.py`, `app/services/ai/runtime/agentscope/agent_runtime.py`, `frontend/src/components/chat/ChatExecutionTimeline.vue`, `frontend/src/utils/processTimeline.ts`, `frontend/src/views/EmbedChat.vue`, `tests/ai/runtime/test_lazy_sandbox_workspace.py`, `tests/frontend/test_chat_shared_helpers_behavior.py`, `tests/CHECKLIST.md` | **按需沙箱权限防御、Bash 卡片进度挂载与执行时间线全层级跳秒动效全闭环**：① **NoneType 崩溃根除**：在 `LazySandboxBashNativeTool` 与 `WorkspaceFileToolProxy` 权限预检中杜绝返回 `None`，在 `AgentScopeNativeApprovalTool` 增加 `if result is not None` 安全兜底，彻底解决 `'NoneType' object has no attribute 'behavior'`；② **Bash 卡片父节点挂载**：引入 `ContextVar` 与 `BashSandboxParentLinkMiddleware`，将 Bash 工具触发的沙箱拉起进度（pending/success/error）通过独立 ID `workspace:sandbox:<bash_id>` 精准挂载至当前 Bash 卡片下方，避免漂移至准备节点；③ **补齐时间线折叠卡片子项 (subStep in child.children) 动效渲染**：彻底定位并根除在「1 个工具调用」主折叠卡片展开后，挂在 Bash 工具（`child`）下方的「沙箱工作区准备」（`subStep`）漏渲染预热跳秒动效的遗漏点，补齐滑动动效条与动态跳秒文案；同步补齐 `nestedStep in subStep.children`；④ **前端 500ms 跳秒与滑动进度条动效继承及安抚文案优化**：放宽 `isWorkspacePrewarmPending` 判定，支持 `id.startsWith("workspace:sandbox:")` 前缀，让挂在 Bash 卡片下方的沙箱拉起也能无缝享受 500ms 动态跳秒（`已等待 Ns`）、滑动动效（`workspace-prewarm-bar`）；优化三阶段安抚文案：阶段一标明「首次创建沙箱，正在申请隔离资源配置…」，阶段二调整为「正在初始化沙箱工作区（拉取镜像与启动运行环境）…」（剔除仅特指 K8s 的 Pod 术语，兼容 Docker 与 K8s 双沙箱）；⑤ **双端自动化单测全覆盖**：后端 8 项 lazy sandbox 测试全绿，前端 89 项契约与行为测试全绿。 | ✅ 定向回归测试通过；后端 8 项 + 前端 89 项单测全绿；未代跑真实服务与启动脚本 | 2026-09-12 |
| 真实结果优先系统提示词加固：工具绑定且任务涉可验证外部/运行时事实时「必须先调用工具」，严禁凭空输出状态码/耗时/版本等可验证数值（泛化反编造，非仅连通性个案） (Real-Result-First Prompt Guard: Mandatory Tool Call for Verifiable Facts & No Fabricated Measurable Values) | `app/services/ai/agent_prompts.py`, `tests/ai/test_prompt_assembler.py`, `tests/CHECKLIST.md` | **可验证真实事实必须真实执行、严禁凭空给出可验证数值的泛化提示词加固闭环**：① **新增固定系统提示词专节「真实结果优先：禁止凭空给出可验证数值」**：明确当任务需要获取可验证的外部或运行时事实（URL/网站连通性与 HTTP 状态码、请求/连接/DNS 耗时、传输/文件字节数、服务或端口在线状态、CPU/内存/磁盘/负载、进程列表、日志内容、软件或依赖版本、实时业务指标等），且本轮已绑定相应工具（Bash/浏览器/网络/系统/文件/进程等）时，**必须先真正调用工具获取结果再回答，不得凭常识、记忆或推测替代执行**；**严禁输出只有真实执行才能得到的数值**（如「HTTP 200」「耗时 5.7ms」「已连接」「版本 3.11」），未执行/失败/为空时须如实说明「未能真实获取」，只允许定性推理或引导用户自行验证，一切以工具真实返回为准；② **连通性/系统状态动态规则同步升级**：将原「…应先调用合适工具」强化为「**必须先调用合适工具获取真实结果再回答，禁止凭空报告状态、状态码或耗时等可验证数值**；调用失败或未执行时如实说明”；③ **泛化反编造（非仅个案）**：该约束覆盖所有「需真实可验证结果」的任务类型与场景，与既有「安全与保密：不得虚构度量数值」「没有对应工具时禁止假装执行」互相呼应，从「执行倾向」层补足模型在工具已绑定却不调用、直接编造状态码/耗时的行为；④ **自动化单测覆盖**：在 `tests/ai/test_prompt_assembler.py` 新增 `test_platform_prompt_requires_real_result_over_fabricating_verifiable_values`，断言固定专节与动态连通性强约束均出现于组装后系统提示词；`test_prompt_assembler.py` 51 项与 `test_workspace_prompt.py`/`test_pipeline_steps.py` 相关 32 项全部绿灯。 | ✅ 新增定向单测+既有 51 项 prompt 组装测试与 32 项相关测试全绿；未代跑真实服务与启动脚本 | 2026-09-12 |
| Docker 与 Kubernetes 沙箱模式按需拉起（调用 Bash 前拉起并推流 SSE，未调 Bash 零容器开销）、移除沙箱提示横条 Banner、拉起与就绪 Toast 提示与预热保持 (On-Demand Docker & K8s Sandbox Startup on Bash Tool Invocation with SSE Timeline Streaming, Workspace Banner Removal & Startup/Ready Toast Feedback) | `app/services/ai/runtime/agentscope/workspace.py`, `app/services/ai/pipeline/steps/assemble_step.py`, `app/services/ai/agent_service.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/services/ai/runners/chatbi/agent_builder.py`, `frontend/src/views/EmbedChat.vue`, `frontend/src/components/chat/DockerWorkspaceBanner.vue`, `tests/frontend/test_chat_sandbox_workspace_contract.py`, `tests/ai/runtime/test_lazy_sandbox_workspace.py`, `tests/CHECKLIST.md` | **Docker 与 Kubernetes 沙箱按需拉起、执行时间线进度推流、横条 Banner 移除与双阶段 Toast 提示闭环**：① **惰性沙箱代理机制（LazySandboxWorkspaceProxy & LazySandboxBashNativeTool）**：将 Docker/K8s 沙箱模式由会话发消息时无条件拉起重构为在具体调用 Bash 工具（`exec_command`）时按需拉起；当用户没有 Bash 权限、进行普通对话、数据查询或仅使用宿主文件工具（Read/Write/Edit/Glob/Grep）时，沙箱容器/Pod 绝不拉起，彻底杜绝服务器容器开销浪费；补齐 AgentScope 原生工具协议元数据（`is_external_tool`, `is_state_injected` 等）与安全属性降级；② **执行时间线 SSE 进度推流与拉起/就绪双阶段 Toast 提示**：在 Bash 工具触发沙箱真实拉起时，向客户端推送 `status: "pending"` 并弹出「正在拉起沙箱运行环境…」Info Toast，无缝对接前端时间线 500ms 动态跳秒与阶段安抚动效；容器/Pod 准备就绪后推送 `status: "success"`，弹出「沙箱环境已就绪，正在执行命令…」Success Toast 并即时刷新左下角浮标状态；失败推送 `status: "error"` 并弹出报错 Toast；③ **保留预热与手动拉起能力**：`ensure_docker_workspace` 与 `ensure_k8s_workspace` 显式设置 `lazy_sandbox=False`，前端打开会话时的后台自动预热（`sandbox_auto_warm`）与用户在前端手动拉起沙箱（`/ensure` 端点）不受影响且能被即时复用；④ **移除 AssembleStep 盲目占位**：彻底清理进入会话时直接发送 `_build_workspace_placeholder_log` 的占位逻辑，消除打招呼或普通查询时闪烁“沙箱创建中”的缺陷；⑤ **彻底移除沙箱提示横条 Banner（DockerWorkspaceBanner）**：根据用户决策彻底删除输入框上方占位提示横条组件 `DockerWorkspaceBanner.vue`，清理 `EmbedChat.vue` 中的 Banner 状态与挂载点，输入框左下角状态浮标管理正常保留；⑥ **契约与单测全覆盖**：更新 `test_chat_sandbox_workspace_contract.py` 契约断言，新增 `test_lazy_sandbox_workspace.py` 覆盖按需拉起、未调用 Bash 不启动、调用 Bash 触发启动推流及已存活沙箱复用等场景。 | ✅ 前后端定向单测全绿；前端契约测试 9 项全绿；未代跑真实服务与启动脚本 | 2026-09-12 |
| 宿主侧文件工具优先、公共文档 data/docs/ 宿主专读与沙箱 Bash 文件操作边界系统提示词强引导增强 (Host File Tools Priority, Public Docs Host-Read Only & Sandbox Bash File Operation Boundary Prompt Guidance) | `app/services/ai/agent_prompts.py`, `app/services/ai/tool_nudge_policy.py`, `tests/ai/runtime/test_workspace_prompt.py`, `tests/CHECKLIST.md` | **文件读写与搜索宿主侧优先、平台公共文档宿主专读与沙箱 Bash 文件操作边界全链路提示词强约束闭环**：① **文件读写与搜索宿主侧工具优先定调**：在 `session_workspace_sandbox_block` 中明确声明「文件读写与搜索一律走宿主侧文件工具（Read/Write/Edit/Glob/Grep），不要用 Bash 访问文件」，严禁通过 Bash 执行 `cat`、`head`、`echo >`、`sed` 等读写或修改常规文件，彻底消除模型习惯性绕过宿主文件工具带来的路径混乱与权限校验穿透；② **平台公共文档专读规则统一**：统一 `agent_prompts.py` 与 `tool_nudge_policy.py` 口径，明确「平台公共文档（`data/docs/*.md`、`FAQ.md`）仅宿主侧可读，沙箱 Bash 不可见；请用 Read/Glob/Grep 直接读取」，严禁在 Bash 中盲目尝试访问 `data/docs/`、`/workspace/docs` 或 `/app/data/docs` 等容器内不存在路径；③ **沙箱内 Bash 文件操作边界精确界定**：明确「只有在触发沙箱内的运行环境操作（如执行脚本生成的输出文件、`/tmp` 容器临时文件、命令管道流转或系统诊断）时，才允许在 Bash 中读写沙箱内部生成的文件」；④ **自动化单测覆盖**：在 `tests/ai/runtime/test_workspace_prompt.py` 中新增 `test_workspace_prompt_enforces_host_file_tools_and_docs_rules` 断言测试，8 项提示词测试 100% 绿灯全过。 | ✅ 8 项单测全绿通过；未代跑真实服务与启动脚本 | 2026-09-12 |
| 执行过程时间线沙箱工作区准备预热文案与滑动动效同行右侧对齐补齐与 500ms 动态时钟跳秒修复（解决 computed 缓存导致 0s 卡死问题、覆盖 subStep / item / child 全层级，单测断言保护） (Timeline Sandbox Workspace Prewarm Progress & 500ms Tick Clock Computed Cache Fix) | `frontend/src/components/chat/ChatExecutionTimeline.vue`, `frontend/src/utils/processTimeline.ts`, `tests/frontend/test_chat_shared_helpers_behavior.py`, `tests/CHECKLIST.md` | **执行过程时间线沙箱工作区准备状态与动效全层级补齐闭环**：① **subStep 子项预热渲染补齐**：彻底修复「鉴权及上下文与能力准备」（log 类型节点）下挂载的「沙箱工作区准备」（subStep）在 pending 时未渲染预热状态导致右侧长条空白的缺陷；② **同行右侧精致对齐**：在子项、顶级项与 child 项内部右侧统一展示滑动进度条微动效（`workspace-prewarm-bar`）与动态文案（`已等待 Ns · 阶段安抚文案`），自适应截断（`truncate`）且带有完整 hover 悬停提示，精准填补单行右侧空白；③ **500ms 响应式时钟驱动链路修复**：彻底定位并根除由于 `void tickNow.value` 被错误放置在下游布尔值计算属性 `isWorkspacePrewarming` 内部导致 `prewarmElapsedMs` 因 Vue 3 computed 缓存优化而永远停留在 0s 不更新的缺陷；将 `void tickNow.value` 移至 `prewarmElapsedMs`，确保每 500ms 动态驱动毫秒重新计算，使已等待秒数（0s -> 1s -> 2s...）与三阶段安抚文案（申请资源 -> 初始化/拉取镜像 -> 耗时长请稍候）按时平滑更新；④ **类型安全与单测保护**：修复 `processTimeline.ts` 索引类型警告与未引用参数；在 `test_chat_shared_helpers_behavior.py` 中补充对 `subStep` 分支预热渲染与 `void tickNow.value` 时钟驱动的断言保护；`vue-tsc` 检查零新增错误。 | ✅ 前端行为与契约单测覆盖；`vue-tsc` 零相关报错；未代跑真实服务与启动脚本 | 2026-09-12 |
| 系统智能体设置行独立化、智能委派用途说明与 Main 兜底主专家状态强制锁定体系 (System Agent Configuration Row, Delegation Purpose Guidance & Main Agent System Lock Enforcement) | `app/services/ai/agent_manager.py`, `frontend/src/components/agent/AgentVersionEditorDrawer.vue`, `frontend/src/views/AgentManagement.vue`, `frontend/src/components/embed/ExpertCascadeMenu.vue`, `frontend/src/views/TaskCenter.vue`, `tests/CHECKLIST.md` | **系统智能体设置行独立化、智能委派用途说明与 Main 智能体系统状态双端强管控闭环**：① **设置项独立卡片行化**：彻底告别新建/编辑智能体抽屉与弹窗中偏安标题右侧隐蔽角落的微型开关，重构为独立的高亮配置行卡片，配专属盾牌微图标与状态胶囊标签，支持 Admin Only 权限校验；② **用途说明透出消除认知盲区**：明确注明「只有系统智能体才会加入智能委派候选列表由主助手自动委派，普通自定义智能体不参与自动委派且仅供手动直接对话；如果只是自己用，也不必设置为系统智能体」；③ **Main 兜底主专家双端锁定防护**：前端识别 `isMainAgent` 时强制将 switch 禁用（`:disabled` 并展示小锁图标与「主专家固定锁定」状态标签），鼠标 hover 提示不可取消；保存提交时前端兜底强制写入 `is_system = true`；后端在 `AgentManager.update_agent` 中加入底层守卫，判定为 Main 主智能体时强制保持 `is_system = True`，杜绝任何外部接口或请求篡改；④ **全平台 Tab 分类微图标统一**：专家中心与任务调度台 Tab 切换栏原实心圆点统一替换为官方认证盾牌（系统）与用户人像（自定义）高质感 SVG 微图标；⑤ **静态类型与语法检查**：`vue-tsc --noEmit` 0 报错通过，Python 编译无报错。 | ✅ 前端 `vue-tsc --noEmit` 0 报错；后端语法通过；未代跑真实服务与启动脚本 | 2026-09-12 |
| MySQL 数据库迁移脚本 Warning 优雅提示与连接时存在性预检 (MySQL Database Migration Script Elegant Warning Output & Safe Preflight Check) | `db-prod/apply_sql.py`, `db-prod/apply-sql.sh`, `db-prod-pg/apply-sql.sh`, `tests/test_db_prod_apply_sql.py`, `tests/CHECKLIST.md` | **MySQL 数据库迁移脚本控制台 Warning 优雅提示与自动建库前置检查（彻底根除 `aiomysql` 内部代码调用行与文件路径暴露，消除虚假崩溃堆栈误报）**：① **Warning 处理器优雅拦截**：在 `db-prod/apply_sql.py` 中重载 `warnings.formatwarning` 与 `warnings.showwarning`，剥离底层 `aiomysql/cursors.py:239 await self._query(query)` 代码调用行与绝对路径；将带有 `IF NOT EXISTS` 时触发的表已存在、库已存在等幂等信息转化为单行 `   ℹ️  [跳过已存在] Table 'xxx' already exists`，其余真实告警转化为 `   ⚠️  [MySQL 提示] ...`，统一流向控制台，消除乱序交错；② **自动建库前置存在性探测**：在无库连接阶段先查询 `information_schema.SCHEMATA`，已存在时直接跳过 `CREATE DATABASE` 操作，彻底消除每个迁移文件执行时重复打印 `Can't create database 'aiagent'; database exists` 警告刷屏的痛点；③ **真实错误严格抛出**：所有语法、字段、外键、网络等真实 Error 依旧正常捕获并打印红色的 `❌ 执行失败`，并立即调用 `sys.exit(1)` 退出；④ **单测全覆盖**：在 `tests/test_db_prod_apply_sql.py` 中新增 `test_warning_formatting_renders_clean_cli_messages` 单测，全量 21 项测试全绿通过。 | ✅ 21 项单测全绿通过；未代跑真实服务与启动脚本 | 2026-09-12 |
| 元数据全量定时物理巡检物理表级缺失（missing_tables_count）指标汇聚、结论文案校准与告警通知触发修复 (Metadata Scheduled Consistency Inspection Physical Table Missing Alert Notification & Summary Bugfix) | `app/services/ai/scheduler_service.py`, `tests/api/test_metadata_cron_inspection.py`, `tests/CHECKLIST.md` | **元数据定时巡检表缺失指标汇聚与告警通知派发闭环（彻底根除当物理库表被 DROP 缺失时定时巡检误判为完全一致且跳过通知的缺陷）**：① **补全表缺失指标提取与初始化**：在 `scheduler_service.py` 的 `metadata_inspection` 系统任务分支中，新增 `missing_tbl_cnt = res.get("missing_tables_count", 0)` 解析；② **修正无差异判定与汇报文案**：判定条件由仅依赖字段缺失扩展为 `missing_tbl_cnt == 0 and stale_cnt == 0 and new_cnt == 0 and mismatch_cnt == 0 and failed_cnt == 0`，避免表缺失时被误判为“物理结构完全一致，无漂移差异”；同时在检出差异时优先拼接 `f"{missing_tbl_cnt} 张表物理缺失"`；③ **补全告警通知派发判定**：在 `has_drift_alert` 中补入 `or missing_tbl_cnt > 0`，确保检出物理表缺失时 100% 触发站内信（portal）与外部渠道（钉钉/企微/飞书/邮件）的告警消息，消除免打扰误拦截；④ **日志打印全要素补齐**：物理比对结束与免打扰跳过日志中统一展示表缺失计数；⑤ **新增自动化单测验证**：在 `tests/api/test_metadata_cron_inspection.py` 中新增 `test_scheduled_metadata_inspection_triggers_alert_on_missing_tables`，完整验证表缺失检出时结论文案包含缺失表、站内信与钉钉告警派发均被正确触发。 | ✅ 新增定向单测通过；未代跑真实服务与启动脚本 | 2026-09-11 |
| 元数据物理表级缺失巡检、整表下线人机协同与数据集变更日志全链路贯通体系（支持探测物理已删除表 table_missing_in_db、单项「下线整表」与级联清理、一键下线全部缺失表、抽屉差异类型 Pills 过滤标签与实时数量徽标、表名/字段名即时搜索、全链路贯通数据集变更日志 Changelog 与字段级 Diff 对比、操作人审计溯源、修复巡检告警通知站内信与群机器人标题重复两遍问题、剔除过时手动同步 RAGFlow 提示并明确已自动同步向量库反馈、流式终端与指标呈现、本地 Redis 向量自动同步） (Metadata Physical Table-Missing Inspection, Complete Table Offline, Changelog Audit & Drawer Filter Search HITL Console) | `app/schemas/metadata.py`, `app/services/metadata_drift_service.py`, `app/services/metadata_inspection_service.py`, `app/services/ai/scheduler_service.py`, `app/api/portal/endpoints/metadata.py`, `frontend/src/api/metadata.ts`, `frontend/src/components/metadata/MetadataDriftAlertsDrawer.vue`, `tests/services/test_metadata_drift_service.py`, `tests/services/test_metadata_inspection_service.py`, `tests/CHECKLIST.md` | **元数据物理表级缺失（table_missing_in_db）巡检、整表下线、变更日志全链路打通与差异分类过滤闭环体系（彻底根除物理库已 DROP TABLE 时巡检盲区、治理操作无审计日志记录缺陷，实现人机协同精准处置与全生命周期溯源）**：① **物理库现存表快速集合比对**：巡检逐表扫描前，优先通过数据源适配器 `adapter.get_tables()` 获取物理库实时存活的全部物理表集合；若纳管的元数据表不存在于物理表中，精准判定为「物理表缺失（整表已删除）」，并跳过后续无谓且易报错的列探测；同时支持在降级模式下从 `get_columns` 的 "doesn't exist" 等底层异常信息智能兜底识别；② **高危表级告警记录与指标呈现**：生成 `drift_type="table_missing_in_db"`, `column_name="*"` 告警，流式日志输出 `⚠️ 发现物理表已不存在: {phys_name}`；单数据集与全库巡检报告汇总中新增 `N 张表物理缺失` 专属统计指标；③ **整表安全下线处置动作（`drop_table`）**：后端扩展 `drop_table` 处置动作，从当前元数据集中下线并删除目标 `MetaTable`（ORM 自动级联清理下属 columns）；同时**将该表关联的所有历史待处理告警一并自动流转为已处置（`status=1`）**，避免残留孤儿告警；④ **全链路打通数据集变更日志 (Changelog) 与 Diff 审计**：在 `MetadataDriftService` 中对下线整表（`delete`）、下线字段（`update`）、录入新增字段（`update`）及同步物理类型（`update`）全量调用 `ChangelogService.log_change`，自动写入当前操作人（`user_id` / `user_name`）与结构化原因（如 `元数据巡检：下线物理缺失表 orders` / `元数据巡检：下线物理缺失字段 orders.col`）；自动计算 `columns` 差异并在数据集详情页【变更日志】Tab 中提供高辨识度的时间线节点与字段级红绿 Diff 对比；同步调用 `_mark_dataset_as_modified` 刷新数据集更新时间；⑤ **修复告警通知（站内信/机器人）标题重复展示两遍缺陷**：定位并剔除 `scheduler_service.py` 在 `body_lines` 正文首行冗余拼接的 `f"### {alert_title}"`，与 `title` 字段彻底解耦，彻底根除站内信卡片与钉钉/企微/飞书通知首部标题出现两次的冗余展示问题；⑥ **本地 Redis 向量自动级联同步与过时 Toast 提示纠正**：整表下线/字段变更后自动触发 `_try_sync_local_vector`，让大模型在后续语义检索中立即生效；**彻底移除后端历史遗留的「建议随后点击同步到 RAGFlow」误导文案**，统一校准为「（已自动同步向量知识库）」明确反馈，免除用户疑虑；⑦ **抽屉差异类型分类 Pills 标签与即时搜索**：抽屉顶部新增精致分类胶囊标签（`全部 (N)`、`🏢 物理表缺失 (N)`、`🗑️ 字段缺失 (N)`、`🔄 类型不匹配 (N)`、`📥 物理新增字段 (N)`），各标签实时展示匹配数量徽标；新增表名/字段名/数据集名即时模糊搜索框（带清空按钮）；顶部批量操作栏与当前选中的分类智能联动聚焦；无匹配结果时展示优雅空状态并支持一键重置过滤；⑧ **前端抽屉高危卡片与一键下线**：缺失表告警卡片以醒目红色主题高亮展示，标明 `[整表物理缺失]` 与 `⚠️ 物理表已不存在` 专属徽章；单项操作区支持【下线整表】二次确认弹窗；顶部批量操作栏新增【🏢 一键下线全部缺失表 (N)】快捷通道；⑨ **全量自动化单测与类型安全**：新增 `test_resolve_alert_records_changelog` 等测试用例，24 项后端漂移与巡检单测 100% 绿灯全过；`vue-tsc --noEmit` 0 报错通过。 | ✅ 后端 24 项单测全绿；前端 `vue-tsc` 0 报错；未代跑真实服务与启动脚本 | 2026-09-11 |
| 元数据巡检物理类型不匹配（type_mismatch）智能校准体系（支持单项处置「同步物理类型」、顶部一键批量同步全量类型差异、双向适配器探测与正则容错兜底、已处置状态流转与本地向量知识库级联同步） (Metadata Schema Drift Type Mismatch Ingestion & Batch Calibration HITL Console) | `app/schemas/metadata.py`, `app/services/metadata_drift_service.py`, `frontend/src/api/metadata.ts`, `frontend/src/components/metadata/MetadataDriftAlertsDrawer.vue`, `tests/services/test_metadata_drift_service.py`, `tests/CHECKLIST.md` | **元数据巡检类型不匹配（type_mismatch）处置与一键校准体系（告别只能点击忽略的痛点，实现人机协同精准同步真实物理类型）**：① **告警动作扩展支持 `sync_type`**：在 `DriftResolutionAction` 中新增 `"sync_type"` 动作，前后端类型完全对齐；② **双重策略精准提取物理库真实类型**：处置时优先通过数据源连接池适配器实时探测物理表对应列的真实数据类型；若外部数据源暂时离线或未连通，自动通过正则从告警异常样本（`error_sample` 如 `巡检发现类型不匹配：元数据声明为 String，物理库实际为 tinyint`）中提取物理类型进行智能兜底，确保离线或沙箱环境 100% 成功；③ **元数据模型校准与告警已解决状态流转**：将元数据字段声明类型更新为物理库真实类型（`col.type = target_type`），告警状态由待处理（`status=0`）流转为已处置（`status=1`），并记录已解决时间；④ **本地向量知识库级联重新同步**：将 `sync_type` 纳入本地 Redis 向量刷新链条，在字段类型变更后自动触发 `_try_sync_local_vector`，确保大模型检索到的元数据与物理库始终保持最新一致；⑤ **前端一站式交互与视觉升级**：针对 `type_mismatch` 告警卡片展示专属 Indigo 蓝紫色主题边框与 `物理类型不匹配` 徽章；在单项操作区新增【同步物理类型】按钮，支持弹窗展示原类型与目标类型二次确认；顶部操作栏新增【🔄 一键同步全部类型差异 (N)】按钮，支持一键批量校准；⑥ **自动化单测与静态类型全覆盖**：后端新增 `test_resolve_alert_sync_type` 定向测试，16 项漂移单测全部绿灯；前端 `vue-tsc --noEmit` 0 报错通过。 | ✅ 后端 16 项单测全绿；前端 `vue-tsc` 0 报错；未代跑真实服务与启动脚本 | 2026-09-11 |
| 元数据全量定时物理结构一致性巡检体系（右上角下拉菜单【定时巡检】入口、常用周期预设与自定义 Cron 配置、调度引擎系统旁路执行零 Token 消耗、状态与健康指标面板、异常漂移告警通知渠道可选、站内信强制勾选、任务中心专属系统徽标与只读保护、AI Review 5 项专项修复闭环、仅扫描开启状态数据集 active_only 隔离维护期、Admin 管理员专属权限强管控、执行历史回溯与步骤 Trace 完整落库） (Metadata Scheduled Consistency Inspection with System Protection in TaskCenter, Active-Only Guard, Alert Notifications, Admin Permission Guard & Execution History Trace) | `app/schemas/metadata.py`, `app/schemas/task.py`, `app/models/task.py`, `app/services/ai/scheduler_service.py`, `app/api/portal/endpoints/metadata.py`, `app/services/ai/runners/data_agent_runner.py`, `app/services/metadata_inspection_service.py`, `app/services/metadata_drift_service.py`, `frontend/src/api/metadata.ts`, `frontend/src/api/task.ts`, `frontend/src/components/metadata/MetadataCronInspectionModal.vue`, `frontend/src/views/MetadataDatasets.vue`, `frontend/src/views/TaskCenter.vue`, `tests/api/test_metadata_cron_inspection.py`, `tests/services/test_metadata_inspection_service.py`, `tests/services/test_metadata_drift_service.py`, `tests/CHECKLIST.md` | **元数据全量定时巡检与任务中心无缝协同体系（含 AI Code Review 5 项专项缺陷与体验加固、仅巡检开启数据集防护、异常告警通知按需勾选且站内信必选、Admin 管理员专属权限强管控、执行历史回溯与步骤 Trace 完整落库）**：① **元数据管理页右上角专属菜单入口与配置弹窗（仅 Admin 可见可点）**：在右上角新建菜单下收拢【⏱️ 定时巡检】入口，受 `v-if="isAdmin"` 保护仅管理员可见，URL query 参数自动唤起与模态框挂载均增加 `isAdmin` 强校验；弹窗提供启用/暂停开关（鲜明翡翠绿勾标与浅灰叉标辨识）、预设快捷周期（每日 2 点/4 点/周一/每 6 小时）与 5 位标准 Cron 自由设定；展示累计巡检次数、运行健康度、上次执行时间与结果汇报；支持一键「⚡ 立即运行一次」与直通任务中心；② **异常告警通知渠道（站内信强制必选 + 钉钉/企微/飞书/邮件可选）**：弹窗中提供通知渠道面板，**站内信始终强制勾选锁定**（平台基础保障），用户可自由组合勾选钉钉、企业微信、飞书机器人或邮件；**严格遵守「仅当检出漂移异常或执行错误时才发通知」**，全库一致正常时不产生打扰；③ **调度引擎原生系统任务旁路执行（零 Token 消耗）与审计历史 Trace 完整落库**：在 `_scheduled_task_wrapper` 中识别 `task_type == "metadata_inspection"` 系统级任务，旁路 Agent 与大模型调用，直接异步触发 `MetadataInspectionService.inspect_all_datasets` 直连物理数据库完成结构扫描，并将检出的字段差异沉淀至漂移大盘，零 Token 消耗且更新执行指标；每次执行自动生成专属 `trace_id` 与 `run_conversation_id`，向 `agent_execution_history` 与 `agent_execution_trace` 写入详尽结构化审计日志与执行步骤（耗时、扫描表数、差异结果），使得任务中心「执行历史回溯」抽屉与全局执行记录完美呈现；④ **仅扫描开启状态数据集防护 (active_only=True)**：在全量批量巡检与定时巡检中加入 `where(MetaDataset.status == 1)` 过滤，自动跳过维护期与已禁用的数据集，避免对下线或维护中数据库发起无谓探测，并在日志与摘要汇报中明确标识扫描范围；⑤ **任务中心专属系统级保护与操作适配（齿轮按钮仅 Admin 可见）**：任务中心识别 `metadata_inspection` 任务类型，展示 `⚡ 系统 · 元数据巡检` 专属紫蓝色徽标与顶部分类过滤 Tab；**严格禁止在任务中心编辑与删除**，卡片与列表视图中的齿轮配置按钮仅对 `userInfo.role === 'admin'` 用户外显；⑥ **后端接口 Admin 权限强校验防越权**：`/cron-inspection` 查询、保存与立即运行接口统一绑定 `dependencies=[Depends(require_admin)]`，非 admin 请求直接抛出 403 Forbidden，杜绝接口绕过；⑦ **AI Code Review 5 项专项缺陷深度加固**：(1) Cron 巡检 task_id 注册防静默吞日志；(2) 运行时反哺异步任务统一 await 回收；(3) 激活列类型不一致检测 `type_mismatch`；(4) 统一权限为 `menu:metadata`；(5) 处置告警后自动刷新本地向量与缓存；⑧ **自动化测试全覆盖**：后端 13 项定时巡检与权限单测 100% 绿灯全过，前端 `vue-tsc` 类型检查通过。 | ✅ 后端 22 项单测全绿；前端 `vue-tsc` 0 报错；未代跑真实服务与启动脚本 | 2026-09-11 |
| 元数据管理 Schema 漂移异常提醒与物理结构流式巡检人机协同体系（支持右上角新建菜单下全局巡检入口、全量大盘统筹视图、一键全库批量巡检、物理新增字段一键录入与批量收录、操作权限隔离、首页全局漂移预警横幅、巡检与治理一站式控制台与禁用状态安全防护） (Metadata Schema Drift Feedback, Streaming Inspection & Ingestion HITL Console with Global Dashboard, Batch Inspection & Banner) | `db-prod/V154-create_meta_schema_drift_alerts.sql`, `db-prod-pg/V55-create_meta_schema_drift_alerts.sql`, `app/models/metadata.py`, `app/schemas/metadata.py`, `app/services/metadata_drift_service.py`, `app/services/metadata_inspection_service.py`, `app/services/ai/runners/chatbi/tool_result_handlers.py`, `app/api/portal/endpoints/metadata.py`, `frontend/src/api/metadata.ts`, `frontend/src/components/metadata/MetadataInspectionModal.vue`, `frontend/src/components/metadata/MetadataDriftAlertsDrawer.vue`, `frontend/src/views/MetadataDatasets.vue`, `frontend/src/views/MetadataTables.vue`, `tests/services/test_metadata_drift_service.py`, `tests/services/test_metadata_inspection_service.py`, `tests/api/portal/test_metadata_drift_contract.py`, `tests/CHECKLIST.md` | **元数据 Schema 漂移闭环告警、全量大盘统筹、全库批量物理巡检与双向人机协同处置体系（绝不擅自篡改线上元数据，Human-in-the-loop 人机协同、精细化权限隔离与一站式治理体验）**：① **右上角菜单全局巡检入口与统筹大盘**：在元数据管理页右上角【+ 新建数据集】下拉菜单内收拢「⚡ 全局巡检」入口，实时展示全库待处理漂移项数量徽标，避免多入口冗余；支持跨数据集统一查看漂移报告，提供按数据集下拉筛选过滤与异常卡片所属数据集标签标识；② **一键全量批量巡检**：后端提供 `POST /api/portal/metadata/inspect-all` 任务与 `GET /api/portal/metadata/inspect/{task_id}/events` 流式推流，前端控制台逐行实时显示全库每个数据集的比对进展并汇总输出全库体检报告；跨数据集支持一键批量处置；③ **运行时物理报错反哺元数据（闭环告警与首页全局横幅提醒）**：ChatBI 执行 SQL 触发物理库 Unknown column 等错误并在方案 A 剔除失效字段后，通过异步非阻塞通道反哺至 `MetadataDriftService`，自动匹配元数据表与列，幂等生成或累加 `pending` 状态的漂移告警；**首页全局横幅预警**：在元数据管理主页顶部常驻柔和警示横幅（`⚠️ Schema 结构漂移待办提醒：N 个数据集 · 共 M 项待治理`），特别注明 AI 运行时物理报错自动反哺与结构巡检发现，提供一键直达抽屉查看；④ **统一常驻入口与一站式治理控制台（告别入口隐蔽与盲目自动巡检）**：在数据集卡片、列表行与详情页将入口统一收拢为「⚡ Schema 巡检与差异治理」，打开后默认呈现差异治理报告（清晰标明 `⚡ 运行时物理报错反哺` 与 `🔍 结构巡检发现`，不再强制盲跑巡检）；⑤ **抽屉内嵌流式巡检终端与禁用状态维护期探测**：在控制台顶部内嵌物理结构巡检卡片，用户按需点击【⚡ 立即执行巡检】后才展开终端流式日志与进度条；支持在禁用维护隔离期直接执行安全巡检；⑥ **双向人机协同精准处置与严格编辑权限隔离**：支持单项【添加到数据集】、【下线该字段】、【忽略】，以及顶部的【一键收录全部新增字段】与【一键下线全部缺失字段】；无编辑权限时按钮禁用并 tooltip 提示；规范使用平台 `<ConfirmModal>` 居中确认框与 `useToast` 提示；⑦ **全量测试与契约保证**：后端 10 项单测全绿；前端无类型冲突；双库迁移 SQL 落盘。 | ✅ 后端 28 项单测全绿；未代跑真实服务与启动脚本 | 2026-09-11 |
| ChatBI 物理数据库报错驱动的 Schema 过时字段剔除与一次性重查纠正（方案 A：以数据库为准，剔除过时字段后重查） (ChatBI Database-Error-Driven Schema Stale Column Drop & One-Off Query Repair) | `app/services/ai/runners/chatbi/schema_stale_correction.py`, `app/services/ai/runners/chatbi/tool_result_handlers.py`, `app/services/ai/runners/chatbi/repair_policy.py`, `app/services/ai/runners/chatbi/run_state.py`, `app/services/ai/runners/chatbi/constants.py`, `tests/ai/runners/test_schema_stale_correction.py`, `tests/CHECKLIST.md` | **物理报错驱动的过时 Schema 字段剔除与重查闭环（方案 A）**：① **真实物理报错驱动（Database Wins）**：当底层表变更导致 SQL 报错 `unknown column` / `does not exist` 时，严格对照当前已声明 Schema。仅对 Schema 中已声明但数据库报不存在的列判定为「真实过时列」予以剔除，未声明列判定为模型编造/未知列走常规纠错，杜绝被模型幻觉诱导删列；② **单表多失效列与表前缀精准消歧**：支持同一 SQL 涉及同表多个失效列时的批量安全剔除，避免字典覆盖；支持带 `table.column` 前缀时优先精准匹配目标表，配合确定性排序杜绝同名字段跨表误删；③ **内存态 Schema 重构与 Preflight 本地网关防线闭环**：基于当前 table_bindings 结构化重建当轮可见 Schema 文本，并同步更新内存态 `state.schema_table_columns`，使得大模型下一轮若再次幻觉生成该失效列时，由本地 Preflight 网关直接拦截，不浪费真实数据库往返开销；④ **一次性广播修复与双重熔断**：新增 `stale_column_corrected` 修复策略并强制绑定 `ToolChoice(mode="execute_sql_query")`，配合 `stale_repair_consumed` 消费标记与预算控制（1 次）实现熔断，重试未成功则回退兜底，杜绝死循环；⑤ **单元测试全量覆盖**：新增 12 项单元测试覆盖驱动条件、真实过时 vs 编造列区分、多列剔除、表前缀消歧、修复策略/熔断转换及 `apply_sql_tool_result` 联动更新。 | ✅ 后端 12 项定向单测全量通过；未代跑真实服务与启动脚本 | 2026-09-11 |
| 委派智能体与子代理时间线合二为一、状态统一收拢与心跳噪音过滤 (Subagent Tool & Lifecycle Timeline Container Consolidation & Heartbeat Denoising) | `frontend/src/utils/processTimeline.ts`, `frontend/src/components/chat/ChatExecutionTimeline.vue`, `frontend/scripts/processTimeline.test.ts`, `tests/CHECKLIST.md` | **委派智能体与子代理时间线合体及降噪闭环**：① **二合一归一化合并**：消除原先「委派智能体」工具调用与「调用子代理」生命周期事件割裂展示为两条独立项的缺陷，在 `upsertTimelineLog` 与 `reorganizeSubagentItems` 中实现双向合并，统一收拢为单个父级容器 `委派智能体 · {智能体名称}`；② **状态与耗时统一挂载**：父级卡片右侧直接联动子智能体实时状态（`进行中 ↻`）与完成总耗时（如 `4.8s`），告别生命周期条悬挂底部的层级倒置；③ **术语全链路统一**：彻底收敛“子代理”杂合称谓，时间线全链路统一规范为“委派智能体”；④ **过滤无信息量心跳日志**：在日志追加与解析阶段自动过滤 `✨ 开始生成回复` 内部流式心跳日志，保持步骤清晰紧凑；⑤ **测试全量通过**：前端单元测试 3 项全绿，契约测试 79 项全绿，`vue-tsc --noEmit` 零报错通过。 | ✅ 前端 79 项契约与行为测试全绿；前端单测通过；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-11 |
| 我的工作台 PersonalWorkbench 全面体验升级（常用助手置顶 Pin、资产卡片专属微图标与深色模式全量适配） (PersonalWorkbench UI/UX Overhaul: Pinned Agents, Icon Badges & Full Dark Mode Support) | `frontend/src/views/PersonalWorkbench.vue`, `frontend/src/components/workbench/WorkbenchPersonalResources.vue`, `frontend/src/components/workbench/WorkbenchAgents.vue`, `frontend/src/components/workbench/WorkbenchResume.vue`, `frontend/src/components/workbench/WorkbenchResults.vue`, `frontend/src/components/workbench/WorkbenchTasks.vue`, `frontend/src/components/workbench/WorkbenchAttention.vue`, `frontend/src/components/workbench/WorkbenchRunning.vue`, `frontend/src/components/workbench/WorkbenchNextScheduled.vue`, `frontend/src/components/workbench/WorkbenchItemMeta.vue`, `frontend/src/components/workbench/WorkbenchSectionHeader.vue`, `tests/frontend/test_personal_workbench_contract.py`, `tests/CHECKLIST.md` | **我的工作台 UI/UX 全面升级与体验闭环**：① **常用助手置顶收藏 (Pin Agents)**：在 `WorkbenchAgents.vue` 中为助手卡片新增快捷 Pin/Unpin 图钉交互，基于本地 `localStorage` 记忆并在列表中置顶优先展示，附带精致微徽标；② **个人资产卡片视觉立体化与微图标**：在 `WorkbenchPersonalResources.vue` 中为记忆、Token、数据、技能、MCP、任务分别配置专属微图标背景色与卡片 hover 上浮阴影（hover:-translate-y-0.5 hover:shadow-md）；③ **全量深色模式 (Dark Mode) 深度适配**：对工作台主视图及全部子组件（待处理、最近会话、最近产出、最近任务、下次调度、运行中等）补齐高质感 `dark:*` 类名；④ **状态指示呼吸微动效**：状态标签新增呼吸微光点；⑤ **严格保持契约测试**：`tests/frontend/test_personal_workbench_contract.py` 11 项契约测试 100% 绿灯全过，`vue-tsc --noEmit` 零报错通过。 | ✅ 前端 11 项工作台契约测试全绿通过；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-11 |
| 工具调用确认卡片 ToolPermissionCard UI/UX 轻量化重构（移除冗余双方格、聚焦终端命令、完结退火浅灰与折叠降噪） (ToolPermissionCard UI/UX Slimming & Post-Resolution Auto-Fade UX) | `frontend/src/components/chat/ToolPermissionCard.vue`, `tests/frontend/test_execution_stage_card_contract.py`, `tests/CHECKLIST.md` | **工具调用确认卡片 UI/UX 全面瘦身与体验闭环**：① **移除冗余双大方格与套话**：将原先占高约 90px 的「风险等级」与「影响范围」两个独立白色大卡片及「执行范围取决于工具权限」套话彻底移除，升级为行内紧凑微徽标条（`风险等级: 需要确认 · 影响范围: 当前运行环境 · 仅本次执行`），垂直空间压缩 70%；② **核心命令代码块终端化**：等宽字体终端块直接外显，支持语法断行、高亮及右上角一键复制即时反馈；③ **完结状态退火与降噪**：一旦用户做出决断（已允许/已拒绝/已过期），卡片背景与边框从刺眼的高警示琥珀黄自动降级为低调优雅的中性灰，折叠状态下仅保留一行轻量概要；④ **按钮精致化**：从粗笨的 44px 撑满药丸按钮调整为桌面端 32px 紧凑方钮并保持移动端触控友好；⑤ **契约与单测护航**：覆盖 79 项前端契约测试 100% 绿灯，`vue-tsc --noEmit` 零报错通过。 | ✅ 前端 79 项契约与行为单测全绿；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-11 |
| 经验库检索工具 search_qa_examples 纳入运行时只读白名单（推断权限 read、免人工审批弹窗） (Tool search_qa_examples Added to Runtime READ_ONLY_TOOL_NAMES WhiteList) | `app/services/ai/runtime/agentscope/tools.py`, `tests/ai/tools/test_knowledge_tool.py`, `tests/CHECKLIST.md` | **经验库检索工具运行时只读化闭环**：① **只读白名单补全**：在 `app/services/ai/runtime/agentscope/tools.py` 的 `READ_ONLY_TOOL_NAMES` 集合中加入 `"search_qa_examples"`，使得 `infer_runtime_permission_scope` 统一将其推断为 `read` 只读权限；② **避免误报审批**：与 `tool_policy.py` 中的 `side_effect="read"` 及 `confirmation="none"` 严格对齐，彻底消除运行时因权限推断缺失而弹出「工具调用确认 / 审批」弹窗的缺陷；③ **自动化测试**：在 `tests/ai/tools/test_knowledge_tool.py` 中新增 `test_search_qa_examples_is_read_only_no_runtime_confirm`，断言白名单归属、推断只读范围以及运行时 Spec 的 `permission_scope == "read"`。 | ✅ 知识库工具定向单测 14 项全绿通过；未代跑真实服务与启动脚本 | 2026-09-11 |
| 沙箱预热进度提示与鉴权能力准备父级完成后自动折叠（分阶段文案、已等待秒数、不确定进度条、契约防挤占） (Sandbox Workspace Prewarm Progress & Auth Capability Timeline Auto-Fold) | `frontend/src/components/chat/ChatExecutionTimeline.vue`, `frontend/src/utils/processTimeline.ts`, `tests/frontend/test_chat_shared_helpers_behavior.py`, `tests/frontend/test_execution_stage_card_contract.py`, `tests/CHECKLIST.md` | **沙箱预热进度提示与准备父级自动折叠闭环**：① **沙箱预热进行中文案与进度提示**：在执行时间线中针对 `workspace:sandbox` 挂起（pending）状态，显示已等待秒数、分阶段安抚文案（申请隔离资源配置、初始化沙箱工作区、拉取镜像与启动环境、提示耗时最长可达 60 秒）及不确定进度条，避免冷启动与镜像拉取期间界面停滞感；② **准备父级完成后自动折叠**：「鉴权及上下文与能力准备」父级默认展开，执行完成（success/failed/error 等非 pending）后自动折叠子树仅保留一行概览标题，使最终界面更紧凑干净（仅针对准备父级 guard，不影响 route 等其它执行父级）；③ **防挤占与回归保护**：在子行插入沙箱预热进度提示时保留原"错误原因"与文件元数据展示；④ **契约与单测护航**：在 `processTimeline.ts` 抽象纯函数判定与阶段文案生成，新增 4 项前端行为与契约测试断言，`vue-tsc --noEmit` 零报错通过。 | ✅ 前端 79 项契约与行为单测全绿；`vue-tsc --noEmit` 0 报错；未代跑真实服务与启动脚本 | 2026-09-11 |
| ChatBI 完整明细导出通道（Excel/CSV/Markdown 三格式、直链下载、首次使用引导浮标） (ChatBI Full Detail Export Channel) | `app/api/portal/endpoints/chatbi_export.py`, `app/api/portal/api.py`, `app/services/ai/tools/data_api.py`, `app/services/sql_query_execution_service.py`, `frontend/src/components/chatbi/ChatBIInsightPanel.vue`, `tests/api/portal/test_chatbi_export.py`, `tests/frontend/test_chatbi_export_contract.py`, `tests/CHECKLIST.md` | **ChatBI 明细 Tab 完整明细导出闭环（绕过 LLM 令牌层直连全量数据）**：① **后端导出端点**：新增 `POST /api/portal/chatbi-export/result`，复用 `execute_sql_query_core(include_total=False, for_export=True)` 以当前用户身份鉴权并按缓存键 `export_full` 绕过令牌层直取全量结果，导出行上限 `MAX_EXPORT_SQL_ROWS=100000`；对联邦查询（cross-dataset 合并）返回 400 并附「暂不支持完整明细导出」复制文案；对非只读 SQL（仅白名单 SELECT/WITH/EXPLAIN/SHOW 等）返回 400；② **三格式导出**：Excel（openpyxl）/ CSV（utf-8-sig 保证 Excel 中文不乱码）/ Markdown（手写转义 `\|` 表格，无额外依赖）；导出文件落盘于 `{workspace_root}/{user_key}/export/chatbi_export_*.{ext}` 并经 `register_artifact` 注册生成限时直链下载；返回 `row_count / truncated / export_limit` 供前端提示截断；③ **前端交互**：明细 Tab 顶部联邦查询展示琥珀色不支持提示；导出按钮改写为下拉菜单（w-80 三格式 XLSX/CSV/MD），禁用态带 `export_disabled_reason` 悬停说明；新增 `openExportMenu / pickExportFormat` 全流程（POST 导出→解析直链→`link.click()` 下载→按 row_count toast 提示）；④ **首次使用引导浮标**：参照 EmbedChat 快捷指令折叠气泡样式，首次进入明细 Tab 展示引导气泡（localStorage 记忆、4.5s 自动隐藏、可手动关闭），引导用户使用完整明细导出；⑤ **前端契约测试**：`test_chatbi_export_contract.py` 断言导出通道复用同一执行入口并携带 `for_export` 标记、端点注册于 `chatbi-export` 路由、前端暴露 `openExportMenu/pickExportFormat` 三格式常量与引导浮标 `showExportHint` 的 v-if 条件；⑥ 后端 `test_chatbi_export.py` 定向覆盖 limit 比值/`_extract_rows_columns` 变体/`_user_dimensions`/请求模型默认值与 `format="md"` 校验/`_md_escape` 转义。| ✅ 后端 11 项定向单测 + 前端 3 项契约测试全绿；`vue-tsc --noEmit` 零报错；未代跑真实服务与启动脚本 | 2026-09-10 |
| Kubernetes 沙箱输入框浮标 Pod 状态/启停控制（状态展示、运行时长、手动启动/停止/重启与停止二次确认） (K8s Sandbox Input Badge Pod Status & Lifecycle Controls) | `app/services/ai/runtime/agentscope/k8s_workspace.py`, `app/services/ai/runtime/agentscope/workspace.py`, `app/api/v1/endpoints/sandbox.py`, `frontend/src/components/embed/ChatInput.vue`, `frontend/src/views/EmbedChat.vue`, `frontend/src/components/chat/DockerWorkspaceBanner.vue`, `tests/services/test_sandbox_policy_k8s.py`, `tests/api/v1/test_sandbox_k8s_workspace_ops.py`, `tests/frontend/test_chat_sandbox_workspace_contract.py`, `docs/superpowers/specs/2026-09-09-k8s-sandbox-workspace-badge-controls-design.md`, `tests/CHECKLIST.md` | **K8s 沙箱浮标与 Docker 对等的 Pod 生命周期闭环**：① **后端只读状态查询辅助**：`read_k8s_sandbox_pod` 通过 kubernetes-asyncio 只读探测 Pod phase/startTime，SDK 缺失/无凭据/RBAC 受限/404 均降级不抛 500；② **k8s runtime 族**：`k8s_workspace_status`（只读、未缓存返回 idle、实时 Pod phase 优先、降级 is_alive 视图）、`ensure_k8s_workspace`（走 `get_local_workspace` 与聊天同缓存键预热）、`stop_k8s_workspace`（逐出缓存并关闭 Pod）、`restart_k8s_workspace`（重建）与元数据函数，策略不符/无会话/无身份统一抛 `K8sSandboxUnavailableError`；③ **用户端点**：`/api/v1/sandbox/k8s/workspace/{status,ensure,stop,restart}`，仅当前登录用户操作自身会话；④ **前端通用化**：`dockerWorkspace*` 更名 `sandboxWorkspace*` 并新增 `sandboxBackend`（docker|k8s）术语维度；浮标展示 Pod 状态点/Pod 名/每秒滚动运行时长/空闲 30m 自动回收提示，k8s「操作」菜单仅重启 Pod/停止关机（无终端）；⑤ **k8s 停止二次确认**：复用 EmbedChat `<ConfirmModal>`，文案警示动态独立卷随 PVC 删除与重建耗时；⑥ **Docker 回归**：docker 浮标文案/交互与既有端点契约不变。 | ✅ 后端 11 项定向单测（Pod 探测 4 项 + runtime 7 项）+ 5 项端点单测 + 前端契约测试全绿；`vue-tsc --noEmit` 零报错；未代跑真实服务与启动脚本 | 2026-09-09 |
| Kubernetes 向导式部署安装器 install.sh（环境自检/镜像探测/演练模式/幂等下发）、运维脚本 nanzi-k8s.sh 与镜像滚动升级指南 (Kubernetes Interactive Wizard Installer install.sh, nanzi-k8s.sh Ops Tool & upgrade.md) | `k8s_deploy/install.sh`, `k8s_deploy/nanzi-k8s.sh`, `k8s_deploy/upgrade.md`, `k8s_deploy/README.md`, `tests/test_k8s_deploy_docs_contract.py`, `tests/CHECKLIST.md` | **Kubernetes 部署向导与运维管理工具全套落地闭环**：① **向导式交互安装器 install.sh**：支持环境连通性自检、当前节点容器运行时（K3s containerd / crictl / docker）镜像智能探测与 Tag 自动推荐提取、未导入时的单行导入命令避坑提示（解决 docker load 无法被 K3s containerd 读取的问题）；支持逐项确认或回车直接沿用默认值；支持 `--try` / `--dry-run` 模拟演练（仅做本地 YAML 语法预检与 Kustomize 静态渲染，绝不下发真实集群变更）；支持 `-y` 免交互快速部署；所有资源下发天然安全幂等；② **运维管理快捷脚本 nanzi-k8s.sh**：支持 `status`（汇总展示 K3s、节点、主平台及 `agent-sandboxes` 沙箱命名空间 Pod/PVC 状态）、`sandboxes`（专门监控沙箱资源）、`restart-pod`（平滑滚动重启并等待就绪）、`restart-k3s`（重启服务并探测 API 自动恢复）、`restart-all`、`logs`、`events`、`test`；内置 POSIX 兼容层（支持 `sh` / `bash` / `./` 运行）；③ **镜像滚动升级指南 upgrade.md**：系统梳理 Tag 变动与不变场景下的平滑更新操作；④ **文档与契约测试**：更新 `k8s_deploy/README.md`，并在 `tests/test_k8s_deploy_docs_contract.py` 中补充契约断言，确保脚本与文档完整性。 | ✅ 文档契约测试断言通过；脚本全量就绪并赋权 777；未代跑真实服务与启动脚本 | 2026-09-09 |
| 会话中断恢复与二次工具审批 awaiting_permission 状态防覆盖及首段流完成防抖守卫 (Chat Continuation awaiting_permission Status Guard & Processing Lock) | `frontend/src/views/EmbedChat.vue`, `frontend/src/views/AgentDebug.vue`, `tests/frontend/test_chat_continuation_dedup_contract.py`, `tests/CHECKLIST.md` | **会话中断恢复与二次审批状态保持闭环**：① **二次审批 awaiting_permission 状态防覆盖**：修复在 EmbedChat 与 AgentDebug 中，当收到后端 `run_status` 为 `awaiting_permission` 时被错误映射为已完成（approved）导致新卡片状态失效的问题，显式将其映射为 `pending` 并默认展开；② **首段流结束防并发重复发送**：在 `sendMessageInternal` 的 `finally` 块中，检测若当前仍有待处理的权限审批（`pendingPermission`）或外部执行（`pendingExternalExecution`），保持 `isProcessing.value = true`，防止用户在等待确认期间重复提交打断链路；③ **前端契约测试保护**：在 `tests/frontend/test_chat_continuation_dedup_contract.py` 中新增 `test_permission_resume_keeps_a_followup_request_pending` 与 `test_initial_stream_keeps_composer_busy_while_continuation_is_pending` 契约断言。 | ✅ 契约测试覆盖；未代跑真实服务与启动脚本 | 2026-09-09 |
| K8s 部署文档 K3s 宿主机 cgroup v1 导致 Kubelet 循环重启排查与免重启解决方案 (K3s cgroup v1 failCgroupV1 Kubelet Troubleshooting & Doc Contract) | `k8s_deploy/README.md`, `tests/test_k8s_deploy_docs_contract.py`, `tests/CHECKLIST.md` | **K3s 单机部署 cgroup v1 避坑排查与免重启解决方案整理闭环**：① **高版本 K3s 启动报错归因**：梳理新版 K3s（如 v1.36.4+k3s1）默认禁止 Kubelet 在 cgroup v1 环境启动导致服务频繁重启的问题，提供 `stat -fc %T /sys/fs/cgroup` 检查命令；② **Kubelet drop-in 配置免重启/免重装方案**：整理 `/var/lib/rancher/k3s/agent/etc/kubelet.conf.d/10-cgroup-v1.conf`（`failCgroupV1: false`）配置步骤与服务重启验证命令；③ **契约测试断言保护**：在 `tests/test_k8s_deploy_docs_contract.py` 中补充核心排查命令与配置关键字断言，防止文档被意外精简遗漏。 | ✅ 契约测试用例断言补充；文档落盘；未代跑真实服务与启动脚本 | 2026-09-09 |
| 智能元数据导入向导支持取消操作、AI 识别请求中断与全链路取消日志 (Smart Metadata Import Wizard Cancel, Abort Controller & Cancellation Telemetry) | `frontend/src/components/metadata/SmartImportWizard.vue`, `frontend/src/api/metadata.ts`, `app/api/portal/endpoints/metadata.py`, `tests/CHECKLIST.md` | **智能元数据导入向导取消与中止闭环**：① **Step 1 底部操作栏新增取消按钮**：在向导第一步底部操作栏右侧「开始智能识别」左侧补齐「取消」按钮，与第二步风格保持统一；② **AI 识别中动态切换为「取消识别」**：识别/连接模型过程中取消按钮自动切换为「取消识别」，点击立即调用 `AbortController.abort()` 中止底层的 HTTP 分析请求，停止进度条与秒表计时，恢复初始状态并 Toast 友好提示；③ **右上角关闭按钮解除强阻断**：优化 `handleClose`，若用户在分析中点击右上角关闭或点击取消，先安全中止分析请求后再清理状态并退出弹窗，彻底根除用户被锁死在向导无法退出的问题；④ **前后端取消日志打点贯通**：前端控制台记录用户点击取消与请求中止的清晰日志，后端在 `tables/import` 捕获 `asyncio.CancelledError` 并向终端输出 `⏹️ [元数据智能导入] 客户端主动断开连接 / 用户取消了智能导入识别`；⑤ **严格类型检查通过**：`vue-tsc --noEmit` 0 报错通过。 | ✅ 前端 TypeScript 类型检查通过；无新增单测报错；未代跑真实服务与启动脚本 | 2026-09-11 |
| ChatBI 经验库未命中诊断增强与案例集检索测试模拟器 (ChatBI Few-shot No-hit Diagnostics & Case Library Search Test Simulator) | `app/services/chatbi_example_service.py`, `app/services/ai/runners/chatbi/few_shot.py`, `app/api/portal/endpoints/examples.py`, `frontend/src/views/ExampleManagement.vue`, `frontend/src/components/example/ExampleSearchTestModal.vue`, `tests/ai/runners/test_chatbi_modules.py`, `tests/api/portal/test_examples_search_test.py`, `FAQ.md`, `data/docs/FAQ.md`, `tests/CHECKLIST.md` | **经验库检索可观测与主动调参验证闭环**：① **few-shot 思考卡片未命中诊断增强**：为「未命中经验库案例」卡片透出检索过程诊断——实际检索模式（local Redis / ragflow）、检索词（含意图改写标注）、top_k、相似度阈值、向量召回数 / 阈值与可用 SQL 过滤后有效数、MySQL 关键词兜底命中数并列出实际拆分关键词、耗时；并按召回 0 条 / 有召回未过阈值两类归因给出引导注释；② **检索统计回传机制**：`ExampleService.search_examples` 新增可选 `stats_dump` dict 引用，在 local 与 ragflow 链路全程写入检索统计（模式、top_k、阈值、召回、过滤、兜底词），返回类型与默认行为完全不变；③ **案例集检索测试模拟器**：案例集管理工具栏新增「检索测试」入口并落地 `ExampleSearchTestModal` 弹窗（参照元数据管理 Schema 检索模拟器），输入用户问题后以后端同一检索链路执行，高级参数折叠区支持临时覆盖检索模式（跟随系统 / local / ragflow）、Top K（1~20）、相似度阈值（0~1）与向量权重（仅 ragflow 生效），左侧展示命中状态条与命中案例卡片（编号、相似度、数据集、SQL 预览），右侧逐条展示执行日志 Trace（检索模式、检索词、参数、召回、过滤、关键词兜底、耗时与 HIT/MISS 结论）；④ **后端检索测试端点**：新增只读 `POST /api/portal/examples/search-test`，`search_examples` 同步支持 `provider_override / threshold_override / vector_weight_override` 三个临时覆盖参数（默认 None 保持既有 few-shot 与工具行为不变），异常不外抛而转为 ERROR 日志行返回；⑤ **工具栏精简**：案例集管理导出与检索测试按钮去除文字改为纯图标方钮并保留悬停提示，一键同步保留文字并精简为「同步」，缓解工具行拥挤；⑥ **文档与测试**：FAQ 双端补充「案例集检索模拟器」使用说明；新增 8 项端点契约与 override 行为测试（命中/未命中/异常/参数透传/default 不强转/强制 local/阈值过滤差异），`vue-tsc --noEmit` 静态类型检查零报错。 | ✅ 8 项检索测试契约/单测新增通过，few-shot 与向量检索相关套件 69 项全量通过；`vue-tsc --noEmit` 零报错；未代跑真实服务与启动脚本 | 2026-09-09 |
| Kubernetes 原生 Pod 安全沙箱策略接入与生产级高可用加固（沙箱策略 k8s、并发多会话引用计数生命周期、subPath 物理目录预创建保护、未认证共享 PVC 隔离强拦截、资源保障 requests/limits、403 RBAC 优雅规避、系统管理 RBAC 指引卡片与一键自检校验按钮、双库迁移及部署文档） (Kubernetes Native Pod Sandbox Policy with Shared/Dynamic PVC, Concurrency RefCount Lifecycle, SubPath Dir Guard, RBAC Self-Subject Review & One-Click Check UI) | `requirements.txt`, `app/services/config_service.py`, `app/services/ai/agent_service.py`, `app/services/ai/runtime/agentscope/workspace.py`, `app/services/ai/runtime/agentscope/k8s_workspace.py`, `app/api/v1/endpoints/sandbox.py`, `db-prod/V152-add-sandbox-k8s-configs.sql`, `db-prod-pg/V53-add-sandbox-k8s-configs.sql`, `k8s_deploy/sandbox-rbac.example.yaml`, `k8s_deploy/README.md`, `FAQ.md`, `README.md`, `frontend/src/views/SystemConfig.vue`, `tests/frontend/test_sandbox_policy_icons_contract.py`, `tests/services/test_sandbox_policy_runtime_guard.py`, `tests/services/test_sandbox_policy_k8s.py`, `tests/test_k8s_deploy_docs_contract.py`, `tests/CHECKLIST.md` | **Kubernetes 原生 Pod 安全沙箱策略全链路闭环与 6 大生产缺陷深度加固**：① **并发多会话进程内生命周期管理（根治并发互相误删 Pod）**：对齐 Docker 模式引入 `_k8s_workspace_cache`、`_k8s_workspace_refcounts` 引用计数与 `_acquire_k8s_workspace` / `_release_k8s_workspace` 机制，同一用户多会话并发执行时递增引用计数，共享同一个存活 Pod；单个会话结束绝不中断同用户正在运行的其他会话，引用计数归零并达到 30 分钟空闲超时（`reap_idle_k8s_workspaces`）时才真正下发 Pod 物理销毁指令；② **共享持久卷 subPath 物理目录自动预建保护**：在 `NanZiK8sAdapter` 拉起沙箱 Pod 之前，自动通过本地挂载点确保 `agent_workspaces/{user_key}/sandbox` 与 `docs` 物理目录存在，彻底根除 Kubelet 挂载子路径不存在导致的永久 `ContainerCreating` 故障；同时销毁时严格保护共享 PVC 绝不误删；③ **未认证用户共享 PVC 隔离强前置拦截**：若未识别到合法 `sandbox_user_key`，强行拦截禁止挂载共享 PVC 根目录，消除未授权/匿名用户读取平台全量数据的越权漏洞；④ **资源调度配额补齐（requests + limits）**：在系统配置中增加 `sandbox_k8s_cpu_request`（默认 100m）与 `sandbox_k8s_memory_request`（默认 128Mi），并在适配器中自动兜底补齐 requests，杜绝 K8s 调度器按 limits（1000m/1Gi）等额提升 requests 造成集群算力被虚假占满、大量 Pod 长久 Pending；动态独立 PVC 默认开启 `delete_pvc_on_close = true` 杜绝存储泄漏；⑤ **规避集群级命名空间 403 权限冲突**：在 `_ensure_namespace` 中对 403 Forbidden 优雅降级容错，确保遵循最小权限原则绑定特定命名空间的 ServiceAccount 能平稳拉起沙箱；⑥ **系统管理操作指引与一键权限自检 (SelfSubjectAccessReview)**：在 `SystemConfig.vue` 的 `sandbox_policy`（选择 `k8s`）下方新增高质感操作引导卡片，展示清晰的授权操作说明与命令（`kubectl apply -f k8s_deploy/sandbox-rbac.example.yaml`）一键复制按钮；后端新增 `POST /api/v1/admin/sandbox/k8s/check-rbac` 管理接口，通过官方标准的 `SelfSubjectAccessReview`（零副作用、免额外特权）探测 API Server 连通性以及当前平台 Pod 在目标命名空间内创建 Pod 与 PVC 的权限；前端支持一键「⚡ 校验 K8s 集群与 RBAC 权限」，实时呈现检测详情与处置建议；⑦ **全量测试验证与代码质量**：20 项单元与契约测试 100% 绿灯全过，`npx vue-tsc --noEmit` 0 报错。 | ✅ 20 项定向单测、前端契约测试与 K8s 文档契约测试全绿通过；双库迁移脚本落盘；未代跑真实服务与启动脚本 | 2026-09-09 |
| OpenAI-compatible 提示词缓存布局与真实命中观测（稳定层前置、会话哈希分桶灰度、供应商真实 Cache Usage 归一化、系统配置 enabled 动态联动与调用明细弹框缓存指标呈现） (OpenAI-Compatible Prompt Cache Layout & Real Token Usage Telemetry) | `app/services/ai/prompt_assembler.py`, `app/services/ai/agent_prompts.py`, `app/services/ai/pipeline/steps/assemble_step.py`, `app/services/ai/runners/assistant_agent_runner.py`, `app/services/ai/runners/knowledge_agent_runner.py`, `app/services/ai/runners/chatbi/system_prompt.py`, `app/services/ai/runtime/agentscope/middleware.py`, `app/services/ai/runtime/agentscope/agent_runtime.py`, `app/services/ai/executors/common.py`, `app/api/v1/endpoints/chat.py`, `frontend/src/views/SystemConfig.vue`, `frontend/src/components/chat/ChatModelCallStatsModal.vue`, `db-prod/V151-add_prompt_cache_layout_configs.sql`, `db-prod-pg/V52-add_prompt_cache_layout_configs.sql`, `tests/ai/test_prompt_assembler.py`, `tests/ai/pipeline/test_pipeline_steps.py`, `tests/services/ai/test_base_executor_token_usage.py`, `tests/ai/runners/test_assistant_route_dedup.py`, `tests/ai/runtime/test_agentscope_runtime_foundation.py`, `tests/ai/test_model_call_context_breakdown.py`, `tests/frontend/test_prompt_cache_system_config_contract.py`, `docs/html/C04-prompt-cache-and-assembly.html`, `docs/html/C04-prompt-cache-and-assembly-plain.html`, `tests/CHECKLIST.md` | **提示词缓存优化与真实命中观测全链路闭环（含深度 Review 闭环优化与深度实战文章）**：① **稳定层前置（Stable Prefix Ahead）**：在系统提示词组装中确保平台固定规则与智能体发布版本提示词置于首部，动态时间锚点、会话记忆、路由决策快照及安全引导严格后置追加，彻底消除动态内容插入头部打碎 KV Cache 的缺陷；声明 Runner 级动态路由已统一收敛为后置追加，杜绝任何模式下动态内容插在第一行的缺陷；移除 HTML 伪标签缓存边界（避免污染大模型指令空间），严格保持执行语义与鉴权能力；② **三态策略与会话哈希分桶灰度**：支持 `legacy`（传统布局）、`observe`（仅观测指标不改布局语义）、`enabled`（按会话比例启用稳定层前置优化）三态配置，结合 0~100% 会话 ID MD5 确定性哈希分桶灰度，提供无缝安全切流；③ **真实 Usage 归一化与全链路 Observability 贯通**：在 `extract_tokens_from_message` 中标准化提取 OpenAI `prompt_tokens_details.cached_tokens` 与主流供应商返回的缓存命中 Token，绝不前端虚标；在 `assemble_step.py` 将 `effective_prompt_layout_mode` 同步打入 `AgentContext.runtime_model_info`，打通 `ModelCallStatsMiddleware` 记录与 API 透出；④ **三大 Runner 动态内容前置拦截与精准去重**：修复 `assistant_agent_runner` 路由去重关键词笔误，精准匹配 `本轮执行上下文（平台路由快照）` 与 `【本轮执行决策（仅供参考）】`，杜绝重复注入；⑤ **系统配置可视化管理、排序与 enabled 动态联动**：在 `SystemConfig.vue` 与双库 SQL 中纠正 `observe`（保持传统布局仅观测）与 `enabled` 描述文案；且**仅当策略选择 `enabled` 时才动态展示灰度比例滑块**，非 enabled 状态自动收起隐藏；⑥ **调用明细弹框指标升级**：在 `ChatModelCallStatsModal.vue` 顶部新增「缓存命中」统计卡片，高亮呈现命中数与命中率，单次调用日志以绿色微徽标清晰展示 `[ 命中: N (P%) ]`；⑦ **番外篇实战长文产出**：完成《NanZi 开源智能体平台实战连载》番外篇 C04（标准卡片版与克制配色版），系统剖析千万级 Token 降本与 KV Cache 落地工程；⑧ **双库幂等迁移**：提供 MySQL `V151` 与 PostgreSQL `V52` 系统配置种子；⑨ **自动化测试与类型检查**：82 项定向单测与前端契约测试 100% 绿灯全过，`vue-tsc --noEmit` 0 报错通过。 | ✅ 82 项定向后端与契约测试全绿通过；`vue-tsc --noEmit` 静态类型检查 0 报错；未代跑真实服务与启动脚本 | 2026-09-09 |
| 智能体版本配置 Diff 深度比对能力补齐（工具超时比对、欢迎卡片图标比对、双版本变动备注对比与模型温度标签优化） (Agent Version Diff Toolcall Timeout, Card Icon, Version Comments & Model Temperature UX) | `frontend/src/utils/agentVersionDiff.ts`, `frontend/src/components/agent/AgentVersionDiffModal.vue`, `frontend/scripts/agentVersionDiff.test.ts`, `tests/CHECKLIST.md` | **智能体版本配置 Diff 盲区补齐与体验优化闭环**：① **补齐工具调用超时比对（`toolcall_timeout_seconds`）**：在「工具」分组比对首项补齐单次工具调用超时对比，支持有数值格式化为 `${value} 秒`，未设置或为 null 时友好显示 `跟随全局配置`，杜绝超时时间变更在 Diff 中漏判；② **补齐欢迎卡片图标比对（`welcome.cards.0.icon`）**：在欢迎语比对中补充 `icon` 图标变动判断（`第 N 张卡片图标`），彻底消除仅比对文字忽视图标变动的缺陷；③ **模型采样温度与合成模型标签与回退语义升级**：将原模糊的「温度」与「合成温度」标签明确为「主模型采样温度」与「合成模型采样温度」，合成模型及其温度为空时友好回退为 `跟随编排模型`（与编辑抽屉语义对齐）；④ **顶部双版本备注（comment）对比卡片**：在 Diff 弹窗顶部同时渲染源版本变动备注与线上版本备注对比卡片，让版本演进与克隆来源一目了然；⑤ **自动化测试验证**：更新 `frontend/scripts/agentVersionDiff.test.ts` 补齐超时与图标变动断言全量通过，`npx vue-tsc --noEmit` 静态类型检查 0 报错通过。 | ✅ 前端 TypeScript 类型检查通过；单元测试全绿通过；未代跑真实服务与启停脚本 | 2026-09-09 |
| 元数据巡检「添加到数据集」新字段 AI 语义分析与可编辑收录预览 (Metadata Drift Add-Column AI Semantics Analysis & Editable Ingest Preview) | `app/schemas/metadata.py`, `app/services/metadata_drift_service.py`, `app/api/portal/endpoints/metadata.py`, `frontend/src/api/metadata.ts`, `frontend/src/components/metadata/MetadataDriftAlertsDrawer.vue`, `tests/services/test_metadata_drift_service.py`, `tests/CHECKLIST.md` | **元数据巡检新增物理字段收录时 AI 语义分析与可编辑确认闭环**：① **add_column 收录默认补充中文业务术语**：新增物理字段收录（添加到数据集）时，管理员可通过预览确认/编辑提交「业务术语 `term`、字段说明 `description`、同义词 `synonyms`」，优先级 管理员确认值 > 物理库注释 > 英文物理列名兜底，并同步写入与旧逻辑一致的物理类型/注释，彻底补齐旧实现仅存英文列名、无中文业务语义与同义词的缺口；② **新增 `POST /drift-alerts/{alert_id}/analyze-new-column` AI 语义分析端点**：`AnalyzeColumnRequest{with_samples=true}` 支持携带 0-3 条抽样值（按方言 `mysql/postgres/clickhouse`→`LIMIT 3`、`oracle`→`ROWNUM<=3`、`tsql`→`TOP 3`），结合同表既有列业务术语作为少量样本上下文，调用 LLM 生成 `term/description/synonyms/ai_error`；LLM 失败**非阻塞**，返回 `llm_succeeded=false` 并附带可读错误，前端回退到默认收录；③ **前端可编辑收录预览弹窗**：点击「添加到数据集」先弹出「收录预览」Modal，展示物理定义摘要、抽样值、同表兄弟字段术语与 AI 建议，管理员可编辑三项后再确认收录，LLM 失败时保留默认兜底并可继续收录，取消则中断；④ **批量收录（一键收录）保持直落**：批量 `add_column` 仍为直接落库、不做 AI 预览，避免批量场景阻塞；⑤ **自动化测试覆盖**：`test_analyze_new_column_ai_llm_success`、`test_analyze_new_column_ai_llm_failure_falls_back`、`test_analyze_new_column_ai_alert_not_found`、`test_resolve_alert_add_column_with_confirmed_term` 四例，18 项服务单测 + 4 项契约测试全绿。 | ✅ 前端 vue-tsc 对改动文件 0 报错；18 项服务单测 + 4 项契约全绿；未代跑真实服务与启停脚本 | 2026-09-12 |
| 元数据巡检新增「字段备注缺失」巡检类型与 AI 补充分析/回填 (Metadata Missing Comment Inspection & AI Comment Backfill) | `app/services/metadata_inspection_service.py`, `app/services/metadata_drift_service.py`, `app/schemas/metadata.py`, `app/api/portal/endpoints/metadata.py`, `frontend/src/api/metadata.ts`, `frontend/src/components/metadata/MetadataDriftAlertsDrawer.vue`, `tests/services/test_metadata_inspection_service.py`, `tests/services/test_metadata_drift_service.py`, `tests/CHECKLIST.md` | **元数据字段备注缺失（missing_comment）巡检与 AI 补充闭环**：① **新增第 5 类巡检告警 `missing_comment`**：巡检逐列比对时，元数据字段 `description` 为空或等于字段名（占位未填）即识别为备注缺失并记录告警，同步计入 diff_summary 与统计；② **`POST /drift-alerts/{alert_id}/analyze-update-comment` AI 补备注**：`analyze_update_comment_ai` 优先采用物理库已有非空注释（`from_source='physical'`，免 LLM），无有效注释时结合现有业务术语、类型、抽样值与同表兄弟术语调用 LLM 推断中文描述（`from_source='ai'`）；均无效返回 `none` 非阻塞；③ **`update_comment` 处置保留现有 term 仅更新 description(+synonyms)**：`_resolve_single_alert_core` 新增 `update_comment` 分支，优先级 管理员确认 > 物理注释 > 保持现有；④ **批量「一键补备注」仅回填有物理注释的**：批量直落时用 `_fetch_physical_comment` 预查，物理库有有效注释才回填并标记处置，无注释自动跳过（返回 `skipped_count`），不做 AI 预览；⑤ **前端完整接入**：新增「备注缺失」筛选 Pills、统计、批量按钮、告警行「✏️ 补备注」动作，及描述预览弹窗（展示物理注释来源 / AI 建议 / 降级手动填写，可编辑确认）；⑥ **自动化测试**：新增 `analyze_update_comment_ai` 物理注释优先/LLM 成功/告警不存在、`update_comment` 确认描述落库 4 项；既有巡检测试补齐 description 适配新行为；10 巡检 + 22 漂移服务 + 4 契约全绿，前端 vue-tsc 改动文件 0 报错。 | ✅ 前后端全量相关测试通过；改动文件 vue-tsc 0 报错；未代跑真实服务与启停脚本 | 2026-09-12 |
